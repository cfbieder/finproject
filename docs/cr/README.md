# CR Index

Status legend: **COMPLETED** · **IN-PROGRESS** · **OPEN** · **PLANNED** (scoped, not yet started) · **DEFERRED** (scoped and reviewed, deliberately not scheduled) · **SUPERSEDED** / **OBSOLETE** (replaced or no longer relevant)

Each CR file's first line carries its status and links back to the matching anchor in [NEXT_STEPS.md](../current/project-roadmap.md). New CRs get the next available number. **This index is a one-line roll-up — the CR file is the spec; keep descriptions to a single line.**

**Version track:** **v4** = the unreleased multi-tenancy line — **CR027** + sub-CRs **CR027A–E** (flag-gated/dormant on `main`; see [DEV_WORKFLOW.md](../guides/dev-workflow.md)). Everything else is v3 / current.

## Summary by status

*Manual roll-up — update when a CR's status changes (alongside its row + CR-file header).*

| Status | Count | CRs |
|--------|------:|-----|
| COMPLETED | 64 | CR001–CR013, CR016, CR017, CR018, CR024, CR025, CR026, CR028, CR030, CR031, CR032, CR033, CR034, CR035, CR036, CR037, CR038, CR039, CR040, CR041, CR042, CR043, CR044 *(decision record)*, CR045, CR046, CR047, CR049, CR051, CR053, CR054, CR055, CR056, CR057, CR058, CR062, CR063, CR065, CR067, CR068, CR069, CR070, CR071, CR072, CR073, CR074, CR075, CR078, CR079, CR080, CR082, CR084, CR085 |
| IN-PROGRESS | 10 | CR019, CR022, CR023, CR050, CR059, CR060, CR064, CR076, CR077, CR083 |
| OPEN | 4 | CR020, CR021, CR048, CR066 |
| PLANNED | 4 | CR027 *(v4, umbrella)*, CR029, CR052, CR061 |
| DEFERRED | 1 | CR081 |
| SUPERSEDED | 1 | CR014 |
| OBSOLETE | 1 | CR015 |
| **Total** | **85** | |

## All CRs

| # | Status | Track | Title | One-line description |
|---|--------|-------|-------|----------------------|
| [CR001](cr-001-migration-mongo-to-postgres.md) | COMPLETED | v3 | MongoDB → PostgreSQL Migration | Moved all storage to PostgreSQL 16; V1 routes and `coa.json` removed. |
| [CR002](cr-002-frontend-refactor.md) | COMPLETED | v3 | Frontend Architecture Refactor | God components decomposed; `features/` module pattern; ~22 duplicate transaction files deleted. |
| [CR003](cr-003-forecast-module.md) | COMPLETED | v3 | Forecast Module | Engine + UI for BS modules and inc/exp items across all phases. |
| [CR004](cr-004-fc-lines-mapping.md) | COMPLETED | v3 | FC Inc/Exp Mapping Layer | FC Lines decouple budget categories from forecast outputs (migration 007). |
| [CR005](cr-005-cash-sweep.md) | COMPLETED | v3 | Cash Sweep & Auto-Balance | Iterative sweep with income↔sweep convergence loop (migrations 012–013). |
| [CR006](cr-006-ai-review.md) | COMPLETED | v3 | AI Review of FC Plan | Conversational review via local `ocr-llm` gateway; async + notifications (migrations 014, 020). |
| [CR007](cr-007-pwa-mobile-shell.md) | COMPLETED | v3 | PWA & Mobile Simplified Shell | Installable PWA + dedicated `/m/*` mobile experience. |
| [CR008](cr-008-hierarchy-filter.md) | COMPLETED | v3 | HierarchyFilter & Transaction Pages Redesign | Two-stage cascading filter; `/trans-actual` + `/trans-budget` redesign. |
| [CR009](cr-009-transfer-analysis.md) | COMPLETED | v3 | Transfer Analysis + Manual Match Groups | Auto + manual transfer matching; `transfer_matched` flag (migrations 005–006). |
| [CR010](cr-010-coa-management.md) | COMPLETED | v3 | COA Management Redesign + Move | Tree view editor with toolbar, inline actions, quick-add, Move modal. |
| [CR011](cr-011-source-mappings.md) | COMPLETED | v3 | Source Mappings (Category + Account) | Decouples external system names from internal names (migrations 018–019). |
| [CR012](cr-012-opening-balance-calibration.md) | COMPLETED | v3 | Opening Balance Calibration | Balance sheet via `opening_balance + SUM(transactions)` (migration 016). |
| [CR013](cr-013-collapse-categories.md) | COMPLETED | v3 | Collapse `categories` into `accounts` | Single COA source of truth; FKs repointed; legacy table dropped (migration 021). |
| [CR014](cr-014-pocketsmith-replacement.md) | SUPERSEDED | v3 | PocketSmith Replacement | In-app dual-provider plan; superseded by CR021 (microservice). |
| [CR015](cr-015-ps-reexport.md) | OBSOLETE | v3 | Re-export Changes Back to PocketSmith | Obsoleted by CR021 (PS removed entirely). |
| [CR016](cr-016-frontend-test-framework.md) | COMPLETED | v3 | Frontend Test Framework (Vitest) | 96 unit tests across 5 helper modules. |
| [CR017](cr-017-cash-sweep-phase-c.md) | COMPLETED | v3 | Cash Sweep Phase C — Multi-Module Priority | Priority-ordered sweep set with backup cascade (migration 031; v3.0.25). |
| [CR018](cr-018-balance-trends.md) | COMPLETED | v3 | Balance Trends Report | Period-end USD balances for selected BS accounts over a chosen range. |
| [CR019](cr-019-quicken-import.md) | IN-PROGRESS | v3 | Quicken Historical Import | One-time pre-2022 backfill (cash done; investment value-only); prod cutover via per-account live loop; owns the schema CR020 needs. |
| [CR020](cr-020-stock-investment-module.md) | OPEN | v3 | Stock Investment Module | Lot-level holdings + portfolio analytics on the CR019 schema (planning skeleton). |
| [CR021](cr-021-bank-feed-service.md) | OPEN | v3 | Bank Feed Service | Standalone microservice (fintable.io/Sheets upstream) behind a versioned `/v1/*` contract; 2 consumers. |
| [CR022](cr-022-bank-feed-parallel-import.md) | IN-PROGRESS | v3 | Bank Feed Parallel Import | Second import route (`source='bank-feed'`) alongside PS; Phases A–F done, Phase G observation (migrations 023–024). |
| [CR023](cr-023-pocketsmith-removal.md) | IN-PROGRESS | v3 | PocketSmith Removal & PS→Feeds Cutover | Reusable cutover engine (PS-side cutoff, source-aware reconcile, feed signs); 28 fed / 2 manual; 13-account tail + deferred PS removal (migrations 028–029). |
| [CR024](cr-024-fidelity-feeds.md) | COMPLETED | v3 | Fidelity Feeds | Market-value balance read-override + categorized investment-activity import (migrations 025–027). |
| [CR025](cr-025-manual-transaction-entry.md) | COMPLETED | v3 | Manual Transaction Entry | `/manual-entry` rapid hand entry (`source='manual'`, accepted) (v3.0.4). |
| [CR026](cr-026-ui-revamp.md) | COMPLETED | v3 | UI Revamp — Sidebar, Dark, ⌘K, Mobile, Help | Sidebar + dark mode + command palette + help + mobile read pages; ON in prod since v3.0.0. |
| [CR027](cr-027-multi-tenancy-final-release.md) | PLANNED (umbrella) | **v4** | Multi-Tenancy & Final-Release Readiness `[v4]` | Schema-per-tenant multi-user SaaS conversion; split into CR027A–E; needs a migration runner first. |
| [CR028](cr-028-securities-trade-neutralization.md) | COMPLETED | v3 | Securities-Trade Neutralization & Orphan Management | Pair-or-mirror neutralize with dry-run warning; orphan Remove/Neutralize on Transfer Analysis (v3.0.1). |
| [CR029](cr-029-fintable-sheet-pruning.md) | PLANNED | v3 | Fintable Sheet Pruning | Guarded bank-feed admin prune of old Sheet rows (Postgres is the archive). |
| [CR030](cr-030-automated-ps-retirement.md) | COMPLETED | v3 | Retire automated PocketSmith | PS-API integration + legacy calibration removed; one-time CSV upload kept (v3.0.11). |
| [CR031](cr-031-ledger-filter-parity-year-range.md) | COMPLETED | v3 | Ledger Filter Parity + Year-Range | Ledger adopts HierarchyFilter/PeriodSelector; multi-year Custom ranges (v3.0.12). |
| [CR032](cr-032-core-cash-sweep-neutralization.md) | COMPLETED | v3 | Fidelity Core-Cash Sweep Auto-Neutralization | Promote auto-mirrors core sweeps so they self-net; neutralize mis-pair guard (v3.0.27). |
| [CR033](cr-033-manual-calibration.md) | COMPLETED | v3 | Manual Calibration (non-fed accounts) | Computed vs user-typed balance with calibrate/MTM per BS leaf (migration 032; v3.0.29–33). **Reset opening** added v3.6.8. |
| [CR034](cr-034-security-hardening-ci.md) | COMPLETED | v3 | Security Hardening & CI Baseline | Secrets untracked + DB password rotated, compose/ports/CORS/pg_dump hardened, fresh-install migration fix, GitHub Actions CI + secret-scan gate, docs restructure (2026-06-12). |
| [CR035](cr-035-feed-sync-freshness.md) | COMPLETED | v3 | Feed Sync Freshness | Promote fintable's per-connection "⚡ Last Update" through the service → fin so recon shows true "synced N days ago" (corrects v3.0.43's fin-poll timestamp); CR021 Phase 5 slice (v3.0.44). |
| [CR036](cr-036-manual-statement-upload.md) | COMPLETED | v3 | Manual Statement Upload (Stale-Feed Fallback) | Upload any bank's CSV as `source='manual'`: auto-detect (built-in + saved profiles) or the P2 interactive column mapper (columns/date-format/sign/currency + typed statement balance, save-as-format); imports only-new rows, reconciles to the stated balance (P1 v3.0.45, P2 v3.0.59 + bank-feed migration 004). Realizes CR021 Phase 4. P3 (LLM mapping-guess/OCR) optional. |
| [CR037](cr-037-correctness-hardening.md) | COMPLETED | v3 | Correctness Hardening (money & date) | Silent-wrong-number batch from the 2026-07-03 design review: TZ date-pattern sweep + eslint guard, split penny-leakage residual fix, `parseCurrency` fail-loud, global error boundary, transactional forecast writes, field-whitelist validation on money-writing endpoints (v3.0.54). |
| [CR038](cr-038-home-dashboard-attention.md) | COMPLETED | v3 | Home Dashboard & Attention Surface | Desktop Home gets the MobileHome live KPIs (via shared `useOverview`) + a "needs attention" strip (unreviewed tx / stale feeds / MTM-due / drift / KI#7 verify rows) + next-step prompts + `/m/reconcile` mobile page tying the weekly refresh→review→reconcile loop together (v3.0.55–57). |
| [CR039](cr-039-forecast-assumptions-to-db.md) | COMPLETED | v3 | Forecast Assumptions to Postgres | Retired the `FCAssump.json` dual source: `forecast_assumptions` document store (migration 034, `json` for byte-parity), byte-identical API + engine checksum parity verified; CSV-gating dropped (audit trail reads them). Clears CR027's assumptions prerequisite (v3.0.58). |
| [CR040](cr-040-forecast-scenario-compare.md) | COMPLETED | v3 | Forecast Scenario Compare | `/forecast-compare`: pick two scenarios, delta KPIs + P&L/BS delta tables (client-side diff reconciling with Review incl. bank running balance), recharts A-vs-B lines + diverging delta bars (validated palettes), deterministic movers commentary + on-demand local-LLM narrative with follow-ups (aiReview `compareWith`, compare prompt, no action blocks; migration 035 `compare_scenario_id`). Released v3.0.60 (2026-07-10); v3.0.61 fix — one-scenario-only accounts (disposed/added assets) now diff as zero-not-null so they render. |
| [CR041](cr-041-module-ownership-gating.md) | COMPLETED | v3 | Module Ownership-Gated Expenses/Income + Edit-Form Regrouping | BS-module expense/income streams start at first ownership (auto-derived from MV/first Invest; 50% in the acquisition year, mirroring Full-disposal) instead of base year — fixes future-purchase assets accruing running costs pre-purchase; `FCModulesEdit` fields regrouped into titled General/Valuation/Expenses/Income/Tax sections. No migration (v3.0.62). |
| [CR042](cr-042-ui-look-and-feel.md) | COMPLETED | v3 | UI Look & Feel Modernization | 2026-07-11 design review: token gaps (dead tokens, no type scale, green does 4 jobs), IA consolidation (35 pages → ≤8 nav items), Home net-worth hero, DataTable/Modal primitives with adoption guardrails, chart-theme convergence; direction = refine Mindful Minimalist, not rebrand. **U1–U5 all shipped v3.0.69→v3.0.97 and owner-accepted** (tokens/green/elevation, chart theme, Home hero, Modal+DataTable primitives with 4 blocking CI guards, reports 9 pages→3). T1 reopened en route: 63 `var(--…)` refs across 30 files pointed at tokens that were **never defined** — they lint clean and silently ignore the theme; all remapped, zero-baseline guard added. **CLOSED v3.0.101** — the final IA items settled 2026-07-13, and two of the CR's own proposals were rejected on inspection: calibration pages **stay** under Transactions (they are recurring *work*, not config — only Bank Feed Setup + Forecast Settings moved), and the Forecast sidebar is **mirrored to the stepper, not collapsed** (collapsing would cost a click on every visit to the busiest area; the real defect was two hand-kept lists that disagreed, so FCStepNav now derives from `routes.jsx`). "Upload PS": KEEP. Creating a module no longer writes before you save. |
| [CR043](cr-043-code-structure-program.md) | COMPLETED *(Phase 4 long-horizon)* | v3 | Code Structure & Architecture Improvement Program | 2026-07-11 review: 14 new findings (vitest never in CI; non-transactional forecast rebuild; docker.sock mount; dead TF/arquero deps; migration runner pulled from CR027A) + verified-debt corrections, sequenced into Phases 0–4 (safety nets → guardrails → extraction → frontend consolidation). **Phases 0–3 shipped v3.0.63→v3.0.97**, incl. transactional forecast rebuild + advisory lock, migration runner, route→service extraction, danfojs/TensorFlow removed, TanStack Query, and **N10** — the forecast write API now **rejects unknown fields** instead of silently dropping them (which is how CR046/CR047 shipped broken); enumerating the contracts found 4 dead keys posted for months to columns that don't exist. **First restore drill held v3.0.96 — PASSED.** **CLOSED v3.0.104:** the **lint gate is BLOCKING** (errors 0; the 7 that remained were real — 2 rules-of-hooks, 4 refs-during-render, 1 render-mutation — and the 56 warnings are baselined by `check-lint-debt.sh`, which may only shrink); **N8** unified the response envelope behind `Rest.unwrap()`, migrated with **no flag day** (`check-api-envelope.sh` ratchets the remaining 27 bare endpoints); **util.js** 651→32 lines behind 12 new contract tests — which found that `POST /coa/update` had been **silently dropping the account type** for months. Only Phase 4 (TypeScript, Playwright) remains, long-horizon. |
| [CR044](cr-044-productization-marketability.md) | COMPLETED | v3 | Productization & Marketability Assessment | 2026-07-11 consulting memo + decision gate: market scan validated the forecast+privacy differentiator but the audience is a niche-of-a-niche; **owner decided same day: stay personal** — release-only moves dropped, no-regret moves (pluggable LLM, SimpleFIN, demo seed, migration runner) kept as backlog. Retained as decision record. |
| [CR045](cr-045-forecast-cash-warnings-liquidation.md) | COMPLETED | v3 | Forecast Cash Warnings & Forced Liquidation | Owner saw the bank line dive to −$3.7M: `copyScenario` never copied `cash_sweep_priority`, so every copied scenario ran with no sweep module and left shortfalls unfunded (prod: 14 years / −$20.1M → 3 / −$3.35M once re-ranked). Phase 1 ships a cash-health warnings pane on `/forecast-review` (the engine wrote 14 shortfall entries and the UI showed none); it immediately found Phase 1b — the sweep never applied the BaseYear cash flow, so it held the band ~a year of NCF above the displayed balance. Phase 2 (shipped v3.0.80): capital-gains tax on forced sweep sales, sold funds stop compounding, and the sweep may never drive a module below zero in any future year (a module that was both a sweep source and had scheduled disposals was sold twice). Engine byte-parity deliberately broken; prod regenerated. |
| [CR046](cr-046-module-income-window-and-hierarchy-graph.md) | COMPLETED | v3 | Module Income/Expense Window + Hierarchy Breakdown Graph | Start/end dates on a module's income and expense streams (migration 037) — "start charging rent in 2030" was inexpressible; the window bounds *when* a stream runs, not how much, NULL = the old behavior. Plus: double-clicking any Forecast Review row now graphs the accounts beneath it as a stacked bar (BS and P&L), which only "Net Assets" used to do. Shipped v3.0.81. |
| [CR047](cr-047-module-income-tax-override.md) | COMPLETED | v3 | Income-Only Tax Rate Override | `tax_rate_override` moved BOTH the capital-gains and the income rate, so "the dividend is already taxed in Poland — only 3% incremental US tax, but a sale is still a normal gain" was inexpressible. Migration 038 adds `income_tax_rate_override` (income only; NULL falls back ⇒ no change; 0 is a real rate). Shipped v3.0.84. |
| [CR048](cr-048-model-review-fixes.md) | OPEN | v3 | Forecast Model Review: Engine Fixes + Ratified Assumptions | Full conceptual review of the model. Fixed: sweep-drained BACKUPS kept paying dividends on money that was gone (convergence loop was primary-only; ~$25K/yr phantom income in the shortfall years), cost basis spent twice between sweep and scheduled sales, CR047's income rate ignored on rebuild. Ratified: 0% cash (deliberate), flat-tax world (Fidelity = brokerage), equity growth to be tested in a scenario copy, FX stress folds into Downside. Shipped v3.0.90. |
| [CR049](cr-049-forecast-base-year-seed-and-final-year-tax.md) | COMPLETED | v3 | Forecast: Base-Year Seed Duplication + Final-Year Sweep Tax | One owner question ("why isn't the sweep taking from Fidelity Stock?") exposed two silent-wrong-number bugs. The **final-year** liquidation never funded its own capital-gains tax — 2062 pulled $852K from Fidelity Stocks, restored the band, then took a $205K tax bill with nothing sold to cover it, ending at **−$60,521 cash beside $4.3M of sellable stock** (no shortfall entry: the band check had already passed). And the engine kept a **hand-copied base-year query** that had silently drifted from `crud.getBaseYearValues` — an `account_type = 'liability' … ELSE 0` gate zeroed every non-liability module expense, so 2026's $64,717 of Property Costs never left the bank and the sweep's opening cash ran $64,717 rich for the whole horizon. Fixes: re-entrant drain (sell → pay tax → re-check the band, converging in a few passes) and one query, one caller. Shipped v3.0.94; prod regenerated. Known issue left open: "2026 Downside" has no sweep backup ranked. |
| [CR052](cr-052-forecast-expense-fx-exposure.md) | PLANNED | v3 | Partial FX Exposure on an Expense Line | CR051's completion for **matched/auto-loaded aggregates**: tag "X% of this line is PLN/EUR" (matched or not) instead of splitting into hand-maintained lines. The % fixes a native amount at base year and floats with FX (owner-chosen); engine applies a per-year FX factor `(1−Σpct)+Σ pct·FX₀/FXᵢ` (byte-identical when no exposure / flat FX). New child table + migration 040; coexists with CR051's whole-line currency (denomination vs partial exposure, orthogonal). |
| [CR051](cr-051-forecast-expense-currency.md) | COMPLETED | v3 | Foreign-Currency Expense Lines | Some spending is really PLN/EUR, so its USD cost floats with FX — invisible as a flat USD number. The rails already existed (the `currency` column, the engine's native-then-convert path, Modules' picker); the Expenses modal never exposed them and the CRUD hook hard-pinned USD. **Shipped v3.2.0 (2026-07-15):** USD/EUR/PLN selector (expense-only, disabled while Matched) + native-amount entry with `base_value_usd` derived server-side at base-year FX (client value ignored; 400 if no FX rate); **F1** engine guard fails loud on a missing/zero FX rate (was latent divide-by-1.0/Infinity). 407 backend / 195 frontend / 7 e2e green. No migration. |
| [CR050](cr-050-forecast-scenario-variants.md) | IN-PROGRESS | v3 | Forecast Scenario Variants (inherit-unless-overridden) | A scenario copy is a severed island: 30 hand-enumerated entities duplicated to change one field, and nothing records which field made it a downside — so the copies rot (fix a basis in Base and "2026 Upside" keeps the wrong one) and the enumeration itself has silently dropped columns **twice** (CR045 §1 `cash_sweep_priority` ⇒ −$3.35M unfunded; CR048 the whole assumptions slice ⇒ 0% inflation). CR050 adds `parent_scenario_id` + a `forecast_scenario_overrides` **JSONB patch keyed to the base row's id** (field-level: NULL is already load-bearing in these columns, so "NULL = inherit" is unavailable), and a **sync step that materializes base ⊕ overrides into real rows** — so the engine, Review, Compare, AI review and the audit CSVs all keep reading an ordinary scenario and **do not change**. Sync's column list is derived from `information_schema`, which is the direct fix for the drop-a-column bug class. Also lands the forecast API's first real scenario-create route (creation is currently a side-effect of `PUT /assumptions`). Migration 039, all-nullable ⇒ parentless scenarios behave exactly as today. **Shipped v3.0.108, rolled up into the v3.1.0 minor** (fixes v3.0.110–112 — all in how edits are *captured and reported* as overrides: DATE-as-instant, float noise, missing schedules, and the assumption-editing UI bypassing the override system, a silent-data-loss bug — never in the inheritance machinery). Overrides captured at write time across modules, income/expense, schedules and assumptions (FX/inflation/tax/sweep); Scenarios-page panel with per-field revert. Owner still to run `adopt-variant` on "2026 Downside" (22 diffs — **8 differ only in `base_value_usd`**: the copy having rotted, not a downside assumption) and "2026 Upside". **§10 (v3.11.0)** ships §5's unbuilt half: `GET /assumptions` now carries `ParentId`, so all **seven** scenario pickers indent each variant under its own base (`↳`) instead of listing four peers flat, and the lineage panel answers *what differs* from the base side too. |
| [CR054](cr-054-cash-flow-by-account.md) | COMPLETED | v3 | Cash Flow "By Account" report | Third Cash Flow tab (`/cash-flow/by-account`): By-Period layout + Budget-Worksheet category/account filter chips + USD⇄original-currency toggle (mixed-currency Original total flagged). Additive backend params on `GET /cash-flow` (`category`/`accounts`/`currency`, `meta.currencies`), backward-compatible. No migration. Shipped v3.4.0. |
| [CR055](cr-055-category-suggest-backoff.md) | COMPLETED | v3 | Category-Suggest Progressive Backoff | The "Suggest categories" key was a fixed 3 tokens, so `AMAZON MKTPL*<orderid>` fragmented into ~177 unique keys and Amazon was never suggested despite 177 categorized rows; now keys at 3→2→1 tokens and takes the most-specific level that clears the ≥2-sample/>50%-majority bar (Amazon → `amazon mktpl`, 92%), guards unchanged. No migration. Shipped v3.4.3. |
| [CR056](cr-056-investment-returns.md) | COMPLETED | v3 | Investment Returns report | `/investment-returns`: pick account (parent rolls up) + period + interval → realized income and unrealized G/L per interval, absolute (USD⇄LC) and as **realized / unrealized / total %** on average capital `(open+close)/2`. Ledger-derived because the CR019/CR022 securities tables are **0 rows** — the numerator is the existing `Unrealized G/L` postings + auto-derived income categories, MV on the Balance Sheet's additive basis. Pass-1 review (verdict *revise*, 6 blocking, all reproduced against prod) forced rev 2: buckets made **exhaustive** so the identity closes by construction (rev 1 was off **$8,781.87** on Fidelity IRA 2025), `Transfer - Securities Trades` **reclassified as a flow** (legs pair, so the unpaired remainder is real deposits — Fidelity Stocks 2020 **+$200,460.86**), whole-period Dietz fallback **removed** (+2,064% headline), "mark-bearing" replaced by **mark coverage** (a never-revalued €422K property was showing 0.00%). Doubles as a data-quality instrument: an `Unattributed` row surfacing miscategorized ledger rows; **read-only**, no correcting entries. **Four more rules fell to the owner clicking the built page:** the page rendered in two columns (`.page-main` is a hard-coded 2-col grid — five pages now carry an undo-class, logged as roadmap §4.1.7); Dietz → **average capital `(open+close)/2`** with **three** return rows; the coverage rule was incoherent (the table printed UB's +1,151,997 while suppressing the % that same posting implies) so a period is now covered when a valuation falls *inside* it, with misdating disclosed via **†** not hidden; and a fourth interval **`marks`** lays columns out between valuations, which is correct by construction. **Shipped v3.5.0.** No migration. |
| [CR053](cr-053-forecast-auto-adjust-spend-to-fund.md) | COMPLETED | v3 | Forecast Auto-Adjust: solve spend-down to fund the plan | When the sweep runs out of assets to sell, pick a **set** of expense lines and the system solves the **least uniform % cut** that makes **no** year breach the low band (threshold search on the cut fraction, full-engine per eval — the cap-gains-tax feedback rules out a closed form; targets the *tightest* year, not the final one; objective read server-side from the engine's persisted `Cash Shortfall` entries, never the client util). v1 = "slow solver" (~7–10 sequential builds on a **standalone deep-copy** scratch — *not* a throwaway variant: a variant-of-a-variant is trigger-rejected and a variant is force-synced on build; audit-CSV off; async job+poll). Apply persists as a CR050 override `expense_amount = effective × p*` on a variant (base target ⇒ create one), then a **verification rebuild** proves it. Senior-review-revised then **built + verified end-to-end on the dev DB** (both solve/apply paths, HTTP job+poll, clean scratch teardown; "2026 Downside (variant)" $451K → ~2% cut → funded); new service + 4 routes + `FCAutoAdjustModal`, 9 new tests. **Shipped v3.3.0 (2026-07-16).** No new schema. |
| [CR057](cr-057-book-income-at-source.md) | COMPLETED | v3 | Book Income at Source (holding-attributed distributions) | Investment income is booked where the **cash landed**, not on the holding that earned it — so `/investment-returns` reports United Beverages at a confident **0.00%** against ~25M PLN of average capital. A per-row **Book at source** action in the Ledger writes a **three-leg booking** (income at the holding + transfer out of the holding + the original row re-categorized as the transfer in), leaving the holding's book value, PKO's balance, net worth and every P&L category total unchanged while moving the *attribution*. Overturns CR056's P2 "Distributions received elsewhere" mapping — that objection only ever covered a one-legged change; with the transfer leg, `Δ totalReturn = 0 − (−X) = +X = Δ income` and the buckets close by construction. Scope: 7 UB + Barkeria rows (5,319,292.39 PLN / 1,380,393.02 USD); CVC deferred (cross-currency: rows USD, funds EUR), `Transfer - Historical` (2,741 rows) explicitly a different shape — it is *not* balance-neutral and each leg needs a paired mark adjustment. **Migration 041** (`income_restatements` + the `Transfer - Distributions` COA row) — reusing `transfer_match_groups` was rejected: its user-facing Unlink would delete the audit trail, and group membership *prevents* the auto-match it was meant to record. Pass 1 **revise** (5 blocking — headline: the verification plan was a tautology, since `fxEffect` is a plug so the identity closes for any input); pass 2 **GO**. **Built + verified on a prod copy on dev (2026-07-27):** flows/income moved by the exact amounts, MVs and **`FX effect` unchanged to the cent**, book values and the balance-sheet/cash-flow reports byte-identical, marks untouched, round-trip clean, undo refuses an edited leg, Transfer Analysis auto-matches 2/2. **Shipped v3.6.0** (migration 041). 494 backend / 195 frontend / build ✓ / six guards ✓. |
| [CR058](cr-058-quicken-valuation-anchors.md) | COMPLETED | v3 | Quicken-era valuation anchors (brokerage history) | Anchors each pre-PocketSmith year-end of a **brokerage** account to Quicken's own Net Worth Report instead of letting the balance drift on cash flows that never see market value. **Shipped to dev 2026-07-28 and prod 2026-07-29** — 27 anchor rows on Fidelity Stocks (27), all **26 target dates tie to the cent** on both, today's balance unchanged at 1,157,037.74, `quicken-verify` 7 pass / 2 expected warnings / 0 failures. Adds migration **042** (`Valuation - Historical` leaf + `calibration_mode`) and a **`preserve-today`** calibration mode for feed-owned accounts. Prod was loaded via `copy-quicken-to-prod.js` (CR019 §23 G2), not a re-upload. Unblocks the [CR019](cr-019-quicken-import.md) §24 backfill for that account. Scope then widened to **all four Fidelity accounts** (2026-07-30): Stocks **47** targets, IRA **41**, Cash Mgt **22**, Bond **8**, every one tying to the cent on dev and prod, via **valuation-only anchor sets** (`--valuation-set` / `--clear`) for the accounts with no Quicken import to inherit a batch from. **§12 (2026-07-31) restates the 2026 month-end marks**, which the anchors do not cover: `mtm()` marked June against a feed balance flat for three days, and the settled close turns out to arrive on the row dated **month-end + 2**, matching the custodian's statement on all four accounts. A stale mark cannot be re-marked (the feed row for that date *is* the stale value), so `restate-mtm.js` rewrites it from the statement — into `Unrealized G/L`, not `Valuation - Historical`, because a stale feed understates exactly one thing. Checking March first split the accounts: Stocks and IRA tie to under a dollar, so June is isolated for them; Bond and Cash Mgt were already adrift and Bond is pinned at **both** quarter-ends. Applied to prod; all four anchor sets re-`--check` clean. **§12.10–12.11 (2026-07-31)** closed two more of the same class. **Stocks read up to 258K high across 2023** — the **handoff reversal** is itself a plug (+321,173.66 in one day), right at exactly one date and wrong elsewhere; it went unnoticed because it *cancelled* by 2025 against two years of unbooked gains. Eight quarter-ends anchored, today unchanged; a sweep of all four accounts against **113** statement dates found no second instance. Then **attribution**: `Valuation - Historical` is `is_transfer`, so CR056 was reporting the anchors as **capital contributions** (+163,865 inside Stocks' 2023 flows). `attribute-unrealized.js` writes a balance-neutral pair per period, moving **291,910 (Stocks) / 119,035 (IRA)** from `flow` to `price` with no balance changing by a cent — Unrealized G/L and return percentages now exist for 2020–2024 where the report showed 0 and a dash. Open: Fidelity Options has **no statement** (markable, never anchorable); Stocks' three 2025 quarter-ends (worst 551.92); whether the +2 lag is calendar or business days; 1998–2015 has no statement coverage so those anchors stay flow-classified. |
| [CR059](cr-059-fintable-api-ingestion.md) | **DONE** | v3 | Fintable API ingestion (retire the Google Sheets adapter) | **LIVE since 2026-08-10 — ingest is the REST API exclusively** (`FINTABLE_SOURCE=api`; every sync since cutover ran `source=api`, none used the Sheet). The Sheet stays reachable only as the **rollback** and the **hourly equivalence watchdog** (throwaway store, not in the daily path); retirement and the 2026-08-31 exception expiry are dated items in the roadmap. Fintable serves its own **REST API (V2)**, so bank-feed stopped scraping the Google Sheet it exports into: the API carries `account_id` on every transaction (so the **display-name join** behind the Black Card incident — 31 duplicates, $8.4K gross, **net +$267** — stops existing), exact decimal amounts, `YYYY-MM-DD` dates, first-class `connection`/`provider`/`institution_name` (vs 17 inferred `bank_connections` rows for ~11 real institutions), `updated_since` incremental sync in place of an hourly two-tab full re-read, the true `last_successful_update`, and **holdings** (the CR019/CR022 securities tables are 0 rows — deferred to its own CR). **P0 ran 2026-07-28:** the ids **differ** (0/31 accounts, 0/1,387 transactions overlap), so the two-database crosswalk (fin migration 043) is confirmed — but it is **exact, not heuristic**, because our Sheet transaction ids are `{api_account_id}--{hash}` and so already carry the API's account ids (15/15 verified). Transactions match on content 1,324/1,387, with all 63 misses on Chase/Akoya where the API's `date` is the posted date and `auth_date` is what we store. P0 also found the documented `pending=true|false` filter 422s, `type` is the raw bank product name (so the keyword inference stays), and **two mapped Revolut wallets no longer exist upstream**. Second, independent risk: the API can serve **more history than the Sheet tab held** while 5 of 31 mappings still have `promote_from_date = NULL`. MCP connector: ops/triage yes, ingestion no (OAuth+PKCE, 1-hour tokens, write-capable `mcp:use` scope, same rate buckets). Needs a read-only PAT to start. **§18 / migration 050 (v3.11.2; **applied dev + prod 2026-08-01** — it rode the accidental 044 deploy, so nothing remains for the cutover):** the API serves four Revolut EUR transactions **twice**, under the current EUR wallet and under a legacy numeric-id wallet fintable labels "(USD)" — different transaction ids, so two genuine upstream records, which **corrects** bank-feed `6d108ec`'s claim that the API path retires this class. Mapping 531 becomes ignore-only: the date floor blocks the four known duplicates but not a new row filed there later, which would book EUR into a USD account. **§22 (2026-08-11) — the first duplicate in this CR to REACH THE LEDGER:** the carry-over's floor was a rolling `syncDate − 5 days` while the fetch floor is a fixed `FINTABLE_API_MIN_DATE`, so they agreed on exactly one day — the cutover — and thereafter rows at the fetch floor were fetched but never *considered*. **28 rows / +2,888.80 phantom income** (+2,432.38 Interest Income), net-of-transfers invisible to a balance check. Fixed at source (bank-feed `4840679`: the floor is now `minDate`, so protection covers fetch by construction; `boundaryFloor` had **no test** — 7 now, 3 falsified against the old code, 190 green) and verified by **forcing the weekly sweep**, which is the 2026-08-17 failure that had not happened yet: **108 rows reclaimed, 0 inserted**, where the 300-row insert guard could never have fired. **The class stays open:** five dedup layers and none could see a fed row duplicating another — v3.7.2's event-hash guard went **silently dormant** at cutover (API ids carry no `--`, which its comment calls *"a non-fintable source"*), and §21's *"human-in-the-loop promote gate"* only stops the **stage-only cron**, not a human pressing Import now. P0 is the **promote-time duplicate guard**. |
| [CR060](cr-060-feed-connection-health.md) | IN-PROGRESS | v3 | Feed connection health (read-only) | Fintable reports `healthy` / `needs_reconnect` / `status_text` per bank connection; the Sheet path cannot express any of it. Two incidents in two months prove the need — **Bank Pekao unhealthy since 2026-07-24** with nothing able to say so, and a 2026-06-06 GoCardless re-consent that silently cut Revolut from three wallets to one, **unnoticed for seven weeks**. **Bank-feed side built 2026-07-29** (`cc6a9bb`, 18 tests, not deployed): additive `upstream` block on `/v1/health/feeds` + the admin routing page, independent of `FINTABLE_SOURCE` and never able to fail the endpoint. Its first live run found a **third** problem nobody knew about — Capital One stale past 26h — plus two Wise connections carrying a provider-outage notice while healthy, which is why notices are reported separately from alarms. fin's recon page still to do. Split out of CR059 §15A by its pass-2 sign-off; the write half (reconnect links) is deliberately excluded. |
| [CR061](cr-061-holdings-and-prices.md) | PLANNED *(statement reader built)* | v3 | Investment holdings and market prices | `securities` and `quicken_price_staging` are **0 rows**, which is why CR056 derives investment returns from ledger postings, CR058 reconstructs brokerage history from Quicken, and CR020 is still a skeleton. Fintable serves daily holdings snapshots (quantity/price/value/cost basis) for the six Fidelity accounts plus public split-adjusted equity prices — fin has no price source at all today. Constraints to design around: `cost_basis` is the position total not per share, no history pagination (one call per day per account), and the IEX feed is one exchange cacheable up to an hour. Split out of CR059 §15B+C. **Partial 2026-07-31:** `parse-fidelity-statement.js` now reads holdings totals (market value / total cost basis / **unrealized G/L**) per account from the custodian's statement PDFs — all **117 account-statements**, 2016–2026 — giving fin its first real unrealized series. Not the fintable ingest this CR is about, and `securities` is still 0 rows, but it settles what the ingest must measure: `Change in Investment Value` **cannot** serve as return (it absorbs transfers — +2.68M reported on an account that moved +107K), while market value minus cost basis can, agreeing to single digits with the other method on a low-transfer account and diverging 31× on a transfer-heavy one. See [CR058 §12.8–12.9](cr-058-quicken-valuation-anchors.md). |
| [CR062](cr-062-forecast-loan-module.md) | COMPLETED | v3 | Forecast Loan Module + Equity report | The forecast cannot model a loan at all, while the owner is actively modelling a Sarasota house purchase whose mortgage is inexpressible — and **liability interest is sign-inverted** (`isLiability ? val : -val` on a negative balance), so the first mortgage entered with a rate would **credit** the bank line every year. Dormant only because all 15 liability-account modules carry `expense_amount = 0`. Stores five assumptions (principal, year taken, rate, end year, per-year amortization %) and **derives** the schedule every generate; keys the engine on `loan_interest_rate`, never the user-editable `module_type`; interest on the *average* outstanding supplies the July-1 half-year convention for free. **P0** (sign fix) + **P1** (the Loan module) shipped as **v3.8.0 on prod**, migration 047; **P2** shipped as **v3.9.0**, migration 048 — `secured_asset_module_id` and `/forecast-equity`: asset value gross vs net of debt, plus income after debt service. Two pre-existing defects it found in passing (`createModule` dropping nine columns; migration 041's checksum drift) were fixed in **v3.9.1**. 694 backend / 238 frontend tests, six guards green. **§11.2 (v3.11.3)** — the trap §8.2 named sprang anyway, with its premise backwards: a secured link set inside a CR050 **variant** carries a *variant* module id, not a base one, so sync's base→variant map missed it and `|| null` unsecured the loan **in the same request that saved it**. Resolution now keys on which scenario the target sits in; four tests where there had been none. |
| [CR063](cr-063-coa-ordering.md) | COMPLETED | v3 | Chart of Accounts ordering (and the dropdowns that read it) | `accounts.display_order` has existed since the seed and **`getTree` ignores it** — it sorts `ORDER BY path` where `path` is `ARRAY[id]`, so the COA page, Balance Sheet, Cash Flow, the budget worksheet and every account dropdown render in **insertion order**, which nobody chose. It cannot simply be honoured: the column is a *global* 0–207 sequence rather than a rank within the parent, and `create()` hard-codes **0**, so 22 accounts share it and **68 of 229 rows would move** if the sort flipped as-is. **P0** removes *Analyze PS Data* — PS is no longer a data source — along with its banner, quick-add path and the three dead `quickadd` modal modes (the backend endpoint stays; Upload PS still calls it), and gives the tree the reports' four expand/collapse controls. **P1** makes `display_order` a per-parent rank via migration **049**, backfilled from today's visible order so day one is byte-identical, flips `getTree` to `ARRAY[display_order, id]`, makes `create()` **append** instead of prepend, and adds `POST /coa/reorder` taking a whole sibling list (idempotent, one transaction, rejects a stale client). **P2** adds ↑/↓ buttons per row, disabled at the ends and while filtered. **P3** deletes the three alphabetical `.sort()`s in `useCoa` — including the one added 2026-07-31, owner-released — and teaches the two flat `ORDER BY a.name` list endpoints the tree rank. **All four phases shipped as v3.10.0 (2026-07-31), migration 049 on dev and prod.** The parity claim holds: `reports/balance`, `reports/cash-flow`, `budget/cash-flow` and `budget/category-groups` are **byte-identical** across the migration, and both COA trees keep an identical name sequence. Half of P3 had to move into P1 — `findAll`/`getBalances` were sorting on the *global* `display_order`, so a per-parent rank **breaks** them rather than leaving them alone; a shared `SORT_PATH_CTE` now gives all four flat-list readers the tree's depth-first rank. 704 backend / 27 frontend suites / six guards green, with the ordering claims **falsified against the pre-CR063 code** first. |
| [CR064](cr-064-forecast-annual-close-and-assumptions.md) | IN-PROGRESS *(P0/P1/P3 v3.11.3 · P6–P9/P11 v3.11.8–.13 · P12 v3.11.15 · P13 v3.11.16)* | v3 | Forecast: the annual close, the assumptions key, and the module form | Started as "we customised the module form for Loan — should the other types get the same treatment?" The answer is **no** (§5: `module_type` is free text the owner edits, and `fcModulePayload` sends every field on every save, so a type-gated form hides live values rather than clearing them — which is why the Loan carve-out needed a preview endpoint and a confirmed delete). Looking for the answer turned up three defects. **P0 (built):** the module editor read the FX assumptions under `Rates.USDPLN`/`USDEUR`, which do not exist in the live document (`{PLN, EUR}` — the engine and FCExpSetup both read `PLN ?? USDPLN`), so it fell back to a rate of **1** and then **multiplied** by it where the engine divides: an unmatched €390K property posts **$390K** and reports an implied FX rate of 1.0, and the sync effect overwrites a correct hand-typed figure. Dormant only because all ten live non-USD modules are matched. **P1 (built, migration 052):** `forecast_assumptions` is keyed by scenario NAME and `PUT /scenarios/:id` renamed the row and nothing else — prod carries five dead names, and two saves after a rename a scenario runs at **0% inflation for 36 years, silently**. `renameScenario` makes it atomic, `updateScenario` now throws on a name, and 052 prunes the orphans — refusing when the orphans look like a rename rather than a deletion. **P3 (built):** sections collapse when empty (Tax is 0-for-103 modules; Real Estate never uses Income, Business never Expenses) with per-type **labels** but never per-type fields; and the two blank prod modules turn out not to be Cancel's doing at all — CR042 fixed that in `11fc3b5` and both rows postdate it — but **Generate**'s, which saves the draft first, so `assertModuleBody` now refuses a module with neither account nor name. **P2 (designed, blocked on an owner decision):** the annual close — `refreshModulesFromActuals` already exists and is wired to copy-scenario, but `PeriodStart` never moves with the anchor, so every scenario is anchored a year behind its own base year. **P6 (built, migration 055):** the follow-up question — *why can't a business's income grow at anything but inflation?* — found the typed amount was **not used at all**: income has two mutually exclusive modes and the yield wins on a **single** row, so all six income modules carry a dead Income Amount (United Beverages: 192,266 PLN typed, 77,163 USD booked). Adds a growth multiplier and permanent step changes, states the live mode in the form, and hides Yield Spread on a business **by data rather than by type**; dormant — **7,916 entries byte-identical** on a copy of prod. **P7 (built, v3.11.9):** the FC-line budget hint printed a **USD** budget beside a **local-currency** input and reconciled when they matched as digits — UB's dividend budget is 690,000 PLN = 192,266 USD and the module holds 192,266, with the hint reading "Remaining: −0"; P6 had just made that mis-scaled amount reachable. Now summed in USD and shown in the module's currency with the rate. **P8 (built, v3.11.10):** the base-year column summed each module's amount in its **own currency** into a figure read as USD — UB showed 500,000 (PLN) against a real 128,205 — and that same figure is the **cash sweep's opening cash**, so `2026 Base`'s base year went **+144,395 → −254,728** and the sweep sells 1.26M more across the horizon. The column also never read `budget_entries` despite its `(BUDGET)` header. **P4/P5 designed, not built.** Also unblocked CI: migration **050**'s guard aborted the chain on an empty database. |
| [CR065](cr-065-neutralize-pair-identity.md) | COMPLETED | v3 | A neutralize counter-leg is claimable exactly once | `neutralize()` asked "does this row already have a counter-leg?" with a **value match** — account, negated amount, ±3 days, compatible category. Value-matching is not identity: two rows of the same value are indistinguishable, so one counter-leg could be claimed by any number of originals, and nothing recorded that a claim had happened. On 2026-07-30 two genuine $150,000 CD purchases claimed the same mirror and Fidelity Cash Mgt ran **$150,000 light**, hidden inside a −107,830.71 "MTM gap" that was **$108,635 bookkeeping and $804.50 market**. Migration 053 adds `paired_with_id` + a partial UNIQUE index so the database refuses a double-claim; the candidate query gains `paired_with_id IS NULL` and `source <> 'auto-offset'`; the decision moves inside the write transaction with compare-and-swap; re-neutralizing is a no-op; the Ledger applies sequentially and reports what actually happened; and Balance Calibration gains a per-account **unpaired-leg** check — the thing that would have caught it the same day. |
| [CR066](cr-066-fc-line-mapping-completeness.md) | OPEN | v3 | Every category with activity reaches an FC line | Twelve COA categories carrying **−78,689 of 2025 expense and +31,474 of income** map to no FC line, so they are absent from the base-year P&L, from every FC-line row on the Review page and from the modelled years — and **no screen says so**. Surfaced by v3.11.14: putting the actuals column on the stacked breakdown made the 2025 stack (487,897) disagree with the `Expense` header above it (566,586), because the header reads the ledger's COA total while every child row is an FC line resolved through `categoryToLineMap`. Neither figure is wrong; the join is incomplete. The largest rows are `Property One-Off` (−47,187) and a five-row `Patrick - *` cluster (−36,500 + −2,502 travel) that looks deliberately held apart, plus `Tax Adjustment` at **+8,078** — a credit inside Expense that must be understood before it is mapped onto `Taxes`. On the income side `Rental - Spain` (+31,306) is probably **already modelled** through the SP property modules' `bs_module_income` path, so mapping it would double-count — which is the real defect: "unmapped" means both *modelled elsewhere* and *not modelled at all*, and nothing distinguishes them. **P0** decides each row (or records it as deliberately excluded); **P1** makes the app report unmapped categories with activity, since a new COA category is unmapped by default and this recurs by construction; **P2** reconciles the Review header with its own children once P0 says whether a permanent gap exists. Identical on dev and prod. |
| [CR067](cr-067-forecast-multi-compare.md) | COMPLETED *(v3.12.0; P3 deferred)* | v3 | Forecast Multi-Compare | One base scenario and any of its variants on a single trajectory chart — base bold, each variant its own hue, the same five metrics Compare toggles. [CR040](cr-040-forecast-scenario-compare.md)'s Compare page is two-scenario by construction (its own non-goal #1), so "how do all my variants sit against the base?" means opening it once per variant. Cheap because `buildScenarioMatrix` is already pure and single-scenario and yields all five metrics; `compareMatrices` is the pairwise layer this page simply does not call. Chart only — no tables, KPI cards, commentary or AI panel. **The trap:** the union-of-years alignment lives in `compareMatrices` and each matrix trims to `>= its own PeriodStart`, so plotting positionally would silently misalign scenarios with different `PeriodStart` — key by year, not index ([CR064 P2](cr-064-forecast-annual-close-and-assumptions.md) is about to make that live). Extracts a shared `FCTrajectoryChart` rendered by both pages, so a parity gate on `/forecast-compare` is part of done. Staleness was to be a **badge** rather than a "Generate all" button, but PM sign-off deferred it (P3): the drift motivating it is a *dev* artifact — on prod all five scenarios build within two hours of each other — so its one load-bearing case (a variant with **no** generated forecast, otherwise just an absent line) shipped inside P2 with no endpoint. Frontend only; no migration, no endpoint. **Reached prod early** inside another thread's v3.11.16 deploy while P2 was uncommitted — harm nil and verified live, but prod matched no tag until v3.12.0, now [Known Issue #17](../current/project-roadmap.md#3-known-issues). |
| [CR068](cr-068-mobile-actuals-search.md) | COMPLETED | v3 | Mobile Actuals Search | `/m/transactions` — the actuals search a phone does not have today: `/trans-actual` is absent from `DESKTOP_TO_MOBILE`, so a phone opening it is silently dropped on `/m` home, and `/m/ledger` is a single-account ledger with no period, category or text filter. Three filter chips (period · accounts · categories) each opening a full-screen sheet, plus a debounced **server-side** description search — desktop's search box filters client-side over loaded rows only, which on a phone page-size would report "no results" for rows that exist. No backend work in P1: `GET /api/v2/transactions` already takes `fromDate`/`toDate`, repeated `account=`/`category=`, `description` and `minAmount`/`maxAmount`, and the page holds an `ACTUAL_CONFIG`-shaped filter object so `useTransactions` is reused verbatim. Generalizes `MobileCategoryPicker` to a multi-select `MobilePickerSheet`. **P2 is a shipped-desktop fix the mobile tiles must not copy:** `getTotalsAmount` reads the *local* `Amount`, so "EXPENSES (BASE)" adds PLN to EUR as one unit (453.64 + 116.23 = 569.87 against a true base of 254.27) — the [CR064 P8](cr-064-forecast-annual-close-and-assumptions.md) mixed-currency class, plus `getActualEntries` silently ignoring `description`/`valueFrom`/`valueTo`/`currency` and truncating at `LIMIT`. **P1 + P2 shipped in v3.13.0 (2026-08-04); P3 (row actions) deferred by decision.** On real July data the base tile read (68,064.39) against a true (46,321.61) — a 21,742.78 overstatement in one month. Two further defects found in `getActualEntries` and fixed: `Description1` read a column that does not exist (so the Budget-vs-Actual popup showed an em-dash for every row) and the LIMIT truncated silently. `MobileSheet` extracted as the shell's one bespoke dialog, keeping the modal ratchet a rename at 14. Read-only; row actions are P3. No migration. |
| [CR069](cr-069-forecast-streams.md) | COMPLETED *(P0 v3.13.1 · P1+P2 v3.14.0 · P3 v3.14.1; migrations 057–060)* | v3 | Forecast streams — one Modules section | Collapses the Expenditures/Modules split: a module becomes *identity + optional valuation + N first-class **streams*** (direction, FC line, mode, change schedule, window, tax), so every `forecast_income_expense` item is just a module with `has_valuation = FALSE` and one stream, and the engine keeps **one** stream evaluator instead of three divergent ones (incexp, module income, module expense — CR064 P6's steps table is a re-implementation of the Fixed $ change migration 001 already had). **P0 ships alone and first:** inc/exp entries label by *account*, so three prod items (`Retirement Home`, `Car Purchase Chris`, `Social Security`) are invisible under their own names, and the "last-write-wins ON CONFLICT" comment is false — `entry_type` is NULL and NULLs never conflict, the rows are silently additive. Streams-as-rows answers CR064 §5's objection structurally (a module with no expense has no expense stream — nothing hidden to go stale); `has_valuation` retires the CR041 gate's "MV 0 forever = never owned" trap that forced the split in the first place. Retires step 4 (6 steps → 5), ~2,770 lines of `FCExp*`, four tables, the `incexp` variant entity type, and the incexp halves of auto-adjust/base-year/AI-review. **Gate: per-(account, year) `forecast_entries` sums identical to the cent, all five scenarios, prod copy** — labels move, no number a report shows may. Four phases, each shippable; **three migrations — P1 schema (inert), P2 backfill + rewrite of the 3 incexp overrides and 48 origin links (rides the cutover deploy atomically; nothing dual-writes, so an early backfill goes stale on the first edit), P3 drops the four tables**. At P2 the old `/incomeexpense` write routes 410 and the Expenditures step leaves the nav, so nothing can write into the retired model between deploys. **Shipped in four phases, every one gated on sums identical to the cent** (4,030 rows / 0 differing on a prod copy, 3,798 / 0 on dev, and 4,030 / 0 again with the tables dropped). **Eleven defects were caught before production by four mechanisms** — the gate itself (a recursion seeded at the wrong index made three FC lines vanish entirely; four variant overrides silently reverted to base), dev's own data (an `income_pct` override moved 504 rows, exposing `syncEntity` silently dropping unknown patch keys and `scheduleEqualsBase` ignoring nested rows), a security review (no variant interception on stream writes; no API path could create a flow module; AI Review writing a dead column while reporting success) and a code review (two that reproduce today: auto-adjust's row-constructor `IN` could never match a line-less expense, and `FCReview` called the now-410 endpoint from the *live* bundle with the error swallowed). Two of the author's own comments were false and are corrected in place. **P3 also fixed the e2e suite's root cause** — it had been testing a July-14 orphaned server for three weeks because `$!` held a subshell pid. |
| [CR070](cr-070-module-inputs-by-type.md) | COMPLETED — **v3.15.0 (2026-08-05)** *(two-pass reviewed; P0–P6, no migration)* | v3 | Module inputs that fit the type | Every type asks the same questions: an Expense module — 50 of prod's 170 — presents Cost Basis, Market Value, Growth, a Capital Gains Tax Override, a Base Date, Invest, Dispose and a Sweep Priority, none of which the engine reads on it. Delivered NOT as nine bespoke forms but as three rules: gate on the data the engine already branches on (`has_valuation`, `market_value` sign, `loan_interest_rate` ∪ type), let type only ADD (creation defaults, labels, warnings), and detect residue in `fcWarnings` so a hidden field can never hold a live value. Also: double-click opens the edit form and the stale read-only drawer is deleted. **QA then found five more CR069 leftovers**: two retired schedules still rendered and silently eating input, a flow module's `Account` naming one of the four accounts its line covers (replaced by `GET /fc-lines/actual-totals`), and the schedule sections still speaking a second visual idiom. No migration. |
| [CR071](cr-071-forecast-numbers-vs-intent.md) | COMPLETED — **v3.15.0 (2026-08-05)**; the two data edits APPLIED to prod 2026-08-06 (§7); **R5 corrected in v3.18.1** (§8) *(no migration)* | v3 | Where the forecast disagrees with intent | Seven places the engine models faithfully a number the owner did not intend, with nothing to say so: `House Morgage` is typed Loan with a NULL rate so 500,000 of debt books no interest — and the one warning that would catch it bails on the same NULL; United Beverages' dividend is taxed twice (~$38K/yr); CVC Fund VIII double-counts ≈158K; 5 of 8 properties carry a basis equal to market value (⚠️ the rule written for this shipped WRONG — see §8); `Sarasota House` moves −45,000/yr with no P&L line. Detections in `fcWarnings.js`, never corrections — the numbers must not move. Split out of CR070 at PM sign-off, which ranked it ABOVE that CR for owner value. No migration. **§7 (2026-08-06):** the two data edits applied to prod through the app's own write path — CVC's phantom yield removed and both Fidelity typed amounts cleared; Base's 2062 net worth −31.3% with CVC's NAV and 2033 exit unmoved. **§8 (2026-08-07, owner-found, v3.18.1): R5 SHIPPED WRONG and said so authoritatively.** It told the owner Barkeria was *"sold without realizing any gain"* while the module's own output showed **334,294 realized in 2040**. Its comment read the basis/market equality at the BASE DATE; the engine reads it at the DISPOSAL YEAR, and the basis is flat while the market value compounds, so the Full branch books `market(disposal) − basis` = 1,339,163 − 1,004,870 = **334,293**. Measured on prod: **wrong on 30 of the 35 modules it fired on** (25 grow ⇒ a gain IS realized; 5 shrink ⇒ a LOSS is realized; only 5 flat ones were ever right) — and wrong in the direction that matters, calling a taxable sale tax-free, which is this CR's own failure class. Now branches on growth. **The lesson: a rule that asserts what the engine does must be derived from the engine's formula, not from a restatement of it** — 8 rules, a full suite and a browser QA all passed, because each checked the warning FIRED and none checked that what it SAID was true. |
| [CR072](cr-072-valuation-module-inputs.md) | COMPLETED — **v3.17.0 (2026-08-06)**; **P7 built + QA'd, awaiting release** *(two-pass reviewed; no migration)* | v3 | The balance-sheet module form, and the budget year the engine skipped | Owner-requested restructure: Valuation splits into **Reference value** / **Assigned value** / **Forecast assumptions**, and an UNMATCHED module collapses the two value blocks — `is_matched` splits all 110 valuation modules perfectly (20 unmatched, all zero-valued with an unclosed base date; 90 matched, all valued). **Collapse, not hide:** `is_matched` is not an engine branch, so hiding would have made the residue panel lie and its Clear button zero a live figure. Also caught pre-ship: `is_matched` switches the USD conversion rate and writes back on open, so hiding those fields would have persisted a different number invisibly. **P5 moves numbers with sign-off** — growth was gated on `year >= PeriodStart`, so an asset grew once across the two-year gap to the forecast; +1,284,241 net worth at 2062. Plus drill-downs into the accounts behind a line's actual and budget, and the fix for a v3.16.0 feature that had never rendered. **P7 (after the release, on dev):** the drill-down is a real modal reusing the Actuals page's own machinery — `useTransactions`, `ACTUAL_CONFIG`/`BUDGET_CONFIG`, `useTransactionSelection`, `TransactionTable` — pre-filtered to the line's leaves from the same CTE the total uses, with the shared `HierarchyFilter` for categories (type-to-narrow, right-click solo, All pill) opening on the line's own group. §13 blamed the render tree; the real cause was that `TransactionTable` takes `{entry, rowId, isSelected}` wrappers, so every cell threw and unmounted BOTH dialogs — and the isolated test passed throughout because its mock returned zero rows. Three defects caught before shipping: `monthEnabled:false` fell through to the CURRENT month (a full-year figure would have shown one month), the budget row would have queried actual transactions, and the per-currency totals silently capped at 500 loaded rows. |
| [CR073](cr-073-two-recurrence-guards.md) | COMPLETED — **v3.17.1 (2026-08-06)** *(no migration)* | v3 | Two recurrence guards: the orphan amount, and the two projections | Owner-instructed, closing the two items both CR072 reviewers asked to be numbered. **(1) Known Issue #2's missing guard.** `Sarasota House` carried an expense of 45,000 with no FC line; the engine's stream loop skips the P&L row (`if (!line) continue`) while the CASH path below takes every stream — **−1,203,432 across 21 years** out of Bank Accounts onto nothing, with Net Cash Flow and Expenses disagreeing by exactly that. CR062 closed this for loans and nothing guarded an asset. `assertStreamsHaveLines` on POST + PUT, keyed on the AMOUNT being non-zero (a 0-amount stream posts nothing in any mode, and **15 such rows exist on prod**); **0 prod rows are in the refused state**, so it closes a hazard rather than blocking an edit. The form checks the same thing first, so the reachable path is a sentence naming the card, not a raw 400. **(2) The LIST/DETAIL projections**, which drifted three times in three days (`HasValuation` v3.14.2, the sweep fields v3.15.0, `fc_line_name` v3.16.0) — each surfacing as the editor guessing at state it should have been told. Now one `moduleCommonFields` both endpoints spread, `loadModuleStreams` joins `fc_lines` (its bare `SELECT *` was the v3.16.0 half), and a parity test asserts on the RESPONSES so a hand-added key cannot re-open the gap; the places the two genuinely differ (`Type` capitalisation, scalars-vs-rows) are pinned so a later "fix" cannot flatten them. Verified against the OLD code still running on prod: **key sets identical both directions, zero type changes** — the value diffs are dev-vs-prod data, provable because each appears in both its snake_case and PascalCase form. Also found: **tests that pass on dev and fail on CI** — the new fixtures used a `line_type` migration 007's CHECK forbids, which passed only because dev and prod have no such constraint (007 was auto-baselined on both). Now Known Issue #18. |
| [CR074](cr-074-dismissible-cash-health-warnings.md) | COMPLETED — **v3.18.0 (2026-08-06)** *(migration 061)* | v3 | Dismissing a cash-health warning | Owner-requested: the panel was showing 21 issues, most already judged and accepted, and a list that long is one nobody reads. Per-row `×`, **Dismiss all**, **Show N dismissed** / **Restore all**, stored per SCENARIO (warning ids embed a module name, and the same module carries different numbers in each scenario). **The design is defined by what it must not be able to do.** CR045 §1 built this panel because a $20M unfunded shortfall sat here unremarked — a dismiss button is precisely a way to make the page quiet. So: the header keeps counting the FULL number and says how many are dismissed; **all-dismissed is not all-clear** (its own state, because "Cash stays funded" is a claim about the plan and would become a lie); and **a dismissal EXPIRES when the warning changes** — it is stored against `warningFingerprint`, a hash of the warning's severity, years, amount and detail sentence, so accepting "Sweep source fully drained 2061" cannot silence the same rule at 2041. Such a warning returns flagged and says why. A dismissal with no fingerprint is refused (400): it could never expire. Migration 061 `forecast_warning_dismissals`, UNIQUE (scenario_id, warning_id), CASCADE from the scenario. "Dismiss all" is ONE request, not N racing writes. Verified in a browser: 25→24 on one dismiss, **survives reload**, 0 visible + all-clear banner absent after Dismiss all, back to 25 on Restore all — and with the stored fingerprints forced stale in the DB, both warnings came back. |
| [CR075](cr-075-base-year-is-the-budget.md) | COMPLETED — **v3.19.0 (2026-08-08)** *(⚠️ MOVES NUMBERS, owner sign-off; no migration)* | v3 | The base year is the budget | Owner-found: *"why are CVC, Dividend and Interest Income all zero in budget year 2026?"* — all three are **yield-mode** streams, whose `amount` is 0 by construction (the card hides the box; a yield is a rate), and `getBaseYearValues` summed each stream's typed amount. So year −1 was never the budget, it was the modules' inputs: **129,000 of budgeted investment income read as zero**, and `UB Income` carried 128,205 against a budget of 192,266. Income understated by 193,071, net by 152,802 — and `index.js` folds that into the cash sweep's OPENING CASH, which the sweep pins to its band every year, so the error rode all 36 forecast years. **The third instance of the CR049 §1 mode in this one function** (after ~65K liability-gated expenses and ~400K unconverted FX), and the engine already had a branch for exactly this shape on LOAN streams — never extended to `yield`. Now reads `budget_entries` grouped by FC line, verified against the budget **15 lines, 0 mismatches, to the cent**. Deletes the machinery both earlier bugs lived in: the CR046 window (a budget entry is dated), the CR062 loan derivation, the CR064 P8 FX conversion (`base_amount` is already USD). **Owner decisions:** budget only with NO derived fallback (a derived figure beside a budgeted one double-counts and cannot be told apart), and the budget's own recorded rates. Consequence stated: `budget_entries` has no scenario, so **all five scenarios now share one base year**. **New R9 rule** pays for budget-only by reporting a module that implies base-year money the budget does not carry — keyed to catch a yield stream despite `amount = 0`; fires 0× on prod, proven able to fire. **A SECOND bug fell out of it:** R9 printed "in 2024", because the warnings were passed `years[0]` as PeriodStart while the Review unshifts TWO columns onto it. **R7 was comparing disposal dates against PeriodStart−2 and silently missed 20 disposals dated 2026-07-01 across all five scenarios** — Full disposals in the budget year the engine never executes, so those balances sit on the books for the whole plan. Same class as CR071 §8; **2 of 8 rules now found wrong this way, the rest unaudited** (⚠️ [CR076 §3](cr-076-forecast-model-review.md) later found R7's SENTENCE wrong too — the engine executes those 20 disposals; it indexes them against the module's own base year, not PeriodStart). Measured on a fresh prod copy with the engine proven idempotent: Base 2062 net worth **+397,705**, SRQ's shortfall −3,195,733 → −1,837,896 — ⚠️ **the LEVELS behind these deltas were later CORRECTED by [CR076 §2](cr-076-forecast-model-review.md)**; the shortfall pair double-counts a cumulative quantity. All 14 tests that failed were REWRITTEN to the new contract, not deleted — one of them would have passed vacuously. |
| [CR076](cr-076-forecast-model-review.md) | **IN-PROGRESS** — **§8 COMPLETE**, shipped across **v3.20.0 → v3.22.0** *(⚠️ MOVES NUMBERS; no migration)*; **§13 (2026-08-10) closes the advisory walkthrough — all 15 already deliberate, NO model change**; §7 retains only *price idle cash* and *loss carry-forward* | v3 | The forecast model review, and the figures it corrected | Owner-requested five-reviewer audit of the whole forecast model (warning rules · P&L · balance sheet · cash sweep · perturbation scenarios). **§1 records what is SOUND** and is the larger half: growth is one inflation-multiplier convention applied to both sides, basis never grows and contributions raise it, partial disposals apportion basis pro-rata (arithmetic closes exactly over a module's life), gain is proceeds − basis **at the disposal year**, the half-year convention is real, loans read interest off the realized balance and retire to exactly zero, no balance-sheet stock is summed across years, and the sweep's availability cap is forward-looking. Gates green, engine idempotent, dev byte-identical to prod. **⚠️ §2 is a P0 correction to OUR OWN published figures:** net worth at 2062 was quoted from a SQL roll-up that read `Bank Accounts` — a per-module **annual cash movement** — as a balance. The app was never wrong; the roll-up was. Correct: Base **4,398,898** (published 3,505,089) · Buy Business **9,474,620** · Downside **1,881,988** · Upside **7,733,471** · SRQ **−829,508**. Error up to 894K and **not constant**, so comparisons were contaminated too; cross-validated two independent ways to within exactly 11,277.06 (= D2). *Rule earned: a forecast figure in a document comes from the app's own exported functions or the engine, never a SQL re-derivation written for the occasion.* **§3 — three more rules wrong**, making **5 of 8**: R7 is false on all 20 rows it fires on (the engine indexes disposals against the module's own base date, and prod shows those balances cleared); W2's headline sums a quantity that is cumulative by construction ($1.2M shown vs a true 1,017,119 gap); R5's loss branch promises an offset the engine never applies. Plus the cash-sweep audit CSV is written **before** Step 7b re-runs the sweep to convergence, so the explanation screen contradicts the warning panel — on SRQ it shows every year landing at 200,000 with no shortfall. **§4 — 8 money-moving defects:** D1 the convergence loop keeps a stale copy of the growth formula and overwrites the builder (−39,715, one line); D2 the sweep's opening cash uses the abandoned `closing_balance` method (11,277 every year); D3 foreign cost basis is re-translated yearly so an FX move is taxed but never booked (−51,659); D4 base-year tax still reads the typed amount CR075 replaced; D5 a stream's final year is halved twice; D6 same-year gains and losses never net; D7 an empty inflation list silently yields 0% for 36 years; D8 a PeriodStart−1 assumption row is discarded. **§5 — two labels teach the wrong model:** the growth hint's clauses are swapped (70 of 110 streams carry a multiplier, most below 1), and a yield stream has no rate field at all, so both CVC funds pay exactly inflation forever. **§6 — inflation is the master dial:** ±1pp moves the horizon +70%/−45%, and asymmetrically, so raising it makes the plan look BETTER. **§7** owner decisions, **§8** fix order, **§9** what was not re-verified (D3/D4/D5). |
| [CR077](cr-077-assumption-advisor-tab.md) | **IN-PROGRESS** — increment 1 **SHIPPED v3.23.0 (2026-08-09)**; **§7 (v3.26.0)** guards two rules that fired on streams not in the plan (**17 → 15**); the LLM stage (§4) is the remainder | v3 | Splitting the Cash Health panel: integrity vs. assumption advice | Owner-requested 2026-08-09. The panel mixes two kinds of statement under one heading and one severity scale — *"`Business Loan` is configured but excluded"* (an integrity DEFECT) sits beside *"`Tax Liabilities` is sold in the base year"* (an EXPLANATION) and *"`CVC Fund IX` both pays a yield and returns capital"* (a QUESTION only the owner can answer). They demand different things: a defect is fixed, a judgement is recorded — so the owner re-triages the same calls on every render, which is what CR074's dismissals exist to relieve. **Tab (a) Integrity**, **tab (b) Assumptions to consider**, counted separately. The new tab is the missing home for [CR076 §11](cr-076-forecast-model-review.md)'s findings: `Social Security` at 0.25 × inflation against a statutory full-CPI COLA, `Purchases` at 0.5 shedding ~36% of purchasing power, `OCME` at −20. None is a defect — the engine does exactly what it was told — and none belongs under *Cash Health*. **Cheap because CR074's machinery is reused wholesale** (`warningFingerprint`, `partitionDismissed`, `forecast_warning_dismissals`, three routes, already per-scenario), and its three load-bearing properties carry over unchanged — dismissed is never invisible, all-dismissed is not all-clear, and **a dismissal expires when the figures change**, which matters MORE here: an assumption accepted at 0.25 must return if the multiplier moves. **Six deterministic advisories are specified for day one**, each already computable and each citing a falsifiable number. **On the local LLM:** allowed as a SECOND stage under one constraint — *nothing generated may state a number or assert what the engine does*. CR076 found seven instances of a claim about the engine derived from a restatement rather than the formula, five of them in rules a human wrote and reviewed; a generator writing sentences about the model is that failure mode with the brakes off, and CR045 §1 exists because a $20M shortfall sat unremarked on this exact panel. So rules compute and state the facts; the model may only prioritise, group or phrase them, with figures rendered by our own formatter. **Prerequisite: D2–D6 first** — three of them change what the numbers ARE, and advice built over figures under repair would teach the owner to trust a value about to move. |
| [CR078](cr-078-disposal-selling-costs.md) | COMPLETED — **v3.24.0 (2026-08-09)**, migration 062; **LIVE** since 2026-08-09 (rates typed, no longer dormant) | v3 | Selling costs on a disposal | From [CR076 §7 Q2](cr-076-forecast-model-review.md), raised independently by two reviewers. Every disposal books **GROSS** proceeds — no agent fee, transfer tax, legal or broker cost, no Spanish plusvalía — so the whole sale price lands in cash and the whole gain is taxed. On `2026 Base`: **property sales total 5,566,755** (3–6% all-in = 167K–334K) and **business sales 5,445,368** (1–3% = 54K–163K). **The base year is the sharpest case**: its 1,239,753 folds straight into the sweep's OPENING CASH (CR075 §2), which the sweep pins to its band every year, so the error rides all 36 years rather than washing out — CR049 §1 again. **Per-ROW, not per-module or per-scenario**, and the data is the argument: a property sale carries 3–6%, a business sale 1–3% negotiated, and `CVC Fund VIII`/`IX`'s 2,033,048 of **capital returns carry nothing at all** — they are distributions modelled as disposals. Any coarser rate would silently charge a selling cost on those. **The accounting decides the code:** a selling cost reduces the AMOUNT REALIZED, so it comes off cash **and** off the gain — missing the second half would tax money never received. A percentage is currency-neutral, so it applies in local currency before the USD conversion and needs no new FX handling. Additive nullable column ⇒ byte-identical until a value is typed (the CR050/CR062 dormancy pattern); engine change lands in the partial branch, the Full branch **and index.js's convergence mirror**, because CR076 D1 is the standing lesson that the mirror writes last. A missing cost on a property disposal is an **advisory** (CR077 tab b), never an integrity error — and must not fire on a capital return or it becomes the always-on noise CR077 §7 deleted. **§6 holds four questions to answer before code**, chiefly whether the cost is netted silently or shown as its own row. |
| [CR079](cr-079-real-terms-view.md) | COMPLETED — **v3.25.0 (2026-08-09)** increments 1 + 2 (Review), **v3.26.0 (2026-08-10)** increment 3 (**Compare**, §7); only the Home hero is still nominal-only | v3 | The plan in today's money | From [CR076 §7 Q3](cr-076-forecast-model-review.md): every forecast figure is **nominal** and no surface says so. At 2.5%, 2026 → 2062 is a **2.43× factor**, so Base's headline **4,674,650 is about 1,921,719** of today's purchasing power. It is also the question the owner has actually been answering all week (`Social Security` at full CPI vs 0.25, `Purchases` at 0.5, `Retirement Home`'s 200,000 dated 2052 are all real-terms judgements made against a nominal-only display). **Increment 1** is `utils/fcRealTerms.js`, pure and 16-tested: it reads the **same step function** `fcbuilder-setup.buildRates` walks, because a deflator built from a different reading of the same rows would silently disagree with the numbers it deflates; the base year is exactly 1; years BEFORE it are **inflated** rather than left alone, so the 2025 actual is comparable with everything to its right; a scenario with no inflation returns **null** so the view is disabled rather than claiming nominal and real are the same (the display-side twin of CR076 D7); and `toRealTerms` passes null/blank straight through, because a missing cell is not a zero. **A test caught a real defect:** `Number(null)` is 0 and passes `Number.isFinite`, so a missing base year anchored the series on **year zero** and produced deflators around 10³⁰⁰. **Increment 2 is deliberately NOT half-done.** A partially deflated page — some rows in today's money, some in 2062 money, under one heading — is worse than a nominal one and *invisible*, because both look like money. There is no single choke point: money reaches FCReview by **four** independent paths (`getCellValue`, the `baseYearBudget` prop, the accumulating `balanceDisplayValues` bank series, and the derived totals/KPIs), and the accumulating series must be deflated **per year after accumulation**, never during. It also wants a browser check rather than unit tests alone. §4 holds four design questions, chiefly where the toggle lives and how the state is labelled so a screenshot cannot lie about its basis. **Increment 3 (§7, v3.26.0) covers Compare** and needed ONE conversion where the Review needed four: `deflateMatrix` runs on matA and matB **before** `compareMatrices`, so the table, charts, commentary and KPI cards read one converted set and there is no path to a half-deflated page. Each scenario deflates by **its own** inflation — the point, since a scenario assuming higher inflation carries a bigger nominal 2062 figure for reasons unrelated to the plan — while the **anchor is shared**, because anchoring each on its own would make every delta the difference of two currencies. Both-or-neither if a deflator cannot be built. The hand-listed field set is guarded by a test asserting it against the keys a real matrix carries (the `disposal_cost_pct` lesson, one release old), verified by watching it fail on a probe field. The browser check failed first run on `Base Case` — a scenario with **no `PeriodStart`**, hence no honest deflator and a correctly disabled toggle — and now asserts both states. |
| [CR080](cr-080-feed-accrual-reconcile-mode.md) | COMPLETED — **v3.28.0 (2026-08-11)**, migrations 065 + 066 + 067 | v3 | The `accrue` reconcile mode (yield a feed never posts) | `Wise - USD` (8) and `WISE - EUR` (13) are **Wise Assets** balances — money-market fund, not cash. The feed delivers the monthly `ACCRUAL_CHECKOUT … Assets service fee` (−0.53 USD / −0.22 EUR) but **never the yield those fees are charged against**, so fin drifts below the feed a little every day with no transaction behind it (~3.6%/yr USD, ~2.1%/yr EUR, measured across three independent intervals each). **Neither existing mode is the right treatment:** `calibrate` folds a *recurring flow* into a constant at opening — the failure **046** documents, today right and every prior date wrong, drift back tomorrow — and `mtm` books the right *shape* to the wrong *place*, `Unrealized G/L` (88), an **expense** category, so yield never reaches income, budget or anything tax-facing. Both accounts were set to `mtm` and had **never had one `mtm` row written**. **Part A** (shipped) books the history: 6 `accrual` rows to `Interest Income`, measured as **Δ(feed − fin) between endpoints the feed has settled** — only dates inside a transaction-free run, because this feed syncs in the small hours and its lag **jitters by a day** (CR065 §11). Both gaps *start negative* (fin ABOVE the feed before any feed history), which unbooked yield cannot cause, so that separate unattributable error is booked as its own dated restatement rather than hidden inside the plug — the USD account's +2.79 today is 26.62 of yield **net of** a 23.83 error, and one row would have understated 2026 interest by ~24. Also caught an FX conversion miscategorised as `Interest Income`, inflating the category by **4.75× the entire real 2026 yield**. **Part B** parameterizes `mtm()`'s three hardcoded values (category 88, month-end snap, source tag) into an `accrue` mode — *not* a new engine. The design work is the **guard**: `mtm`'s 15%-of-balance plausibility test is useless on a cash account (a missed $500 transfer on Wise USD is 12% and sails through, laundered into income and never revisited, where an MTM row at least re-marks and self-corrects), so it is replaced by **implied annualised yield**, and the verification is falsification — inject a missing transaction and confirm the guard *refuses*. **Building it corrected the design twice.** (1) The draft seeded the accounts to `accrue` in the schema migration, "inert until the engine ships" — it was not inert: the dispatch sent **any** unrecognised mode to `calibrate`, which rewrites `opening_balance`, and migrations land *before* code, so a Reconcile click in that window would have destroyed Part A. Split into **067** (column, before deploy) + **068** (flip, after), and an unrecognised mode now **throws**. (2) The draft's booking rule — the observation's own `balance_date`, gated on being synced after that day ended — selected **zero** observations: measured across the feed's whole history `synced_on − balance_date` is 0/−1/−2 and **never positive**, so the mode could never have run. Now `bookDate = LEAST(balance_date, synced_on − 1)`, a conservative pairing rather than a claim about a lag that is still unproven (Known Issue #14). Both fixes are **falsified against the unfixed code**, and against prod both accounts currently *refuse* — correctly, one per guard. **068 was written and then withdrawn**: its header spent twenty lines justifying an inversion of the migrations-before-code rule, which was the tell that a migration was the wrong vehicle — `reconcile_mode` and `accrual_category_id` are per-install configuration, like `feed_sign` and `feed_negate_tx`, and are set on the reconcile page instead. **067 is the only migration this CR ships.** |
| [CR081](cr-081-ai-line-assistant.md) | **DEFERRED** (owner, 2026-08-14) after a two-pass review — pass 1 *revise*, pass 2 *defer*; **P0a carved out and SHIPPED v3.28.3** (§12); successor is an editor-side consequence preview with NO LLM (§13). Six of the CR's own claims corrected in §11. Originally scoped 2026-08-11; supersedes [CR077](cr-077-assumption-advisor-tab.md) §4's LLM stage | v3 | Ask about this line: AI-proposed edits, previewed before they land | Owner-requested: a question box scoped to a forecast line, the local LLM returns a few candidate answers, and selecting one applies it. **Three-quarters already exists** — `aiReview.applyAction` has a typed action allowlist and already names stream edits as deferred "P3 scope", CR053's auto-adjust already deep-copies to a standalone scratch, runs REAL engine builds, reads the engine's own entries "never the client warnings util" and verifies with a rebuild, and CR079 makes a 2052 edit legible in today's money. **⚠️ Two defects on the SHIPPED apply path must be closed first:** it is a CONFIRMATION not a preview (shows the input, never the consequence), and `current_value` is rendered from the MODEL — `applyAction` never reads it server-side — so the owner can approve against a false premise and one click writes (roadmap Known Issue #22; CR077 §4's rule already violated in production). **The invariant:** no AI-originated change reaches a real scenario except through a preview the owner has seen, and the preview shows the CONSEQUENCE — enforced by a preview TOKEN the apply endpoint requires, hashed over (bundle + scenario + entries fingerprint) so it EXPIRES when the plan moves, which is CR074's dismissal-expiry idiom reused. A preview must also name its **blast radius** — a base edit moves four variants, and silence there is the `disposal_cost_pct` class. **The rule extends CR077 §4 from sentences to actions: the model may choose among actions and phrase them, never originate a number** — the four legitimate sources are a figure already in the plan, arithmetic our code performs, the owner's own answer, or a benchmark with a stored source. Demonstrated on the motivating example: answering *"is 200,000 reasonable for senior living"* needed a WEB SEARCH for Sarasota costs, which a local model cannot do and will confidently fake. P0 preview gate · P1 deterministic "explain this line" (no LLM) · P2 question → typed proposals · P3 stream + change-row actions as ATOMIC bundles · P4 benchmarks. ~a week for P0–P3, useful at every stop. |
| [CR082](cr-082-tax-section-fbar-114.md) | **COMPLETED 2026-08-16 — every phase built and every open item closed.** **Fully deployed 2026-08-16**, migration **071** applied to prod before the code (deploy Step 2b) and the TY2024 filing seeded there too; verified against the running API afterwards. P0b + P1 + P2 + P3 **LIVE ON PROD 2026-08-16**; migration **070** applied dev + prod 2026-08-15. TY2025 complete in the app: **16 lines, every one carrying a figure** (13 computed, 3 typed from statements), aggregate **$2,627,821**, threshold exceeded, Treasury rates from the Fiscal Data API. **The seven remaining items closed the same day** ([§11c](cr-082-tax-section-fbar-114.md#11c-the-remaining-items-closed-2026-08-16)): **P0a** the `coa-traits` account-number leak · **year-scoped `review_state`** (migration **071**) · the **§12b.14 carry-in guard** · **freeze-on-file wired to a button plus amendments** · **TY2024 seeded as filed** (31 lines, **$1,462,652**, matching the client copy) · **XLSX** · **Part I filer block**. **Four defects surfaced while closing them, none visible to the suite:** P0a's frontend half would have written an empty string over real account numbers on any unrelated rename — and over every selected account at once in a multi-edit — had it followed the roadmap's own one-line prescription; **item 15a blocked its own filing**, since an unknown maximum sat in `needs_attention` and `freezeYear` refuses those, so the only way to file a legitimately unknown figure was to override the guard protecting every other line; the filed-vs-recomputed diff **reported "nothing has moved" when nothing had been *checked*** (TY2024's filed lines stand alone by design, so a null delta was indistinguishable from a figure that still reconciles) — found within a minute of seeding TY2024, which is precisely the verification §9 said that seed would buy; and the typed-figure dialog **erased `manual_reason` on a re-edit**, the cause §11b noted without finding. ⚠️ **Nothing has been rendered in a browser** — no renderer on this host, and §11b's eight owner-found defects are the standing evidence that green gates and a correct page are different claims | v3 | A Taxes section, first form: FinCEN Form 114 (FBAR) | Every year the same reconstruction happens outside the app: which accounts are foreign, their account numbers, and the highest each reached during the year. Fin holds every input — **29 years of transactions (1998–2026)**, daily FX back to 1999, per-account opening balances — in none of the shapes the form wants, and the account numbers are not in the app at all (**3 of 57** asset accounts have `accounts.account_number` populated). **Three premises of the request are wrong and each changes the build:** it is **FinCEN**, not FINRA and not the IRS (BSA E-Filing, separate from the 1040; Apr 15 with an *automatic* extension to Oct 15 — the IRS sibling is Form **8938**); Form 114 has **no year-end balance field** at all, asking only for the maximum, so year-end ships as *supporting* data or the export teaches the wrong thing annually; and **"foreign" is not a currency** — `Wise - USD` is reportable, a EUR-denominated Spanish property is not an account — so the designation is an explicit per-account flag with the currency only as a confirmable hint. **The guess about the feed is confirmed: Fintable carries no account numbers** — the v1 `Account` resource is `{id, connection_id, external_id, name, currency, type, owner_app}`, both converters agree, `bankfeed_balances.raw` is **NULL on all 2,120 rows**, and the only institution datum is `institution_name` per *connection*, with no address. **⏱ TARGET TY2025, DUE 2026-10-15** — the owner is on the automatic extension (confirmed 2026-08-15), so P1+P2 is the shippable unit and the freeze ships **with** P2. **Both review passes returned *revise*, and the technical pass falsified four of the CR's own load-bearing numbers — all recorded in its §12** rather than quietly patched, the worst being the CR converting PLN at a stale ≈0.2344 in §5 while §4 argued that a plausible rate asserted as an authoritative one is this project's most-repeated failure. **Three findings shape the design. (1) The maximum must be taken over END-OF-DAY balances, never a row-ordered running sum:** verified on prod, `PKO`'s 2025 maximum is **631,678.72 PLN ($175,842)** end-of-day, against **793,547.72** under `ORDER BY (date, id)` — what `findLedgerWithRunningBalance` (`transactions.js:228-254`) actually does — and **4,393,629.03** under `(date, amount DESC)`. 2025-06-23 saw **4,294,000 PLN in and out, net 0.00**; transactions carry a date and no timestamp, so intra-day order is not information we hold and the naive answer is not one wrong number but a **range selected by an arbitrary tie-break**, +$45,060 to +$1,047,226 of maximum that never existed. The compliance argument is the stronger one: FinCEN directs filers to *periodic account statement* values, which are end-of-day figures. **(2) The maximum must include the January-1 carry-in** — 12 accounts carry in above their in-year maximum, including `Wise - USD` (**1,718.27 vs 1,552.20**, this CR's own flagship reportable example) and `United Beverages` (30,425,000 vs 27,642,000 PLN), while eight with no in-year activity return **NULL**, which must never render as 0. **(3) History is not stable, so a filed year is FROZEN rather than recomputed** — `calibrate()` rewrites `opening_balance`, one constant across **every** historical date and for years with no audit row at all (migration **069** is the case study: months of owner calibrations dragged history and 065 misread the residue as an unrealized loss, and it still writes **none** — the only `audit_log` writer in the repo is `aiReview.js:772`), while `mtm`/`accrue` insert back-dated month-end plugs. All correct for a ledger; all fatal to a number that must never move once filed. Marking a year filed copies every figure, account number and institution name into a snapshot — **copied, not joined** — and reopening shows filed-vs-recomputed side by side, which is also the amended-FBAR story. Conversion uses the **Treasury Dec-31 rate**, prefilled from our frankfurter series into a separate `tax_fx_rates` table (a `source='treasury'` row in `exchange_rates` would collide with that table's `UNIQUE (from, to, rate_date)` and corrupt a series the budget and balance sheet read) and visibly marked a prefill until the owner pastes the real one — the export prints the rate **and its source** on every line. Securities and fund accounts (`PKO TFI`, `CVC Fund VIII`/`IX`) get **no computed maximum**: their reportable value is market value, on a different footing, so they render "needs a figure" with a manual override and reason, and the export refuses to print a number nobody computed or typed. Exports are **XLSX + print-stylesheet PDF worksheets** (owner decision) reusing `excelExporter.js` and the existing `xlsx@0.18.5` — no new library; **explicitly not a filable Form 114**, since FinCEN's own is an Adobe-only XFA form standard tooling cannot fill and a look-alike would look official while being worth exactly what the worksheet is worth. BSA batch XML deferred — its schema is built for volume filers and a rejection cannot be tested without filing for real. ⚠️ **P0 is security, not features.** The owner chose to store **full** numbers, in Postgres only, never in a document — but "masked in the UI by default" was **false as drafted**: `GET /api/v2/util/coa-traits` (`routes/util/coa.js:24-31` → `accounts.js:503-518`) already returns `AccountNumber` for **every active account** to any unauthenticated caller, and `COATreeRow.jsx:78` renders it as a plain column, so §10's "appears in no log line" test would have passed while the API served the whole set. That endpoint, and **nginx basic-auth in front of Fin** (its own tracked item, independent of CR027), land **before any number is entered** — the mitigation cannot be a sentence inside the CR that creates the exposure. Same treatment for the Part I **TIN and DOB**. **§8 holds six owner (not design) calls, none blocking:** the Polish operating companies (a >50%-owned foreign corporation's accounts are reportable as a financial interest; signature authority is Part IV), the CVC funds and `PKO TFI`, the PLN credit cards (defaulted excluded, visible and reversible), which accounts are joint, **signature authority over accounts not in fin at all** (Part IV — the most commonly missed category, and the report-only line's main job), and whether a spouse is covered by **Form 114a**. **P0** nginx basic-auth + close the `coa-traits` leak, before any number is entered · **P1** migration 070 + designation incl. report-only lines + `Taxes` nav (early Sept) · **P2** the end-of-day engine + the report + the $10,000 verdict **+ freeze-on-file** (late Sept) · **P3** exports (early Oct). **The freeze moved out from behind the export on PM review:** you can file by transcribing from the browser, so the export is the convenience while the freeze is the only thing that makes filed figures recoverable — P2 shipping without it means one `calibrate()` run destroys what was filed. Also seeds **TY2024 as `filed`** from last year's actual return, so P2's first run is a verification against a known-good output rather than a leap of faith. |
| [CR083](cr-083-budget-latest-estimate.md) | **IN-PROGRESS — P0a + P0b LIVE (v3.31.0, 2026-08-18), migration 072.** `/budget-le`: create an LE, read it in COA order, edit any category's months in its worksheet, plus a deterministic deviations section and the FY-landing strip on `/budget-vs-actual`. Finalise/recut and warnings L1/L4/L6 are NOT built, and **before finalise ships the full-year budget must be snapshotted onto the LE** — `BUDGET FY` is read live today, correct for a draft and wrong for a frozen artefact (§17.4). Design closed after a THIRD correction round (2026-08-16). Two full dual-review rounds: round 1 falsified **three** of the CR's headline figures, round 2 falsified **four more — three of them inside paragraphs round 1 had just corrected**, which is the finding worth carrying (*a correction is not a fix until its replacement is verified too*). All recorded in §16, none quietly patched. **All ten owner decisions resolved in one `/question` pass** (§15). ⚠️ Round 2 also caught the parallelism biting within the hour: **migration 071 was already taken** by the CR082 thread on disk, so the number is claimed at build time, not reserved. **CR082 and CR084 have since COMPLETED, so the budget surface is uncontended** and migration **072** is free; P0a + P0b committed together | v3 | Budget: the Latest Estimate (LE) | A full-calendar-year LE — actuals to a cut, plus estimate months seeded from the budget, saved as `LE-MM-YY`. **It gets its own two tables, because eleven functions plus a view read `budget_entries` ignoring `version_id`** (`forecast/crud.js:494` — the CR075 base year; four in `fcLines.js`; `budget.js sumByCategory`/`sumByMonth`; `reports.js:526`; `services/budget.js:320`/`:634`; `v_budget_vs_actual` — only `compareToActual` filters), so a second budget *version* would double the forecast base year and every budget report — and `POST /budget/versions/:id/copy` already accepts the **same** `budget_year` with no guard, so that mis-click is one click away today. **Four parts of the ask produce a wrong number as stated.** (1) **Scope decides the answer**: `Unrealized G/L` is **+213,595** YTD with no budget line at all, and with transfers and valuation left in, the LE lands 2026 at **+44,259** against a −224,351 budget — a 269K "improvement" that is entirely unrealised market movement; excluded, it lands at **−102,999**, favourable by 34,556. **The exclusion needs no new flag** — `accounts.is_transfer` is already TRUE on exactly the right 13 accounts, and a `Transfer - %` name match would have missed `Return of Capital` **and** `Valuation - Historical`, which is itself inside the Transfers subtree (−30,438, not the −14,029 a name match gives). (2) **Naive run rate is $147,028 wrong on this book and reports a profitable year** — `UB Dividend` pays in 2 months of 12 (annualising adds **+136,288**) and `Taxes US` −55,000 is budgeted in December alone (annualising makes a real bill vanish). Only **37%** of the remaining number is even a candidate for a method; on the other 63% the correct answer is carry the budget, so `CARRY` is the default and only bucket-C lines are auto-proposed. **The technical pass then found the proposal engine auto-proposing the one line the CR says it must never touch:** `Option Trade` passed all three original guards (r = 3.10 sits *inside* the clamp) and was **57% of the whole effect**. A fourth guard — churn `Σ|amount| ÷ |Σ amount| ≥ 3.0`, on which the book separates cleanly at **29.0 against 1.0–1.3** everywhere else — refuses exactly that one line, and the effect becomes **+9,335**, landing **−93,664**. (3) **"The last full month completed" is not closed** — `Unrealized G/L` for month M is written in M+1 and May's was still being amended on 2026-07-30 — **but "closed" cannot be derived either**: 102 of 113 balance-sheet accounts are `calibrate`, which writes no dated row and no audit row, so the signal exists for 11 accounts and those are exactly the MTM rows the LE excludes. The cut is therefore the **calendar month**, L1 becomes an arrival-lag advisory, and snapshot-plus-drift (L2) carries the load — the whole apparatus was buying **$664** of accuracy. (4) **`MM` = the first estimate month**, so the owner's `LE-08-26` is right when July is closed and honest (`LE-07-26`) when it is not; a January LE is refused, since with zero closed months an LE *is* the budget. **The LE must not feed the forecast base year** — it would move opening cash by **$34,556–$43,891** with 36 years of compounding behind it, and CR075 §2 records that a base-year error does not wash out; a read-only reference column on `FCReview` is the P2 form of that link. FX stays deliberately mixed (actuals at transaction rates, estimates at budget rates — restating at market is worth **+$7 against the DECLARED budget rates** — the −$1,048 an earlier draft quoted is the staleness of stored `base_amount`, which carries nine distinct PLN rates for Sep–Dec), but every row stores its own `fx_rate` + `fx_basis`, because a plausible rate asserted as an authoritative one is this project's most-repeated failure, and the 2026 budget rates are already **4.8% stale on PLN** for Sep–Dec against the real market rate — though the CR's own flagship example was wrong, since the PLN 690,000 dividend is dated 2026-01-01 and sits wholly in the *actual* half. A finalised LE is **immutable and snapshots its actuals**, since `calibrate()` rewrites history with no audit row. 10 warning rules, each stating the number it protects (L11 was cut with the comparison surface). **P0a needs no LE at all** — the FY landing is `budget_FY + (actual_YTD − budget_YTD)`, both terms already on `/budget-vs-actual`, so a KPI there plus the exclusions delivers the headline in about a day and tests whether the owner looks. **The owner's answers then cut a third of the build.** Asked what the LE is *for*, the answer was **primarily a landing number, secondarily an input to next year's budget, explicitly not a frozen series** — which removed the Compare tab, the Versions tab, the LE-to-LE walk, the trend chart, `PY_SEASON`, `L11` and the two `proposed_*` sources. The grid narrowed to **one YTD ACTUAL column + Aug–Dec editable, ten columns, no modes, no sticky column, no horizontal scroll** — which removes the CR082 print-clipping hazard at its root instead of mitigating it. Proposals ship as an **inline advisory with no accept button** (CR081 measured accept-flow uptake at 0/15, twice). And P2 gains what was never in the CR: **seed next year's budget from an LE**. One evidence finding closed the last open question — all 72 uncategorised allowance rows are from the 2026-01-29 initial import and **none** has been added since, while the owner has added **131 itemised categorised rows on top of them**, so they are legacy, not incremental. |
| [CR084](cr-084-save-time-consequence-preview.md) | **COMPLETED** — **LIVE on prod** (v3.29.0 code, shipped inside v3.30.0; spinner v3.30.1); two residuals recorded in §9 | v3 | What this edit does, before you save it | From [CR081 §13](cr-081-ai-line-assistant.md), which both its reviewers preferred to the AI assistant it was carved out of: it serves EVERY edit, not only AI-proposed ones, and needs no token, action schema or benchmark table. `withScratchScenario` is CR053's throwaway-copy lifecycle **extracted** (standalone-not-variant, prune the assumptions DOCUMENT, cleanup in a `finally` — each load-bearing). ⚠️ **Both sides are BUILT**: reading stored entries as "before" would attribute every un-regenerated edit since the last build to the one change being previewed, and stale-entries-beside-fresh-inputs is this system's NORMAL state — a test poisons the stored entries and asserts they never reach the result, verified by making it fail. **Blast radius is COMPUTED**, both `moves` and `doesNotMove`, because CR081 §5's fixed "four variants" was contradicted by live override data and the dangerous direction is the one a number hides. Entries returned RAW so the client diffs them with the same `compareMatrices` Compare uses — a server-side roll-up is what published five wrong figures in CR076 §2. Verified on dev: `Fidelity Stocks` 1× → 2× inflation shows **3,413,574 → 8,325,884** at 2062, real module untouched, zero scratch leaks. **The wiring was built and REVERTED**: the browser check returned 400 because the preview was handed the editor's WIRE payload while `updateModule` takes COLUMNS — the save route maps them in **24 branches** plus schedules, and a preview applying a different transformation previews something the save will not do. The first wiring also caught a preview failure and **saved anyway**; that is gone. The finished modal was DELETED rather than left unreferenced (the orphaned `FCReview.css` shape). |
| [CR085](cr-085-forecast-sensitivity.md) | **COMPLETED — P0 + P1 + P2 all built and LIVE** (migration 073; P0/P1 v3.32.0, P2 v3.33.0, its affordance fix v3.33.1). P1 and P2 both built at owner instruction over the sign-off's defer, with all five scope cuts kept | v3 | Which assumption is load-bearing: sensitivity as a tornado | `/forecast-sensitivity`: up to 8 module/stream/disposal knobs grouped by **type** (asset · liability · income · expense, derived from what the engine branches on, never from free-text `module_type`), each moved down and up on its own, every point a REAL `generateForecast` build on CR084's scratch harness. Ranked bars coloured by whether the **metric** moved favourably; clicking one opens the **trajectory** behind it (`FCTrajectoryChart` reused unchanged) with an absolute and a difference-from-base view, because the bar cannot say WHEN the damage lands. **Four defects of one shape found while building it** — an excluded module, a negative-width bar label, a valuation-gated field (**109 of 300 knobs could not move anything**), and its own §4 filing `growth_rate` as a rate when the engine reads it as a MULTIPLIER of inflation. Pass 1 *revise* (§14), pass 2 *GO P0 / DEFER P1* (§15), competitor review (§16), as-built §17–§18 |
