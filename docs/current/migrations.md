# Database Migrations Registry

One line per migration, in apply order. The SQL files in
[`server/db/migrations/`](../../server/db/migrations/) are the source of truth —
keep this list in sync when adding a migration (CI applies the whole chain to
a fresh database, so a migration that only works on a data-bearing DB will
fail there).

**How migrations run today:** a real runner exists (CR043 Phase 1.1, pulled
forward from CR027A) — `server/db/migrate.js`, `npm run migrate` (dry-run:
`npm run migrate:dry`). It records applied files in a `schema_migrations`
ledger (filename + md5 checksum + baselined flag) and applies only the gap,
each file in its own transaction; it warns on checksum drift (an applied file
edited afterward — the class that bit CI in 4931b2a). On its **first** run
against an already-populated DB it *auto-baselines*: records every current
migration as applied without executing it (also correct for a fresh volume,
where initdb.d has already run them). `deploy-to-production.sh` runs it as
Step 2b (after backup, before rebuild), so prod adopts the ledger on the next
deploy. Dev adopted it 2026-07-11 (36 baselined). Postgres
`docker-entrypoint-initdb.d` still auto-applies `*.sql` on a fresh empty
volume; the runner and initdb.d coexist (the runner baselines whatever initdb
already ran). Wiring the runner into container **start** is a possible
follow-up; not done (deploy-time application is the v3-safe payoff).

**CI baseline:** [`server/db/ci-seed.sql`](../../server/db/ci-seed.sql) (not a
migration) seeds the few COA rows engines reference by hardcoded id/name
(`accounts.id=88` Unrealized G/L; `Transfer - Securities Trades`;
`Financial Income - Dividend`; `Option Trade`; `Interest Income`) so the test
suite runs on a fresh DB. `Interest Income` was added 2026-08-01 — it is in
`INVST_INCOME_LEAF` (quicken-promote.js) like the other two, and its absence
made the promote suite fail with the engine's own "seed it before promoting"
error. It is a **baseline**, not a fixture drawer: a test that needs an
account, an fc_line or a transaction seeds its own and cleans up by name.

| # | File | What it does | CR |
|---|------|--------------|----|
| 001 | `001_initial_schema.sql` | Core schema: accounts, transactions, budget, forecast scenarios/modules, exchange rates | — |
| 002 | `002_psdata_staging.sql` | PocketSmith staging table | — |
| 003 | `003_accepted_field.sql` | `transactions.accepted` review flag | — |
| 004 | `004_budget_fx_rates.sql` | `budget_fx_rates` (monthly budget FX per currency/year) | — |
| 005 | `005_transfer_match_groups.sql` | Manual transfer match groups | — |
| 006 | `006_transfer_matched_flag.sql` | `transactions.transfer_matched` flag | — |
| 007 | `007_fc_lines.sql` | `fc_lines` + `fc_line_categories` + module FK columns (forecast mapping layer) | FC |
| 008 | `008_drop_old_fc_columns.sql` | Drops `expense_category`/`income_category`/`expense_pct` (replaced by FC Lines) | FC |
| 009 | `009_target_cash.sql` | `target_cash` on scenarios (cash auto-balance) | FC |
| 010 | `010_tax_rate_override.sql` | Per-module `tax_rate_override` | FC |
| 011 | `011_setup_status.sql` | `setup_status` on modules (income_expense was altered ad hoc, never in this file — backfilled by 036) | FC |
| 012 | `012_cash_sweep_target.sql` | Single `cash_sweep_target` flag per scenario | CR005 |
| 013 | `013_cash_sweep_band.sql` | `cash_sweep_low/high` band replacing `target_cash` | CR005 |
| 014 | `014_ai_reviews.sql` | `ai_reviews` conversation storage | FC |
| 015 | `015_disposal_date_end.sql` | Optional end date for periodic disposals | FC |
| 016 | `016_opening_balance.sql` | `opening_balance` calibration columns on accounts | — |
| 017 | `017_investment_date_end.sql` | Optional end date for periodic investments (mirrors 015) | FC |
| 018 | `018_category_source_mappings.sql` | `category_source_mappings` (external↔internal category names) | — |
| 019 | `019_account_source_mappings.sql` | `account_source_mappings` (external↔internal account names) | — |
| 020 | `020_ai_review_async.sql` | Async status tracking on AI reviews (poll via `GET /:reviewId/status`) | FC |
| 021 | `021_collapse_categories_into_accounts.sql` | Collapses `categories` into `accounts` (P&L leaves carry `is_transfer`/`ps_category_id`; FKs repointed; `categories` + `category_source_mappings` dropped) | — |
| 022 | `022_quicken_import.sql` | Quicken import scaffolding: 12 tables (4 staging, 6 investment, batches, audit), `security_tx_type` enum, `import_batch_id`, `skip_transfer_analysis`, sentinel `opening_balance_date` → 1990-01-01, 4 COA leaves; creates the `Transfers` root on a fresh DB | CR019 |
| 023 | `023_bank_feed_import.sql` | Bank-feed parallel import: `bank_feed_external_id`, `bankfeed_staging`, `sync_metadata`, `account_source_mappings.ignored` | CR022 |
| 024 | `024_bank_feed_ignore_unmapped.sql` | Drop NOT NULL on `account_source_mappings.account_id` (ignore-without-mapping) | CR022 |
| 025 | `025_fidelity_feeds.sql` | `bankfeed_balances` cache + `balance_from_feed`/`trade_treatment` mapping flags | CR024 |
| 026 | `026_fidelity_activity.sql` | `bankfeed_staging.activity_type`/`.suppressed` (SnapTrade activity routing) | CR024 |
| 027 | `027_promote_from_date.sql` | Per-mapping promote cutoff date (cutover gate) | CR024 |
| 028 | `028_reconcile_mode.sql` | `reconcile_mode` (`calibrate` \| `mtm`) per mapping (source-aware reconciliation) | CR023 |
| 029 | `029_feed_balance_sign.sql` | `feed_sign` per-mapping balance-sign override (Plaid negative-liability cards) | CR023 |
| 030 | `030_feed_negate_tx.sql` | `feed_negate_tx` per-mapping transaction-sign flip (e.g. Chase purchases-positive) | CR028 |
| 031 | `031_cash_sweep_priority.sql` | `cash_sweep_priority` ordered sweep set (backfills `cash_sweep_target` → priority 1) | CR017 |
| 032 | `032_manual_calibration.sql` | `manual_balances` table + `accounts.manual_reconcile_mode` (non-fed calibration) | CR033 |
| 033 | `033_feed_source_synced_at.sql` | `bankfeed_balances.source_synced_at` — true upstream connection sync time | CR035 |
| 034 | `034_forecast_assumptions.sql` | Drops the never-used 001-era `forecast_assumptions` and recreates it as the CR039 document store (key/JSON value/ord) replacing `FCAssump.json`; **after applying, run `node server/src/v2/scripts/import-fc-assumptions.js`** | CR039 |
| 035 | `035_ai_review_compare.sql` | Adds nullable `fc_ai_reviews.compare_scenario_id` (FK → forecast_scenarios, CASCADE) + index so Compare-page AI conversations persist their scenario pair; NULL = plain single-scenario review | CR040 |
| 036 | `036_incexp_setup_status.sql` | Backfills schema drift: `forecast_income_expense.setup_status` (existed on dev/prod since the 2026-04 AI review work but never in a migration; broke CI's fresh-from-migrations DB once aiReviewCompare tests exercised the query). No-op where the column already exists | — |
| 037 | `037_module_income_expense_window.sql` | **CR046** — four nullable DATE columns on `forecast_modules`: `income_start_date` / `income_end_date` / `expense_start_date` / `expense_end_date`. Bound **when** a module's amount-based income/expense stream runs; the amount stays a base-year figure compounded at inflation. NULL = unbounded = the old behavior, so every existing scenario is byte-identical. Applied dev + prod 2026-07-12 (v3.0.81). |
| 038 | `038_module_income_tax_override.sql` | **CR047** — `forecast_modules.income_tax_rate_override` (nullable): tax rate applied to a module's **income only**; realized capital gains keep `tax_rate_override` / the scenario rate. For income received already taxed abroad (United Beverages' dividend is net of Polish tax; the incremental US tax is ~3%) while a future sale is still an ordinary gain. NULL = falls back = no change. Applied dev + prod 2026-07-12 (v3.0.84). |
| 039 | `039_scenario_variants.sql` | **CR050** — scenario **variants** (inherit-unless-overridden): `forecast_scenarios.parent_scenario_id` (RESTRICT — a base with variants cannot be deleted) + `synced_at`; `origin_base_id` on `forecast_modules` / `forecast_income_expense` (the base row a variant row was materialized from; NULL = variant-local) + partial unique indexes; and `forecast_scenario_overrides` — one **JSONB patch per overridden base row**, keyed to the base row's **id** (field-level: `NULL` is already load-bearing in these columns, so "NULL = inherit" was never available). A trigger rejects a variant of a variant. All nullable/empty ⇒ **a scenario with no parent behaves exactly as it does today**; no backfill. Applied dev + prod 2026-07-14 (v3.0.108). |
| 040 | `040_fix_container_account_types.sql` | **Data fix (v3.4.6)** — corrects five accounts whose `account_type` sat on the wrong side of their (correct) `section`, surfaced by the COA Type filter: `Income` container `expense→income`; `Liabilities` / `Tax Liabilities` / `Tax Reserve - PL` / `Tax Reserve - US` `asset→liability`. Guarded by name + current type + section ⇒ idempotent, touches only the five known rows. **Report-neutral** (Balance Sheet is `section`/tree-driven; the sole forecast `account_type` branch gates `ExpensePct`, hardcoded 0; Budget/Tx use it only in `GROUP BY`). Legacy data only — the current `seedAccounts.js` already types these correctly, so no seed change. Applied dev 2026-07-23; prod via deploy runner. |
| 041 | `041_income_restatements.sql` | **Checksum drift resolved 2026-07-31** — the file was edited in place by `10eb270` *after* it had applied on dev and prod (it hard-coded `parent_id = 200`, which aborts the whole chain on CI's migrations-only DB where `Transfers` gets a serial id; a forward migration cannot repair a file that aborts before later ones run). The ledger therefore recorded the pre-edit checksum and every deploy warned. Verified equivalent before accepting: `Transfer - Distributions` is id **228**, parent `Transfers`, `is_transfer = TRUE` on **both** dev and prod, and re-running the current file inside a transaction produces **no INSERT, no UPDATE and no error** on either. Re-recorded with `migrate.js --accept-drift=041_income_restatements.sql`. | **CR057** — two objects for **Book Income at Source**. (1) `income_restatements` — the undo record for a three-leg restatement: `source_transaction_id` **UNIQUE** (makes the "already booked" 409 structural, not a skippable lookup), `holding_account_id`, `original_category_id`, the two created leg ids, and a `leg_snapshot` JSONB of both legs as written — undo compares field-by-field and **REFUSES** on divergence, because deleting a leg someone has since edited would move the holding's book value and silently invalidate every later `Unrealized G/L` mark (each was written as `target − book`). A dedicated table rather than reusing `transfer_match_groups`, whose user-facing **Unlink** button would delete the audit trail, whose `create()` cannot join an outer transaction, and whose membership *excludes* rows from auto-matching — it would have prevented the very match it recorded. (2) The `Transfer - Distributions` COA row under `Transfers` (200), `is_transfer = TRUE`, `skip_transfer_analysis = FALSE` — created **here**, not in `seed-cr019-coa.js` (a manual admin CLI, not a seed path): via the generic COA create path `is_transfer` DEFAULTs FALSE, which fails **silently and identically to the bug being fixed** (leg 2 would bucket as income, income would net to zero, the report would still read 0.00%, and the identity would still close because `fxEffect` is a plug). A `DO` block re-asserts the flags and raises if wrong. Both statements name-guarded ⇒ idempotent. Applied dev 2026-07-27; prod before the v3.6.0 deploy. |
| 042 | `042_valuation_anchors.sql` | **CR058** — the `Valuation - Historical` COA leaf under `Transfers` (`is_transfer=TRUE`, `skip_transfer_analysis=TRUE`) that Quicken-era valuation anchors post to, plus `quicken_import_batches.calibration_mode` (`TEXT NOT NULL DEFAULT 'ps-anchored'`, CHECK in `('ps-anchored','preserve-today')`). The leaf is **not** `Unrealized G/L`: each anchor mixes real market movement with liquidation timing, money-market sweep churn and gaps in Quicken's own share history, so routing them to CR056's unrealized numerator would manufacture a confident, wrong pre-2020 return series (CR058 §3.3). `skip_transfer_analysis` matters — an anchor has no counterparty and would otherwise sit in /transfer-analysis forever as unmatched. The parent is resolved **by name**, never a hard-coded id: that is exactly what broke 041 on CI's migrations-only database. The new column defaults every existing batch to `ps-anchored`, so nothing already promoted changes behaviour. Idempotent (name-guarded insert, `ADD COLUMN IF NOT EXISTS`, constraint added only if absent) and verified on a fresh `postgres:16-alpine` — all 42 migrations + `ci-seed.sql` apply cleanly. Applied dev 2026-07-28; **prod 2026-07-28** (rode along with the v3.6.6 deploy, ahead of the data rollout that references the leaf — verified in place as id 229 with both flags true before anchors were written). |
| 043 | `043_pin_bank_feed_promote_cutoffs.sql` | **Data fix (CR059 follow-up)** — pins the two *live* bank-feed mappings that still had `promote_from_date IS NULL`. A NULL cutoff means "promote every staged row whatever its date", which is the mechanism behind the 2026-07-14 **Black Card** incident (mapping an account back-filled its whole staged history over a period a manual upload already covered: 31 duplicates, $8.4K gross, **net only +$267**, so no balance check could see it). Each is pinned to the **earliest row already staged** for it (`531` Revolut-USD → 2026-07-26; `530` Revolut-PLN → 2026-07-30, nothing staged), so **current behavior is unchanged** — everything staged still promotes — while a row arriving *later* dated before that point is blocked. The three ignore-only rows (`366`, `369`, `440`, `account_id IS NULL`) stay NULL deliberately: nothing promotes for them and a pin set now would be stale by the time they are mapped. A `DO` block re-counts and **raises** if any mapped bank-feed row still lacks a cutoff, so a half-applied fix rolls back. Idempotent (`WHERE promote_from_date IS NULL`; re-run gives `UPDATE 0`). **Not claimed:** this does not close the Black Card class — see the CR059 §17 R5 note. Applied dev + **prod 2026-07-30**. |
| 044 | `044_bank_feed_api_id_crosswalk.sql` | **CR059 P3a** — rewrites bank-feed account ids from the Google Sheet's UUIDs to fintable's API ids, at the two fin-side sites: `account_source_mappings.external_name` (31 rows) and `bankfeed_staging.feed_account_external_id` (2,222 rows). Without it, swapping upstreams orphans **every mapping at once** — each fed account silently unmapped, taking its `feed_sign`, `feed_negate_tx` and `promote_from_date` with it (CR059 §4). The row is UPDATEd rather than replaced, so those settings carry across; ignore-only rows are rewritten too, or an account deliberately excluded quietly returns. **GENERATED** by bank-feed `scripts/emit-crosswalk-sql.js` from a verified plan (`scripts/crosswalk-plan.js`) — the 31 pairs are never retyped. The plan uses **two independently computed signals** (the API account id embedded in our own composite tx ids, and name+currency), not a fallback chain: where both fire they must agree, and disagreement produces a refusal rather than a mapping. Verified a complete **bijection** (31 distinct Sheet ids → 31 distinct API ids, no unclaimed API account) and identical across repeated runs before generation. **`transactions.bank_feed_external_id` is NOT touched** — §4 called it "the site that bites", but pass-2 M1 removed the need by putting the ingest floor at the cutover, and CR059 §19's boundary carry-over keeps the overlap rows on the ids we already hold, so the ledger's only duplicate guard keeps matching and this migration never writes to the ledger. Preconditions abort before any write (an API id already present as a mapping; a mapping the plan does not cover); post-conditions check no leftover Sheet ids, **no orphaned staging row**, and no duplicate `external_name` — the orphan check being the one a count comparison cannot replace. **Rehearsed against a throwaway copy of real prod data:** 31 + 2,222 rows rewritten, 0 leftovers, 0 orphans, counts preserved, idempotent (`UPDATE 0` on re-run); the coverage precondition **falsified** by injecting an uncovered mapping, which aborted and rolled back. Pairs with bank-feed migration **006** (`feed_accounts.external_id`) — both must be applied in the same window, or fin and bank-feed disagree about what an account is called and nothing promotes. Order-independent vs **050**. **APPLIED TO PROD 2026-08-01 18:53:04 BY ACCIDENT AND REVERSED BY 051** — see that row; it was still untracked in `server/db/migrations/` when another thread's deploy scanned the directory. It is also **incomplete**: it rewrites two of fin's three feed-id columns and misses `bankfeed_balances.feed_account_external_id`, so the reconcile page could not join any account to its feed balance. Kept as an applied-then-reversed artifact rather than edited under its recorded checksum; **the real crosswalk needs a new number and must cover all three columns.** |
| 045 | `045_retire_2000_sentinel.sql` | **Sentinel root fix (data half)** — finishes what **022** started. Every balance reads `opening_balance + SUM(amount) WHERE transaction_date >= opening_balance_date`, so that date is a **floor** and rows below it are invisible to every report. 022 moved all accounts off the `2000-01-01` sentinel and set the column DEFAULT to `1990-01-01` — but `accounts.create()` hard-coded `'2000-01-01'`, so every account created since **re-introduced the sentinel 022 removed** (11 by 2026-07-30; the code path is fixed in the same commit). Only one currently hid anything — **Chase Checking**, a 1999-12-31 row worth **1,950.61** invisible to every read — but the other ten were latent traps for any pre-2000 row added later. Lowers the floor to `1990-01-01` and reduces `opening_balance` by exactly what that makes visible, so **today's balance is preserved on every account** (same preserve-today reasoning as CR058: the current balance is feed-owned and already right, so the correction belongs in the plug). Snapshots each balance into a temp table BEFORE the change and **raises** if any moved by more than a cent, rather than re-deriving the same arithmetic and proving nothing. Applied dev + prod 2026-07-30; 11 accounts retired, 0 balances moved, 0 rows hidden anywhere afterwards, and both anchored Fidelity accounts still tie. *(044 is reserved by CR059 P3a.)* |
| 046 | `046_cashmgt_mtm_mode.sql` | **CR058 §12 follow-up — stop Fidelity Cash Mgt calibrating its history away.** `reconcileToFeed.calibrate()` sets `opening_balance = expected − Σtx`, so the entire gap between fin and the custodian is absorbed into a **single constant at the account's opening** — making *today* right and every other date wrong by exactly the movement it swallowed. Cash Mgt was the **only** Fidelity account still on `calibrate` for its feed mapping and the only one with **zero mtm rows ever written** (Stocks, IRA, Bond and Options all carry `mtm`); its plug had reached a constant **−14,436.32**. `mtm` is right here on evidence rather than by analogy: the account holds **brokered CDs**, whose market value genuinely moves, its cash flows match the custodian exactly, and its gap **flips sign** between quarters (**+4,863.51** at 2026-03-31, **−5,161.51** over Q2) — an accumulating bookkeeping error does not change direction, unbooked price movement does. **Order matters, and is why the plug is NOT reset here:** CR058 §12 first restated the 2026 quarter-ends from the custodian's statements (2026-03-31 → 589,320.11, 2026-06-30 → 804,030.47) so the movement now lives in dated rows; reducing `opening_balance` while that history was still unbooked would have moved today's balance by 14,436.32 and broken a figure that is currently correct. A `DO` block **raises** unless exactly one mtm feed mapping results, so a renamed account or a re-keyed feed id fails loud instead of silently no-opping. Idempotent (`WHERE reconcile_mode = 'calibrate'`; re-run gives `UPDATE 0`, verified). Applied dev + **prod 2026-07-31**. **Amended 2026-08-01 — the guard was unconditional and aborted the CI chain.** CI builds its database from the migrations alone, seeds afterwards, and therefore has no bank-feed mapping at all; `n_mtm = 0` there is the *absence* of anything to guard, not a re-keyed feed, so `backend-tests` and `e2e` both died at 046 and every job downstream of the chain went with them. A forward migration cannot repair a file that aborts before it runs (the 041 precedent), so the file was edited: the zero-mapping case now RAISEs a NOTICE and skips, while a mapping that exists and is not `mtm` — including the renamed-account case, which keeps its row because the mapping is keyed by `external_name` — still fails loud. **Verified equivalent before accepting the checksum drift:** dev and prod both hold exactly one mtm mapping and zero calibrate, so the edited file takes the same branch and reports the same NOTICE as the original on both. |
| 047 | `047_forecast_loan_module.sql` | **CR062 P1** — the forecast can model a **loan**. Four nullable columns on `forecast_modules` (`loan_principal`, `loan_start_date`, `loan_end_date`, `loan_interest_rate`) plus `forecast_module_amortization` (per-year principal schedule, `pct` = % of the **original** amount, `CHECK (pct >= 0)` because a negative percentage is a silent re-draw). Until now a loan was **inexpressible**: liabilities exist as modules but nothing could charge interest — migration 008 dropped `expense_pct` and the loader hard-codes `ExpensePct = 0`, so the engine's "liability interest model" has been unreachable from real data ever since; and `Dispose` is **silently zeroed** on a negative balance (the "MV cannot go negative" cap), leaving `Invest` as the only working primitive. The five assumptions are **stored**; the schedule is **derived on every generate** and never materialized — writing thirty `Invest` rows from a wizard is the CR049/CR050 rot pattern, where changing the rate leaves stale rows that still look authoritative. The engine activates on `loan_interest_rate IS NOT NULL`, **never** on `module_type`: that column is a user-editable free-text list in Forecast Settings (prod already carries a lowercase `asset`) which the engine has never read, so a renamed type must not stop a scenario charging interest. The `loan_end_date` year is the **remainder** year and carries no schedule row — that is what makes a loan close at exactly zero rather than 40 cents short, since a straight-line `100/9` stored at 4dp sums to 99.9999%. Every column nullable + the table empty ⇒ **dormant**: an existing scenario regenerates byte-for-byte. Idempotent (`ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`; re-run verified) with a `DO` block that **raises** unless all four columns and the table exist, so a half-apply rolls back. Applied dev 2026-07-31; **prod 2026-07-31** with the v3.8.0 deploy (Step 2b). |
| 048 | `048_forecast_loan_secured_asset.sql` | **CR062 P2** — `forecast_modules.secured_asset_module_id` (nullable, self-FK, `ON DELETE SET NULL`): the asset a **loan** is secured against, so the forecast can report **equity** rather than only gross value. Direction is many loans → one asset (a mortgage and a HELOC on one house), and **any** module qualifies — a margin loan against a brokerage account and a shareholder loan against a business are the same shape. `SET NULL` not `CASCADE`: deleting the house must not delete the mortgage. **The migration exists as much for its `DO` block as for its column.** `copyScenario` inserts modules ONE AT A TIME, so a naive copy carries the *source* scenario's module id into the copy — and the copy's Equity report would then read the **source scenario's asset**, with both figures real and neither obviously wrong. CR050 variant sync has the same shape (the override patch stores a base-scenario id). Both are fixed in code (an `oldId → newId` map plus a second UPDATE pass in `copyScenario`; `origin_base_id` translation in `syncEntity`), and the `DO` block **RAISES** if any link crosses a scenario boundary or points at itself — so a future edit that reintroduces the class fails at deploy rather than in a report. Idempotent (`ADD COLUMN IF NOT EXISTS`, partial index `IF NOT EXISTS`; re-run verified). Applied dev 2026-07-31; **prod 2026-07-31** with the v3.9.0 deploy (Step 2b). |
| 049 | `049_coa_display_order_per_parent.sql` | **CR063 P1** — makes `accounts.display_order` the authoritative Chart of Accounts order, as a **rank within the parent**. The column has been in the schema since the seed and `findAll`/`getChildren`/`getBalances` all order by it — but `getTree()`, the one path every tree, report and dropdown goes through, **selected it and then sorted `ORDER BY path`, where `path` is `ARRAY[id]`**, so the COA page, Balance Sheet, Cash Flow, the budget worksheet and the forecast account list have all rendered in **insertion order**. The sort could not simply be flipped: the column was seeded as one flat 0–207 sequence rather than a per-parent rank, and `accounts.create()` hard-coded `display_order = 0` (22 accounts tied on dev), so **68 of 229 rows would have moved** — in three reports, not just the page being changed. **The one decision that matters:** the rank is computed `ORDER BY id` — the currently *visible* order — and deliberately **not** `ORDER BY display_order, id`, which would bake the reshuffle in instead of preventing it. Three `DO`-block post-conditions: no duplicate `(parent_id, display_order)` pair (the state that makes sibling order non-deterministic again), ranks 1..n gap-free per parent (or "append at MAX+1" and "swap with the neighbour" stop agreeing what a position is), and — the one that matters — the sequence `getTree` will render **equals** the sequence it renders today, snapshotted into a temp table before the UPDATE. That third check verifies the write reached every row; the real evidence is external, and it holds: `reports/balance`, `reports/cash-flow`, `budget/cash-flow` and `budget/category-groups` are **byte-identical** before and after, and both COA trees have an **identical name sequence** (differing only by the added `id`/`display_order` fields). Inactive rows are ranked too, so a reactivated account lands somewhere defined rather than colliding at 0. Idempotent (`IS DISTINCT FROM` guard; re-run updates 0 rows). Applied dev **and prod** 2026-07-31 (prod via `deploy-to-production.sh` Step 2b, ahead of the rebuild — mandatory here, since the code that ships with it reads a column whose *meaning* this migration changes). |
| 050 | `050_revolut_usd_ignore_only.sql` | **CR059 §18 / P4 prerequisite** — stops promoting from fintable's legacy Revolut wallet. **Measured 2026-08-01:** fintable's API serves four Revolut EUR transactions **twice** — once under the current EUR wallet (`acc_01KYS5BECVFH89QPSRQSFN4M99`, 4 rows all EUR, balance 98.13 EUR) and once under the legacy numeric-id wallet `4044604745776048193` that fintable labels **"(USD)"** (4 rows, also all EUR, balance 1.46 USD). Same dates, amounts and descriptions but **different transaction ids** (`tx_01KYS5PV…` vs `tx_01KYFYT7…`), so these are two genuine upstream records, not a converter mis-filing one row; the `KYS5` ULID prefix matches the rebuilt wallet, so the rebuild re-served history under fresh ids. This **corrects** bank-feed `6d108ec`'s claim that the API converter "never had the defect, so the cutover retires this class" — it inherits it by a different mechanism (fintable's own attribution, not our name join). **Not a problem today:** `6d108ec` fixed the Sheet converter to read `ext_nordigen_acc_id`, the live store carries 0 transactions on that wallet, and all three Revolut accounts tie exactly (EUR 98.13, PLN 72.14, USD 1.46) with every row promoted once. **The cutover risk it removes:** the date floor (`FINTABLE_API_MIN_DATE`) blocks all four known duplicates (dated ≤ 2026-07-26) but not a *new* row filed there later — mapping 531 was live with `promote_from_date = 2026-07-26`, so such a row would book a EUR amount into a USD account, invisible to a balance check for the same reason the Black Card incident was. Mapping 531 becomes ignore-only (`account_id` NULL, `ignored` TRUE, `promote_from_date` NULL per 043's convention). **Costs nothing real:** Revolut-USD holds 1.46 USD across 4 transactions, most recent 2025-01-19; it keeps its balance and history. Owner decision 2026-08-01, over leaving it mapped behind the floor and over a general currency guard (new code + tests, would delay cutover — kept as a roadmap item). A `DO` block **raises** if the mapping is still live *or* if any other mapped bank-feed row lost its cutoff, so a mistake fails rather than silently re-opening a back-fill window. Idempotent (re-run gives `UPDATE 0`, verified). **Order-independent vs 044:** it originally keyed on the Sheet UUID that 044 rewrites, so 044-then-050 would have matched nothing — and the guard would have *passed*, since zero rows match a name that no longer exists, leaving the mapping live. It now names **both** ids and **raises unless exactly one matches**, so "nothing matched" can no longer read as success; both orders verified to converge on an identical final state. Applied **dev 2026-08-01**, **prod 2026-08-01** — it rode the accidental deploy that carried 044 (harmless, and still correct), so there is nothing left to apply at the P4 cutover. **AMENDED 2026-08-02 (CR064 P1) — an applied migration edited under the `.claude/rules/migrations.md` "unavoidable" clause, the same clause 046 used:** the `found <> 1` guard added by `2d49ff3` was unconditional, so it **aborted the whole chain on a data-free database** — CI applies every migration to an empty Postgres and runs the seed only afterwards, and an aborted migration cannot be repaired by a later one. That is the third instance of this class (046 was the first) and it means **CI has been red on `main` since `2d49ff3`**, unannounced, exactly as [Known Issue #12](project-roadmap.md#3-known-issues) predicts. The fix keeps the guard and adds the 046 skip: a database with **no bank-feed mappings at all** has nothing to guard and skips; a database that has feed mappings but not this one still fails loud. Verified: full 52-file chain now builds on an empty Postgres. **The ledger checksum on dev and prod now disagrees with the file** — resolved 2026-08-02 per the rule on **dev and prod**, per file, after proving equivalence rather than asserting it: prod carries **31 bank-feed mappings**, so the added `any_feed = 0` skip branch cannot fire, and re-running the current file inside a transaction against prod gives **`UPDATE 0`, the `DO` block passing, and a rollback**. Both ledgers now report zero drift. |
| 051 | `051_rollback_bank_feed_api_id_crosswalk.sql` | **Reverses 044.** Two independent failures met: 044 reached prod **72 seconds before it was committed** (untracked in `server/db/migrations/`, swept up by another thread's `deploy-to-production.sh`, which scans that directory — an unfinished migration parked there is not inert), and it was **incomplete**. fin keys feed accounts in **three** columns — `account_source_mappings.external_name`, `bankfeed_staging.feed_account_external_id`, and `bankfeed_balances.feed_account_external_id` — and 044 rewrites the first two. `bankfeed_balances` is named in neither 044 nor CR059 §4's identifier inventory, which listed four sites and missed this one. Result: **all 27 live mappings read "no feed"** on the reconcile page, which is how it was found. **The feed stalled rather than corrupting** — the safe direction: Sheet rows kept arriving under Sheet ids, matched no mapping, and did not promote. Measured before the rollback: **255 staged rows unmapped, 90 unpromoted, 0 promoted and 0 ledger rows created** across the whole ~14-hour window, so there was nothing to repair. 051 reverses both UPDATEs from the **same pairs** as the forward migration (generated by the same script) so the directions cannot drift, and its post-conditions assert what actually matters — no orphaned staging row, and **every live mapping resolves to a feed balance again** — rather than counting rows changed. It deliberately does **not** undo **050**: the legacy Revolut wallet being ignore-only is still correct and 050 matches either id (verified preserved). Rehearsed on a throwaway copy of prod first (no-feed mappings 27 → 0, unmapped staging 255 → 0, idempotent), with the three tables dumped and gzip-verified beforehand. Applied **prod 2026-08-02**; all six reported accounts resolve to same-day balances afterwards. |
| 052 | `052_forecast_assumptions_orphan_prune.sql` | **CR064 P1/P3** — prunes assumption entries that name a scenario which no longer exists, and deletes the two blank forecast modules. `forecast_assumptions` is four JSON documents keyed by scenario **NAME** (CR039); nothing enforces that the name resolves. **Measured on prod 2026-08-02:** five scenarios exist, but `inflation` carried ten entries, `FX` eleven, `Tax Rate` ten and `scenarios` six — five dead names between them (`Test`, `ZZ Test Sandbox`, `2026 with House Purchase`, `2026 Base - Market Returns`, `Base_Buy Business`). They are dead weight on their own; what makes them worth a migration is what they are evidence OF — `PUT /scenarios/:id` renames the row and leaves every document behind, and the resulting failure is **not** the loud one it looks like: the next generate throws, the owner saves Forecast Settings to clear it, that write refreshes only `scenarios` from the DB names, and generate then **succeeds** with an empty inflation list, which `buildRates` seeds as `entries[0]?.Rate ?? 0` — **0% inflation for 36 years, silently** (the CR045 §1 / CR048 copy-drops-a-column class). CR064 §2.4 makes rename atomic (`renameScenario`, one transaction, and `updateScenario` now **throws** on a name so no caller can bypass it); this migration cleans up after the renames that already happened. Rebuilt with `json_build_object` in the existing key order + `json_agg` over `WITH ORDINALITY` — never a jsonb round-trip, because `value` is `json` on purpose (CR039: jsonb reorders keys and broke byte-parity); **surviving entries verified byte-identical, key order included**, against a copy of prod. The **pre-condition is the part that matters**: orphans alongside a live scenario that has no inflation entry of its own is the shape of a RENAME, not a deletion, so the migration **refuses** rather than destroying a live scenario's assumptions — falsified by renaming `2026 Base` on a prod copy and watching it abort and roll back with everything intact. Post-conditions assert the invariant, not row counts: no entry names an unknown scenario, and nothing that resolved before the prune is missing after it (**not** "every scenario has an entry", which would abort CI's data-free chain — the 046/050 defect). Also deletes the two nameless, accountless, value-free modules in `2026 Upside` / `2026 Downside` (**0 `forecast_entries` between them**, so no report can move), on a predicate that spares anything merely half-filled; §4.3 stops them being written at all. Idempotent (re-run: `DELETE 0`, documents rewritten to themselves). Rehearsed three ways on copies of real prod data — apply, re-run, and the rename refusal — plus the **full 52-file chain on an empty Postgres + `ci-seed.sql` + 731 backend tests green**. Applied **dev + prod 2026-08-02** (schema before code, ahead of the v3.11.3 deploy); prod's prune left **0 blank modules** and no orphaned assumption entry. |
| 053 | `053_neutralize_pair_identity.sql` | **CR065** — `transactions.paired_with_id` (nullable self-FK, symmetric on both rows of a neutralize pair) + a **partial UNIQUE index** so two rows may not name the same counter-leg: the double-claim is refused by the database, not by a `WHERE` clause. `neutralize()` had decided "does this row already have a counter-leg?" by re-running a **value match**, and value-matching is not identity — on 2026-07-30 two identical $150,000 CD purchases both paired against one +150,000 mirror and Fidelity Cash Mgt ran **$150,000 light** (most of a −107,830.71 "MTM gap"). Backfills the **mirror path only** and only where exact (`original.updated_at = mirror.created_at` to the microsecond, plus account/date/description/negated amount, 1:1 both ways — 316 rows / 158 pairs on dev, 18 mirrors left NULL); PAIR-path history left no trace and is deliberately not guessed at. Also stamps `app_data.cr065_pairing_since_tx_id = MAX(id)` — the watermark bounding the new unpaired-leg check, so it reports the era where pairing is actually recorded instead of ~1,800 legacy legs. Idempotent. Applied **dev + prod 2026-08-02** (prod via `node server/db/migrate.js`, ledger recorded; backfill linked **328 rows / 164 pairs**, 18 mirrors left NULL). |
| 054 | `054_link_sweep_mirror_pairs.sql` | **CR065 follow-up** — links the pairs 053 could not see. `neutralize()` is not the only thing that makes a pair: `refreshBankFeedV2` injects the CR032 core-sweep mirror on every promote, and `transferToAccount` writes a cross-account offset — neither recorded `paired_with_id`, so **an unrecorded pair looks unclaimed**, which is exactly the state the double-claim guard reads. Proven on prod within the hour: the v3.11.4 boot-reconcile promoted a Fidelity Bond sweep (2709863 −20,000 + mirror 2709864) without recording it, a neutralize of the bond redemption 2709858 then claimed 2709863 as apparently free, and 2709864 was orphaned — the same defect one level down, +$20,000. It also made the new unpaired-leg check report its own false positives (6 on Fidelity Cash Mgt straight after the deploy, gaining two per sweep per day). Links post-watermark pairs on account + date + description1 + negated amount where `mirror.created_at` matches the original`s `created_at`/`updated_at`, 1:1 in both directions, leaving anything ambiguous NULL. Bounded to the 053 watermark. Idempotent. Applied **dev + prod 2026-08-02** (8 pairs). |
| 055 | `055_forecast_income_growth_and_steps.sql` | **CR064 P6** — income a **business** can express. Recurring income had two mutually exclusive modes: `income_amount` compounded at **exactly** inflation, or `avg(market value) × (inflation + spread)` if any `IncomePct` row existed — and the yield wins on a **single row**, discarding the typed amount entirely. CR003 built `IncomePct` as a deposit interest rate, which is right for Fidelity Fixed Income and wrong for a company, whose profit is not a percentage of its own valuation. **Measured on prod 2026-08-02: all six income-bearing modules are in yield mode, so all six have a dead Income Amount** — United Beverages shows 192,266 PLN typed while the engine books 77,163 USD for 2027 = `avg(3,846,154 / 3,870,192) × (2.5% − 0.5%)`, matching to the dollar. Adds `forecast_modules.income_growth_rate` (a multiplier of inflation, read exactly like the existing `growth_rate` for value: 1 = inflation, 0 = flat nominal, 0.5 = half, negative = a business in decline; **NULL = 1 = the old behaviour**) and `forecast_module_income_steps` (permanent level changes — "2029: +10,000", "2033: −25,000"; `amount` signed, one row per year, same key and cascade as `forecast_module_income_pct`). **Owner's decision 2026-08-02:** a step is typed in the money of the year it happens and **keeps its real value** afterwards, compounding from its own year at the stream's growth rate rather than eroding over 36 years; it applies in **full** in its year, deliberately **not** taking the July-1 half-year convention, because a step is a change to the annual run-rate rather than an event with a date. Steps are **stored** and the series **derived** every generate — materialising 36 rows of computed income would rot the moment the growth rate changed (CR049/CR050, and why CR062 derives a loan's amortization). **Dormant, and proved so on real data:** a copy of prod with all pending migrations applied, regenerated with the pre-change engine and with the new one, gives **7,916 entries across all five scenarios byte-identical**. The feature was then proved on the same copy — Barkeria moved off yield mode at `income_growth_rate = 0.5` with a `2029: +10,000` step regenerates to **55,687.52 → 56,383.59 → 67,088.39 → 67,927.00 PLN**, matching the hand-computed example to the cent. Every hand-maintained list that has silently dropped a column before was updated with it: `copyScenario`'s INSERT, `MODULE_COLUMN_DEFAULTS`, the CR050 variant `SCHEDULE_TABLES`, `replaceModuleSchedules`, the route write contract, and `previewLoanRetype` (a loan has no income, so its steps go with the retype). Idempotent (`ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS`); dormancy is **reported** by the post-condition rather than asserted, because raising on a non-zero count would make a re-run fail once the feature was legitimately used. **Numbered 055, not 053** — another agent thread committed `053`/`054` mid-build. **Pending on dev and prod.** Applied **prod 2026-08-02** (by the deploy runner, Step 2b) and **dev 2026-08-02** — dev was applied *after* prod and only because the shared test gate had gone red against it (50 failures on `column "income_growth_rate" does not exist`). See [roadmap Known Issue #15](project-roadmap.md#3-known-issues). |
| 056 | `056_forecast_module_currency_repair.sql` | **CR064 P13** — five `PLN Credit Cards` modules (one per scenario, the same row copied by CR050's variant materialization) carried `currency = 'USD'` over values that were **PLN**: `market_value −24,542.66` against `market_value_usd −6,832.01`, an implied rate of 3.5923. Not inferred — account 65's own ledger (`opening_balance + SUM(amount)` over its four PKO children, every one of them PLN) stood at **−24,129.55 PLN on 2025-12-31**, the modules' `base_date`, within 413 PLN of the stored figure where a USD reading is off by 3.6×; and `exchange_rates` gives PLN→USD 0.278373 that day = **3.5922**, matching the implied rate to four figures. The label came from account 65 itself — a **parent rollup holding no transactions of its own**, mislabelled `USD` in `accounts` — and the module inherited it. **Why it was invisible:** `fcbuilder-module.js` consults the FX assumptions only when `Currency !== 'USD'`, so the branch never ran, `fxrates` kept its `fill(1)`, and `marketValues[i] / 1` posted the PLN amount straight onto a USD balance sheet. The `MarketValueUSD` override repairs **index 0 only** — the `base_date` year, 2025, which is not even an output column when `PeriodStart` is 2027 — so the one correct year was never displayed and all 36 wrong ones were. **What it cost:** `2026 Base` and `2026 Downside` (the two scenarios where the module was `setup_status='complete'`) posted **−24,542.66 USD in every forecast year** against a correct −6,293 at the scenario's PLN 3.9 — verified in `forecast_entries` before the repair (2026…2030 all −24,542.66). That is **18,250 USD of liability that does not exist**, carried in Net Assets, in the Compare deltas, and in the cash sweep's view of the balance sheet. **The change** relabels those rows `PLN` and touches **no value**: the engine now divides `market_value` by the FX series as it does for every other PLN module. **The rows are named, not derived** — 3.5923 is a plausible PLN rate, so a generic "infer the currency from the ratio" would work on these five and silently invent a currency the next time two columns disagree for another reason; the companion engine guard **throws** on that state rather than healing it, for the same reason. Post-condition asserts the **invariant** the guard enforces (no module may claim USD while its local and USD columns disagree by more than a cent), **not** a count of rows changed — a count-based check aborts the chain on a data-free database, the defect shipped in 046, 050 and 052; an empty `forecast_modules` satisfies it trivially. **Ordering matters:** the guard refuses to compute a module in the pre-repair state, so this must be applied **before** the code that carries it — the order `deploy-to-production.sh` already uses. Verified: full 56-file chain + `ci-seed.sql` on an empty Postgres, then **779 backend tests green**; idempotent (re-run gives `UPDATE 0`). Applied **dev + prod 2026-08-03**, prod ahead of the code deploy. |
| 057 | `057_forecast_streams_schema.sql` | **CR069 P1 — the stream schema, INERT.** `forecast_streams` (module_id, direction, fc_line_id, mode `amount`/`yield`/`pct_of_value`/`derived`, `amount` magnitude + `amount_usd` CR051 twin, `growth_mult`, CR046 window, CR047 tax override) · `forecast_stream_changes` (**signed** amount at scale 4, four flags: `Percent %`/`Fixed $`/`One-Off $`/`Spread %`) · `forecast_modules.has_valuation` (NOT NULL DEFAULT TRUE — all 110 existing modules are balance-sheet modules; FALSE arrives in P2 with the converted Expenditure items). **Zero readers, zero writers** — grep-verified across `server/` and `frontend/src`; a forecast generated after this migration is byte-identical to one generated before, because the engine cannot see any of it. Shipping the schema alone is what leaves P2 carrying only code and a backfill. **The backfill is deliberately NOT here:** nothing dual-writes these tables, so a backfill landing at P1 goes stale on the first module edit, `syncVariant`, auto-adjust apply or `copyScenario`, and P2 would cut over to data that stopped being true days earlier — wrong numbers, no error. It rides P2's own deploy, atomically with the cutover. **Four review findings fixed before it touched any database.** (1) The CR drafted a plain `UNIQUE (module_id, direction, fc_line_id)`, which constrains **nothing** when `fc_line_id` is NULL — NULLs are distinct in a Postgres unique index — and 15 inc/exp items plus the five line-less `Sarasota House` modules are exactly the population that would have exercised it; split into two partial indexes, both probed with real inserts. *That is the identical trap CR069 P0 had just finished paying for on `forecast_entries`.* (2) A third partial index enforces Decision 3's second rule (**at most one `yield` stream per module**, which the convergence loop assumes) — the CR left it to the route, and `copyScenario`/`syncVariant`/auto-adjust all write modules **without** passing through the route. (3) The magnitude CHECK covers **`amount_usd` as well as `amount`**: the two source populations use opposite sign conventions (`forecast_income_expense` stores an expense negative on 33 of 48 rows, `forecast_modules.expense_amount` positive on 30 of 30), so a one-sided rule would have let a negative twin sit beside a positive magnitude, unchecked, on every one of those rows. (4) The `pg_constraint` post-condition is **schema-qualified** like its three siblings — `conname` is unique per table, not per database, so the unqualified form reads 4 and aborts the moment the chain reaches a second schema; CR027 is schema-per-tenant and on `main`. Verified by applying the file into a second schema, which now passes and previously would not. **The most valuable catch is a comment, not a constraint:** the file's own backfill instruction said "negate expense-direction rows", which is right for the money flags and **wrong for `Percent %`** — a rate whose sign is a direction of *change*, not of money. `Children` carries **−100% in 2032** ("the kids leave, this expense goes to zero"); negated it reads +100% and the expense **doubles**. That is **58 of 113 change rows**, and neither CR069 §6.4's assertion (`|amount|` preserved *and* amount × direction-sign preserved — a negated Percent % satisfies both) nor the per-(account, year) sum gate would have caught it. The rule is now keyed on the **flag**: money flags negate, rate flags carry through untouched. Post-conditions are **structural only** — object existence, all four uniqueness indexes, both CHECKs — every one true of an empty database, because a row-count assertion here is the defect shipped in 046, 050 and 052 ([Known Issue #12](project-roadmap.md#3-known-issues), three incidents). Also records three obligations it hands to P2, in the append-only file P2's author will actually read: `forecast_stream_changes` is the schema's first **grandchild** and CR050 variant sync cannot express one (`replaceSchedule` deletes by a single fk one level down; `forecast_streams` has neither `scenario_id` nor `origin_base_id`, so `syncEntity` is not available either) — the largest unbudgeted item in P2; `copyScenario`'s hand-maintained column list omits `has_valuation` and `DEFAULT TRUE` **masks** it, so a copied flow module would silently return as a balance-sheet module and CR041's gate would zero all its streams (P2 must add the column **and** `DROP DEFAULT`); and `fc_line_id` is `ON DELETE RESTRICT` where the module's line columns are `SET NULL`, a deliberate but real behaviour change the fc-line delete path must report as a sentence rather than a 23503. Verified: full **57-file chain on an empty Postgres + `ci-seed.sql`**, the `DO` block passing and emitting its NOTICE; **idempotent** (re-run twice, no change); `ADD COLUMN … NOT NULL DEFAULT TRUE` is a PG11+ **fast default** (`atthasmissing`), so no table rewrite on the 110 rows; and every constraint probed with real inserts — negative `amount_usd` rejected, expense-side tax rejected, second line-less stream rejected, second yield stream rejected, duplicate `(stream, date, flag)` rejected, same date with a different flag accepted, `Spread %` at 4dp preserved losslessly. **Applied dev 2026-08-04, then prod 2026-08-04** — dev first, deliberately (Known Issue #15 is migrations reaching prod ahead of dev). **⚠️ Applied by hand (`psql -f`), which does NOT write `schema_migrations`** — so prod's ledger still tops out at 056 while the schema carries 057's objects. Found by the pre-deploy migration review, not by the deploy. Consequence: the next `migrate.js` run re-applies 057, which is safe (idempotent throughout, and the end state was verified hash-identical on a prod copy) but will print `✓ applied 057…` in the deploy log, where it must not be read as a fault. *The teeth are in the other branch:* had `schema_migrations` been absent entirely, `planMigrations` returns **`mode: 'baseline'`** and would record 058/059 as applied **without running them** — the cutover code would then meet an empty `forecast_streams` and produce silently wrong numbers. Verified before the deploy with `node server/db/migrate.js --dry-run`, which reports **`would APPLY`** 057, 058, 059. *Lesson: apply through the runner, not `psql -f`.* **Inertness proved empirically, not argued:** a fresh copy of prod regenerated all five scenarios *before* the migration and again *after* it — 4,030 sum rows and 158 module labels, **byte-identical on both**. Prod itself is untouched (8,383 entries, 34 labels, unchanged), and `has_valuation` reads TRUE on all 110 prod / 97 dev modules with zero flow modules, which is the correct pre-P2 state. |
| 058 | `058_forecast_streams_backfill.sql` | **CR069 P2 — every module column-pair and every Expenditure item becomes a stream.** 60 `forecast_income_expense` rows → modules with `has_valuation = FALSE` and one amount stream; 115 change rows; 25 `income_pct` rows → `Spread %`; the CR064 P6 steps → `Fixed $`; 48 `origin_base_id` links and 3 `incexp` overrides rewritten. **Ships with the engine cutover in ONE deploy** — nothing dual-writes `forecast_streams`, so a backfill landing earlier goes stale on the first module save, `syncVariant`, auto-adjust apply or `copyScenario`, and the engine would cut over to data that stopped being true. **The sign rule is the whole risk, and it keys on the FLAG, not the direction:** `Fixed $`/`One-Off $` are money and are negated for an expense stream; `Percent %`/`Spread %` are RATES whose sign means a direction of *change*, and carry through untouched. Both halves were got wrong once in review and each would have been silent — taking magnitudes would invert the 20 positive `Fixed $` rows that are deliberate REDUCTIONS (`Children` +25,000 when the kids leave), and negating the rate rows would turn `Children`'s **−100% in 2032** ("this expense ends") into +100% and **double** it, across 58 of 113 rows, satisfying both CR069 §6.4's assertion and the sums gate. Post-conditions assert rate rows are byte-identical to source and money rows preserved magnitude × direction. **Two override classes translated, not just re-pointed:** the 3 `incexp` patches name `base_value`, meaningless on a module whose money lives on a stream (Downside would have reverted to the base's Living Expenses/Travel/Purchases); and one MODULE patch names `income_amount` (Upside's United Beverages, 750,000 — the point of that scenario), a column the engine stops reading, which would have applied cleanly to nothing for 36 years. Both become `streams` schedule patches. **Gate: per-(scenario, account, year) `forecast_entries` sums identical TO THE CENT** — 4,030 rows, 0 differing, all five scenarios, on a prod copy; and 3,798 rows, 0 differing, on dev. **Re-run behaviour, measured rather than claimed:** the `cr069_skip` guard (a populated `forecast_streams` ⇒ skip) covers steps 1, 3a and 3b but NOT 3c/3d, so a second apply reaches the `Spread %`/`Fixed $` inserts and is **refused by `idx_fc_stream_changes_unique`** — the whole transaction aborts and the data is left byte-identical (verified on a prod copy: 145 streams / 145 changes / 60 flow modules before and after the failed re-run). So it is *safe* to re-run but **not a silent no-op**: it errors. That is the migration runner's ledger doing its job in practice (`deploy-to-production.sh` never re-applies an applied file), and the unique index 057 added catching the case the guard misses. Recorded because "idempotent" would have been the wrong word. Applied **dev 2026-08-04, prod 2026-08-04** (v3.14.0, through `migrate.js` — recorded in `schema_migrations`). |
| 059 | `059_forecast_stream_schedule_overrides.sql` | **CR069 P2 follow-up — the override keys 058 missed, found by dev.** 058 translated patches naming the eleven income_*/expense_* COLUMNS; it did not translate the two SCHEDULE keys that moved in the same change — `income_pct` and `income_steps`, now `Spread %` and `Fixed $` rows inside a stream. **Prod carries no such override, so the prod-copy gate passed clean; dev does** — `2026 Upside` overrides United Beverages' yield with `{"income_pct":[{"value":1,…}]}` — and applying 058 there moved **504 rows**: the override was dropped, UB reverted to the base's spread, and the sweep and convergence loop carried it across every year. That is the entire argument for dev-before-prod ([Known Issue #15](project-roadmap.md#3-known-issues)) and the only reason it was caught before prod. A forward migration rather than an edit, because 058 was already applied on dev (append-only). Starts from the patch's existing `streams` key when 058 already wrote one, so an override carrying both a column and a schedule key keeps both. **Two code defects fixed alongside it, and they are the durable half:** `syncEntity` SILENTLY DROPPED any patch key that was neither a schedule key nor a column (`if (cols.includes(key))`) — an override naming a moved field applied cleanly to nothing, which is how this was invisible — and now throws; and `scheduleEqualsBase` compared only the PARENT columns of a two-level schedule, so a stream override differing **only** in its change rows read as equal to base and was pruned on the next sync (it destroyed the Upside override twice before the cause was found). Both now covered by a regression test asserting an override that changes nothing but a schedule survives a sync and reverts on clear. Post-condition: no override may name any retired field. **Genuinely idempotent** — a second run finds no override naming a retired key and reports "no schedule overrides to translate" (verified twice on a prod copy). Applied **dev 2026-08-04, prod 2026-08-04** (v3.14.0, through `migrate.js` — recorded in `schema_migrations`). |
| 060 | `060_forecast_drop_retired_incexp.sql` | **CR069 P3 — the contract step.** Drops `forecast_income_expense`, `forecast_incexp_changes`, `forecast_module_income_pct` and `forecast_module_income_steps`; the eleven `income_*`/`expense_*` columns on `forecast_modules`; the long-dead `expense_pct` (migration 008 removed it from the write path and the loader hard-coded `ExpensePct = 0`, so the branch reading it was unreachable in production for its whole life — CR069 P2 deleted the branch, this removes the column); and finally `has_valuation`'s DEFAULT. **Every one of these stopped being read two deploys earlier** (v3.14.0 / migration 058 cut the engine, `getBaseYearValues`, `copyScenario`, auto-adjust, AI review and the fc-line guard over together, and the four `/incomeexpense` routes have answered 410 since); P3 removed the legacy field names from the write contract, so nothing can even send them. **The DROP DEFAULT is migration 057's own stated obligation (b)**, deliberately deferred in P2 with its reasoning recorded — nine INSERT sites omitted the column, and the specific hazard 057 named (a scenario copy silently defaulting a flow module back into a balance-sheet module, where CR041's gate would zero all its streams) was already closed by deriving `copyScenario`'s column list from `information_schema`. What it adds is that the NEXT hand-written insert fails loud instead of quietly picking TRUE; every path now names it. **The pre-condition is the point of the file:** it REFUSES to drop anything if there are `forecast_income_expense` rows but fewer flow modules — i.e. if the P2 backfill never ran or rolled back — because destroying the source of data that has no replacement is the one outcome a contract step must never produce. Vacuous at 0 = 0 on a data-free database (Known Issue #12's class). Post-conditions assert the four tables and eleven columns are gone, the DEFAULT is gone, **and that the stream tables still exist** — dropping the old model while the new one is missing is the other outcome it must never produce. **Gate: per-(scenario, account, year) `forecast_entries` sums identical TO THE CENT on a prod copy WITH the tables dropped — 4,030 rows, 0 differing.** Irreversible; `deploy-to-production.sh` takes a `pg_dump` first, which is the restore path. Applied **prod 2026-08-04** (v3.14.1, by `deploy-to-production.sh` at Step 2b, through `migrate.js` — recorded in `schema_migrations`), then **dev 2026-08-04**. **Prod-before-dev again — [Known Issue #15](project-roadmap.md#3-known-issues), and this time it is structural rather than an oversight:** the deploy script applies migrations as part of the deploy, so any file riding a release reaches prod first by construction unless it is applied to dev by hand beforehand. Applying it to dev afterwards then hit a second thing worth recording: **dev's ledger was stale**, because 058 and 059 had been applied there by hand (`psql -f`) during P2 development, exactly as 057's row warns. The runner therefore re-ran 058, which was **refused by `idx_fc_stream_changes_unique`** and rolled the whole run back — the re-run behaviour 058's own row documents, doing its job. Dev's forecast entries were verified byte-identical after the abort. The ledger was then reconciled by recording 058/059 as `baselined = true` with the runner's own checksums (not `--baseline`, which would have marked **060** applied without running it, leaving the dropped-table state unreached and unrecorded), after which the runner reported one pending file and applied it. Post-conditions passed on both: 0 retired tables, 0 retired columns. **Dev's sums did move — 3,798 → 3,925 rows — and it is NOT this migration:** module 80 (`CVC Fund VIII`) was edited on dev at 19:41 the same day while testing P3's stream cards, leaving stream 430 at `amount 0.00` with no FC line, which zeroed that module's income, dropped its label from the entries and pushed the late years into `Cash Shortfall`. Dev's baseline was *stored* entries predating that edit, so it was never a valid before/after pair; two consecutive regenerates under the current code are identical (3,925 = 3,925), so dev is self-consistent. The valid gate is prod's, whose baseline was regenerated under deployed code minutes earlier: **4,030 rows, 0 differing**, all five scenarios, after the tables were dropped — matching the pre-deploy rehearsal on a prod copy. **SUPERSEDED 2026-08-05 — the dev half of this row no longer describes dev.** Dev was refreshed from the v3.14.1 prod backup later the same day, which replaced the reconciled ledger described above with prod's own rows: 058/059 now read `baselined = false` at prod's timestamps, and 060 was re-applied to dev through the runner at 2026-08-05 00:21 UTC. Dev now regenerates **4,030 sum rows identical to prod's**, so the 3,925 figure and the module-80 divergence below are the record of a state that no longer exists — kept because the reasoning (a stored baseline is not a regenerated one) is what made the prod gate the trustworthy half. Note also that the backup a deploy takes at Step 1 PREDATES the migration it applies at Step 2b, so restoring from one lands a migration short: this restore brought back the four tables 060 had just dropped, and the runner re-dropped them. |
| 061 | `061_forecast_warning_dismissals.sql` | **CR074 — dismissing a cash-health warning.** New table `forecast_warning_dismissals` (`scenario_id` → `forecast_scenarios` ON DELETE CASCADE, `warning_id`, `fingerprint`, `dismissed_at`), UNIQUE (scenario_id, warning_id) + an index on scenario_id. Scoped per scenario because warning ids embed a module NAME and the same module carries different numbers in each. `fingerprint` is what stops a dismissal hiding a WORSE version of the same problem — a hash of the warning's substance, checked on render, so "drained by 2061" accepted today re-appears when the plan makes it 2041. Additive and inert until the owner clicks: **0 rows on first apply.** | dev ✅ 2026-08-06 · prod ✅ 2026-08-06 |
| 062 | `062_disposal_selling_costs.sql` | **CR078 — a selling cost on a disposal.** Adds `forecast_module_disposals.disposal_cost_pct numeric(6,4)`, **nullable with NO default**, plus a CHECK keeping it in `[0, 100)`. NULL means *no cost modelled* — deliberately **not** the same as a typed 0 (*considered, and free*); a `DEFAULT 0` would read identically to the engine but would assert that the owner had considered and chosen zero for 20 existing disposals, which is untrue, and CR077's advisory rule needs the distinction to know what to ask about. **Per ROW because the live data demands it:** a property sale carries 3–6% and a business sale 1–3%, while `CVC Fund VIII`/`IX`'s **2,033,048 of capital returns carry nothing at all** — they are distributions modelled as disposals, and any coarser rate would silently charge a selling cost on them. Scale 4 so `5.7500` round-trips losslessly, matching `forecast_stream_changes.amount`. **Post-conditions are STRUCTURAL only** (column exists, constraint exists, schema-qualified like 057's siblings since `conname` is unique per TABLE and CR027 is schema-per-tenant) — no row-count assertion, which is the defect shipped in 046/050/052 and vacuous on an empty database. **Inert on apply:** 0 rows carry a value and nothing read the column until CR078's engine shipped in the same release; proved rather than asserted — all five scenarios regenerated **byte-identical** before and after. Probed with real writes: `−1` rejected by the CHECK, `100` rejected by the numeric precision, `5.7500` preserved exactly. | dev ✅ 2026-08-09 · prod ✅ 2026-08-09 (v3.24.0, `deploy-to-production.sh` Step 2b through `migrate.js` — the dev-first guard confirmed it had already run on dev, closing Known Issue #15's shape). Verified on prod after apply: column 1, constraint 1, **0 rows carry a cost**, ledger row present, and the entries fingerprint **unchanged** — dormant as designed. |
| 063 | `063_bank_feed_api_id_crosswalk.sql` | **CR059 P3a — the account crosswalk, done right.** Replaces **044** (applied early, incomplete, reversed by **051**). Rewrites **all three** fin columns that key a feed account — `account_source_mappings.external_name` (31), `bankfeed_staging.feed_account_external_id` (2,515) and `bankfeed_balances.feed_account_external_id` (1,962) — where 044 rewrote two and took the reconcile page down. The column list is now **DERIVED from `information_schema`** rather than reasoned about and reviewed, which is how the missing one was found; the two adjacent columns that query also returns were ruled out with evidence (`security_source_mappings` is empty and matches nothing; no `bank_connections.external_id` collides with an account id). Generated from a verified plan by bank-feed `scripts/emit-crosswalk-sql.js`, whose pairs come from **two independently computed signals** that must agree — disagreement produces a refusal, not a mapping — and which refuses a plan that is not a bijection. The plan was byte-identical hourly for a week through Revolut's id churn and is committed as `scripts/crosswalk-plan.verified.json`. **Carries the post-condition 044 lacked:** every live mapping must resolve to a feed balance — asserting what the owner actually sees rather than counting rows changed. **Falsified:** stripping only the balances UPDATE (reproducing 044) aborts with "1962 balance row(s) still carry a Sheet id" and rolls back untouched. Rehearsed on fresh copies of live prod and the live feed store before applying; idempotent. Applied **prod 2026-08-10** in the cutover window, paired with bank-feed **006** (`feed_accounts.external_id`) and **005** (`sync_state`). Afterwards: 0 no-feed mappings, 0 unmapped staging, 0 duplicate ledger ids, all balances same-day. |
| 064 | `064_account_rollup_currency_labels.sql` | **Roadmap Known Issue #19** — parent rollup accounts labelled `USD` over non-USD children. **Measured 2026-08-10:** nine parents carry `currency='USD'` while every child beneath them is EUR, PLN or GBP — `EUR Bank Accounts` (5 children), `PLN Bank Accounts` (7), `Other Bank Accounts` (1, **GBP**), `CVC Investments` (2), `SP - Properties` (3), `PL Investments` (4), `PL - Properties` (2), `PLN Credit Cards` (4) and `Tax Liabilities` (PLN+USD). Every one holds **zero transactions of its own** — pure aggregation nodes — so no balance moves: `reports.js` converts each account by its own currency, and converting a zero balance at any rate is still zero. **Why it is worth a migration:** `forecast_modules.currency` is SEEDED FROM THE LINKED ACCOUNT, and `fcbuilder-module.js` reads the FX assumptions only when `Currency !== 'USD'` — so a module that inherits a wrong USD label keeps `fxrates`' `fill(1)` and posts its LOCAL amount onto a USD balance sheet for every year. Not hypothetical: that is exactly what account 65 produced, **18,250 USD of liability that did not exist** in `2026 Base` and `2026 Downside` until [056](#). **EIGHT rows are relabelled, not seven** — 056 fixed the five MODULES on account 65 and left the ACCOUNT saying USD, so the source of that defect was still in place and the next module linked to it would have inherited the same label again. **DERIVED, where 056 named its rows explicitly:** 056 had two numeric columns whose ratio merely *looked* like a PLN rate, so inferring from it would have invented a currency; here the children's currency is a stored fact, making the predicate safe to state generally — *relabel a transaction-free parent whose children are UNANIMOUS and non-USD, to that currency*. A **mixed** parent is deliberately left alone: `Tax Liabilities` (215) aggregates a USD and a PLN reserve, has no single correct label, and its module is correctly `USD`. **Audited before running** — every reader of `accounts.currency`: `reports.js` and `getOpeningBankCash` convert each account's own (zero) balance; `refreshModulesFromActuals` keys on `t.account_id` so a rollup never matches; `manualReconciliation.js` EXCLUDES any account with children by design; `restate-mtm.js` is invoked per account and never on a parent. The nine `account_source_mappings` rows on these accounts are all `source='pocketsmith'` — a retired, name-keyed source that does not read currency — with no bank-feed mapping, no budget entry, no manual balance and `opening_balance = 0`. Post-condition asserts the **invariant** (no transaction-free parent may claim USD over unanimous non-USD children), **not** a row count, so an empty `accounts` passes trivially — the 046/050/052 defect. Idempotent (re-run: `UPDATE 0`). Verified: full chain + `ci-seed.sql` on an empty Postgres, then **886 backend tests green**. **Numbered 064, not 063** — another thread committed `063_bank_feed_api_id_crosswalk.sql` mid-build; the stale dev ledger row was deleted and the file re-applied through `migrate.js` under its new number. Applied **dev 2026-08-10** (through the runner); prod at the v3.27.0 deploy, Step 2b. |
| 065 | `065_wise_assets_yield_accrual.sql` | **CR080 §Part A — the yield Wise never posts as a transaction.** `Wise - USD` (8) and `WISE - EUR` (13) are **Wise Assets** balances: the money sits in a money-market fund, not as cash. The feed proves it — it delivers the monthly `ACCRUAL_CHECKOUT … Assets service fee` (−0.53 USD / −0.22 EUR) but never the yield those fees are charged against, so fin drifts below the feed a little every day with no transaction behind it. **Neither existing reconcile mode fixes it:** `calibrate` folds a *recurring* flow into a constant at opening (the failure **046** documents — today right, every prior date wrong, drift back tomorrow), and `mtm` books the right *shape* but to `Unrealized G/L` (88), an **expense** category, so money-market yield never reaches income, budget or anything tax-facing. Both accounts were set to `mtm` and had **never had one `mtm` row written**. Books 6 `source='accrual'` rows to `Interest Income` (74) — USD 10.19 / 12.03 / 3.21, EUR 3.59 / 3.61 / 0.69 — measured as **Δ(feed − fin) between endpoints the feed has settled**: only dates inside a transaction-free run where same-day and lag-1 comparison give the same gap, because this feed syncs in the small hours and its lag jitters by a day (CR065 §11). Three independent intervals per account each imply the same annualised yield (~3.6% USD, ~2.1% EUR) — a missing transaction does not accrue linearly. **⚠️ The restatement half of this row was WRONG — see 069, which moved it.** **Plus 2 `source='restatement'` rows dated 2026-06-04** (−23.83 / −7.50 to `Unrealized G/L`): both gaps *start negative*, i.e. fin was ABOVE the feed before any feed history exists, which unbooked yield cannot cause — a separate, unattributable error. Booking today's gap as one interest row would have hidden it (the USD account's +2.79 is 26.62 of yield **net of** a 23.83 error, understating 2026 interest by ~24). **Not** folded into `opening_balance`: these accounts carry rows to 2022 and that would move four years of history by an amount that demonstrably did not exist for most of it. Also recategorises the `BALANCE-5794451815 -- Converted 34.99 CHF to 37.52 EUR` row (+37.52) from `Interest Income` to `Transfer - FX` (208) — an FX conversion booked as income, inflating the category by **4.75× the entire real 2026 yield** on that account. Idempotent; **asserts the reconciliation invariant** (each account's computed balance must equal the settled feed observation to the cent) and aborts otherwise — which it did on dev, correctly, dev being 100.00 behind prod. Applied **prod 2026-08-11**; dev picks it up at the next `sync-db-prod-to-dev.sh`. |
| 066 | `066_wise_accrual_base_amount.sql` | **CR080 §Part A follow-up — 065's USD rows landed with a NULL `base_amount`.** 065 inlined the USD-conversion rule as a scalar subquery against `exchange_rates`, which holds **no USD→USD row** (a 1:1 identity nobody stores), so the multiply produced NULL for the three `Wise - USD` accrual rows and its restatement row. [`usdBaseAmount`](../../server/src/v2/services/fx.js) special-cases USD as rate 1 for exactly this reason; the inline copy dropped that case — **the argument for CR080 calling the helper rather than re-deriving the rule in SQL, made by the migration that re-derived it.** EUR rows were unaffected (their rates exist). Severity: those four were the **only** USD transactions in the database with a NULL `base_amount` (409 of 413 on these accounts carry one; pocketsmith / quicken-import / mtm / auto-offset are all at 100%), so any USD-base report summing `base_amount` would silently omit them. Forward repair, not an edit — 065 was already applied and migrations are append-only. Written as a **general rule** (any `accrual`/`restatement` row on a USD account with NULL `base_amount` gets `base_amount = amount`) so it is a clean no-op on a fresh DB where 065 produced correct values. Post-condition: zero accrual/restatement rows with a NULL `base_amount`, or abort. Applied **prod 2026-08-11**. |
| 067 | `067_accrual_category.sql` | **CR080 Part B — schema only, INERT.** Adds `account_source_mappings.accrual_category_id INTEGER REFERENCES accounts(id)`, the income category an `accrue`-mode account's yield books to. **No default, by design:** `accrue()` refuses when it is NULL rather than falling back, because falling back to `Unrealized G/L` (88) is precisely the defect CR080 exists to fix. **This migration deliberately does NOT set any account to `'accrue'`, and that separation is the whole point.** Migrations are applied to prod *before* the code that uses them; until CR080's engine shipped, `reconcileToFeed` routed **any** mode that was not `'mtm'` to `calibrate`, which rewrites `opening_balance`. A migration flipping accounts 8 and 13 ahead of the deploy — which the CR's first draft proposed, described as "inert because nothing reads 'accrue' yet" — would have left a Reconcile click re-anchoring both accounts and destroying exactly the history migration 065 had just booked. The flip is **068**, applied after the deploy; the same deploy makes an unrecognised mode **throw** instead of falling through, so the trap cannot be re-set by a future mode. Applied **dev + prod 2026-08-11**. |
| 068 | *(withdrawn — number retired, never applied)* | **CR080 Part B — written, then deleted before it ever ran.** It would have set `Wise - USD` (8) and `WISE - EUR` (13) to `reconcile_mode='accrue'` with `accrual_category_id=74`. Withdrawn because **a migration was the wrong vehicle, and the twenty lines its header spent explaining an ordering exception were the evidence**: it had to be applied AFTER the code deploy, inverting the standing rule, purely because it wrote a *value* the old build would have routed to `calibrate`. But `reconcile_mode` and `accrual_category_id` are per-install **configuration**, the same family as `feed_sign`, `feed_negate_tx` and `promote_from_date` — every one of which is set through the UI and none through a migration. Setting it on the reconcile page after the deploy removes the inverted ordering entirely, avoids `--allow-unverified-migrations` (Step 2b(i) would have refused it, correctly, as never having run on dev), and gets the validation a migration cannot have: the route refuses `'accrue'` without a category, and refuses clearing the category while that mode is selected. **The number is retired, not reused** — the file existed in commit `d911af6` and the next migration is 069. |
| 069 | `069_wise_calibration_plug_not_a_loss.sql` | **CR080 Part A, corrected — the "unexplained difference" was a calibration plug, and 065 filed it as a loss.** 065 booked −23.83 / −7.50 dated 2026-06-04 to `Unrealized G/L` (88), reasoning that fin sat ABOVE the feed at 2026-06-05, that unbooked yield pushes fin BELOW, and that the offset therefore *could not* be yield. **The premise holds only for a ledger nobody has re-anchored.** The owner had been **calibrating these accounts for months** (reported 2026-08-11), and `calibrate()` rewrites `opening_balance` — one constant applied to EVERY historical date — so an August calibration drags June's balance up with it, and fin sitting above the feed in June is exactly what a later calibration looks like. Nothing was logged because `calibrate()` overwrites `opening_balance` **with no audit row at all**, which is why 065 could not see it and reached for "unexplained" instead. The data alone says the same: if a calibration happened on date T the gap must be a straight line through zero at T, and both accounts cross zero in the **first days of August** (8: −23.83 → 0.00 on 08-01/02 → +2.79; 13: −7.50 → ≈0 on 08-03/04 → +0.39). Deletes both rows and reduces `opening_balance` by the same amounts — reversing 065's own argument against `opening_balance` ("would move four years of history by an amount that did not exist"), because **the plug is already in there**: this moves 23.83 / 7.50 OUT of the existing smear and onto the dated rows 065 had already booked correctly. **Balance-neutral on every date** (the deleted rows predate every anchor); this is a P&L correction only — `Unrealized G/L` loses a fabricated **−32.56** and `Interest Income` is untouched at 25.43 USD / 7.89 EUR. **Post-condition is the proof: all EIGHT measured anchors across both accounts tie to the feed to the cent** (06-05, 06-30, 07-30/31 and 08-07/09), not merely today — a wrong split ties at the plug's own date and drifts everywhere else. Guarded on the exact expected plug amount, so a different ledger aborts rather than moving an unknown number into `opening_balance`; idempotent (no row ⇒ no adjustment). **Still not recoverable:** yield earned before 2026-06-05, the feed's first observation — it stays lumped in `opening_balance` and would need Wise's own statements. Applied **prod 2026-08-11**. |
| 070 | `070_tax_fbar_schema.sql` | **CR082 P1 — the Taxes section's schema, INERT.** Four tables for FinCEN Form 114: `tax_foreign_accounts` (the designation — institution block, account number, Part II/III/IV, tri-state `review_state`), `tax_fx_rates` (Treasury Dec-31 rate per year per currency), `tax_fbar_filings` + `tax_fbar_filing_lines` (the **frozen** snapshot). **Zero readers, zero writers** until the P1 routes ship. **Additive DDL only — no data is written**, deliberately: the FX prefill is an app action, because a `SELECT ... FROM exchange_rates` inside a migration is data-dependent, inserts nothing on a fresh database, and is configuration in a migration — the reasoning that withdrew **068**. **Three choices that look wrong and are not.** (1) `UNIQUE (account_id)` over a NULLABLE column is the **opposite** of **057**'s case: NULLs are distinct in Postgres, which is exactly what permits MANY report-only lines (a reportable account with no fin ledger behind it — a company account under signature authority) while allowing at most ONE designation per real account; 057 added a partial index because *there* the NULL branch needed constraining, and "fixing" this one the same way would forbid a second report-only line. (2) `tax_fbar_filings` is keyed `(tax_year, amendment_seq)` and **not** `tax_year UNIQUE` — an amended FBAR is a second filing for the same year. (3) `tax_fbar_filing_lines` **COPIES** label/account number/institution rather than joining: `calibrate()` rewrites `opening_balance` across **every** historical date through a bare UPDATE that writes **no audit row** (069 is the case study; the only `audit_log` writer in the repo is `aiReview.js`), so a figure filed with FinCEN must not be recomputable. `rate_to_usd` is USD per one unit of foreign currency — **Treasury publishes the reciprocal**, and EUR/GBP are plausible in either direction (1.175005 vs ~0.85), so the column comment carries the direction and the UI must gate on the prefill. Pseudo-enums are **VARCHAR + CHECK**, not Postgres enums (`accounts.account_type` is a real enum and altering one is the migration this repo least wants to repeat). Post-conditions are **structural only** — 4 tables + 3 named constraints, **schema-qualified** because `conname` is unique per table and CR027 is schema-per-tenant on `main` (057's lesson) — since a row-count assertion is vacuous on a data-free database (Known Issue #12). **Verified on the full 70-file chain against an empty Postgres + `ci-seed.sql`** (`Scripts/test-fresh-db.sh`), `DO` block NOTICE emitted, and **idempotent by measurement**: applied twice, still 4 tables / 21 columns. Applied **dev and prod 2026-08-15** (backup taken first; `schema_migrations` checksum is **md5**, not sha256 — the registry insert fails on a NOT NULL `checksum` if you forget). No longer inert: the P1/P2 routes, both pages and the report assembler all read these tables. |
| 071 | `071_tax_foreign_account_year_states.sql` | **CR082 §7 — year-scope the FBAR designation, which the P1 build did not.** `tax_foreign_accounts.review_state` was ONE value for all time, so excluding `PKO TFI` and `Revolut-PLN` (both opened in **2026**, both sitting on the TY2025 report carrying a carry-in figure — see the `carry_in_only` flag and §12b.14) removed them from **every** year including TY2026, where they belong. The stopgap was a capitalised note in `notes` reading "set back to reportable for TY2026" — a correctness control that depends on somebody reading a free-text field a year later. Adds `tax_foreign_account_year_states (tax_foreign_account_id, tax_year, review_state, note)`, UNIQUE on `(designation, year)`, resolved as **`COALESCE(override, standing)`**. **Three shapes were available and two are worse.** Replacing the column with per-year rows for every designation needs a copy-forward rule and opens every year `unreviewed` for all 36 rows — turning the review back into a re-read, which is the exact thing §7's tri-state exists to prevent. `reportable_from_year`/`_to_year` bounds model only a contiguous open/closed life, cannot express "excluded this year, no position on next", and would make a year outside the range `excluded` **by arithmetic rather than by a recorded decision** — losing the unreviewed/excluded distinction [CR066](../cr/cr-066-fc-line-mapping-completeness.md) exists one floor down to fix. The sparse override leaves the 36 existing decisions in place as the default and costs one row per genuine exception (**two**, today). The standing column is deliberately **not** deprecated: for the 34 designations whose answer does not change, "reportable, every year" is the truth. The unique key is not decoration — a second row for the same (designation, year) would let the report pick a winner by arbitrary tie-break, which is the §12.1 shape this CR has already been bitten by twice (two override rows on a filing line; the seeder's one-candidate rule). **Additive DDL only, no data written** — the two live exceptions are the owner's tax position, entered through the UI, the reasoning that withdrew **068**. Post-conditions **structural only** (table + the named unique constraint), **schema-qualified** because `conname` is unique per table and CR027 is schema-per-tenant on `main` (057's lesson), since a row-count assertion is vacuous on a data-free database (Known Issue #12). Applied **dev 2026-08-16**, then **prod 2026-08-16** — both through the runner (`migrate.js`, recorded in `schema_migrations`), the prod half by `deploy-to-production.sh` Step 2b with a `pg_dump` taken first and Step 2b(i) confirming the file had already run on dev. Verified after: `to_regclass('tax_foreign_account_year_states')` non-null and the row present in prod's ledger. |
| 072 | `072_budget_le_schema.sql` | **CR083 P0b — the budget Latest Estimate, INERT.** Two tables: `budget_le` (one LE — a full calendar year, actual months to a cut plus estimate months for the rest, named `LE-MM-YY`) and `budget_le_lines` (one row per LE × category × month × **currency**). **Zero readers, zero writers** until the P0b routes ship. **No data written, and no column added to `accounts`** — an earlier draft proposed `accounts.exclude_from_le DEFAULT false`, which with no writer and no screen would have meant the LE included `Unrealized G/L` (**+213,595** YTD 2026) on day one and landed the year at **+44,259** instead of **−102,999**; the exclusion instead reuses the existing `accounts.is_transfer` (TRUE on exactly the 13 descendants of the `Transfers` subtree, verified — 13 in the subtree, 13 flagged, **zero** flagged outside it) plus `Unrealized G/L` resolved **by name**, which is Known Issue #21 (a suite borrowed an id that is 74 on dev and 11 on CI). Configuration in a migration is what withdrew **068**. **Why its own tables and not another `budget_versions` row:** `POST /budget/versions/:id/copy` accepts an arbitrary `budget_year` **including the same one**, unguarded (`routes/budget.js:57-69` → `repositories/budget.js:486`), while **eleven live functions plus `v_budget_vs_actual`** read `budget_entries` filtered on `budget_year` with no `version_id` predicate — including `services/forecast/crud.js:494`, the CR075 forecast base year, which `index.js:579` folds into `startingCash` and the sweep then rides for all 36 years. **Four choices that look wrong and are not.** (1) **`currency` is part of the grain**: 38 budget and 81 actual category-months hold more than one currency in 2026, and `Bank Fees` holds **three** (EUR/PLN/USD) in both 2026-06 and **2026-09** — the latter inside an August cut’s *estimate* window — so one `amount`/`fx_rate` per category-month would be right for one slice and silently wrong for the rest (the CR037 class). (2) **Both line uniques are PARTIAL**, the opposite of **070**’s case: `category_id` is nullable, and since Postgres treats every NULL as distinct a plain unique would enforce nothing on the other branch; because exactly one dimension is non-null the two partials give total, disjoint coverage. (3) **`account_id` ships unused** so that revisiting the 72 uncategorised budget rows (−86,796/yr, answered "plug, excluded" on the evidence that all 72 date from the 2026-01-29 import with none added since while 131 itemised rows were added on top) is a classifier branch rather than a migration. (4) **`method` lists `TRAIL_3`**, a P1 value with no P0b writer — listing it costs nothing, omitting it would spend a whole migration widening a CHECK. **Review changed four things before it was committed.** The only uniqueness had been on **`name`** — keying on the label, which §6 says never to do — and it was demonstrably porous: `'le-08-26'`, `'LE-08-26 '` and a name whose `actual_through` disagreed with it were all accepted, leaving **two live LEs on one cut**; `budget_le_year_cut_uniq (budget_year, actual_through)` now carries the rule and the name index is kept only for the human label. `superseded_by` gained **`ON DELETE SET NULL`** — deleting the newest LE of a recut chain raised a raw FK error the picker’s Delete would have surfaced as a 500. A `CHECK` now keeps `status` and `superseded_by` from contradicting each other. And `budget_le_month_end_chk` was binding the **timestamptz** (STABLE) overload of `date_trunc`, which Postgres accepts inside a CHECK without complaint and which then cannot be reused in an index — hygiene, not a live bug (across 216 month-ends × 10 timezones including the midnight-DST cases the two forms never disagree). **Three invariants the schema cannot express**, named in the file and owned by the routes: a line’s `period_month` must fall in its LE’s `budget_year` (cross-table), a finalised LE is immutable, and a finalised LE’s actual rows must carry their snapshot — this repo has exactly one non-internal trigger, so a trigger would be against convention. **Verified on the full 72-file chain against an empty Postgres + `ci-seed.sql`**, and idempotent; every constraint was tested for what it *rejects*, not just what it accepts — `2026-12-31` (MM=13), `2025-12-31` (a January LE), `2026-08-15` (not a month end), `2024-02-29` vs `2024-02-28`, duplicate grain, both-or-neither dimension, bad `source`/`method`, and a snapshot on a non-actual row. Applied **dev 2026-08-17** through the runner (`migrate.js`, recorded in `schema_migrations`), then **prod 2026-08-18** by `deploy-to-production.sh` Step 2b with a `pg_dump` taken first. |
| 074 | `074_accounts_opening_balance_audit.sql` | **CR087 P0a — a re-anchor stops being invisible.** Adds `fn_audit_account_opening_balance()` + `trg_audit_account_opening_balance`, an **AFTER UPDATE OF `opening_balance`, `opening_balance_date` ON accounts** trigger writing old / new / **delta** / account name / currency into the existing **`audit_log`**. **Additive and inert:** creates a function, a trigger and one COMMENT; changes no data, blocks no write, alters no existing behaviour. `DROP TRIGGER trg_audit_account_opening_balance ON accounts` reverses it completely. **Why:** `calibrate()` re-anchors `opening_balance = expected − Σtx`, which shifts **every historical date on that account by one constant**, and left no record — measured on prod 2026-08-23 as **20 live `calibrate` mappings re-anchored monthly, 10 of them non-USD**. The cost is on the record: [CR080](../cr/cr-080-feed-accrual-reconcile-mode.md)'s fabricated **−32.56** `Unrealized G/L` loss took **three migrations (065 → 069) and a multi-day reconstruction** to move back, precisely because no writer left a trace, and [CR082](../cr/cr-082-tax-section-fbar-114.md) had to design freeze-on-file around the same property. ⚠️ **A TRIGGER, not a service insert, and this reverses a convention migration 072 wrote down eight weeks ago** (*"this repo has exactly one non-internal trigger"*). 072 was declining a trigger that would **enforce an invariant**; this is **observation** — it refuses nothing and drops in one statement. The deciding fact is that the column has more writers than anyone remembers: `reconcileToFeed.js:600`, `reconcileManual.js:317`, `reconcileManual.js:291` (`SET opening_balance = 0`, found by a reviewer and not by the CR that set out to audit this column), `repositories/accounts.js:360`'s generic COA whitelist, **plus five in `v2/scripts/`** — and a human at `psql`, which is where the CR080-class damage actually happened. Owner decision, 2026-08-23. **UPDATE only** (an INSERT is an opening balance being stated, not revised). **`user_info` reads `app.actor` if a caller sets it** and records NULL otherwise, so the services can add `SET LOCAL app.actor` later and start recording *which path* wrote **with no second migration** — verified working. ⚠️ **`accounts.last_calibrated_at` is NOT dropped**, deliberately: it is stale since 2026-06-03 and nothing writes it, but its **67 populated rows are historical evidence**, and destroying that in the migration whose purpose is to stop losing exactly that would be self-defeating. It carries a `COMMENT ON COLUMN` marking it superseded, because a stale column someone later wires into a UI is CR085's named defect class. Reader sources from `audit_log`. **10 tests** in `openingBalanceAudit.test.js`; re-apply verified idempotent. Applied **dev 2026-08-23, then prod 2026-08-24** (deploy Step 2b, v3.38.0; verified on prod afterwards — ledger row present, trigger and function present, COMMENT applied, `audit_log` correctly **empty** because the trail starts forward). Dev first, deliberately ([Known Issue #15](project-roadmap.md#3-known-issues) is migrations reaching prod ahead of dev). (through the runner — an earlier hand-applied `psql -f` did **not** write `schema_migrations`, the trap 057's row records). |
| 073 | `073_forecast_scenario_is_scratch.sql` | **CR085 P0 — a throwaway scenario says so in the database.** Adds `forecast_scenarios.is_scratch BOOLEAN NOT NULL DEFAULT FALSE` + a partial index on `created_at WHERE is_scratch`, and back-fills the flag onto anything already named `__scratch_%` or `__autoadjust_%` (0 rows on dev). **Additive and inert:** every existing scenario is `FALSE`, which is exactly today's behaviour. Fixes a defect open since CR053 and recorded in **CR084 §9.2** — CR053's solver and CR084's save-time preview both build a deep-copy scratch through `copyScenario`, which inserts `is_active = TRUE`, and `findAllScenarios` filtered on nothing else, so a scratch was visible in **all seven scenario pickers** while it existed and permanently if the process died before its teardown. `findAllScenarios` now excludes it from **both** branches (`activeOnly` true *and* false), `copyScenario` takes `{ isScratch }`, and `sweepStaleScratch()` (boot, > 1 h old) removes the leaked ones. **Not a name test:** two prefixes are already in use and CR050 §3 rejected name-keying for this exact reason; **not `is_active`**, which is owner-facing (`SCENARIO_UPDATE_FIELDS`, exposed as `IsActive`) and could be flipped back from the UI. Applied **dev 2026-08-19**, **prod 2026-08-21** with v3.32.0 (deploy Step 2b, before the code — `findAllScenarios` and `copyScenario` read the column unguarded). Verified on prod after the deploy: column present and NOT NULL, in the ledger, **0 rows flagged** (so the boot sweep, which *deletes* what the back-fill flags, had nothing to act on), all five scenarios still listed, and a real 7-build sensitivity run left **0 scratch scenarios and 0 residue in the assumptions document**. |
| 075 | `075_investment_positions.sql` | **CR061 P1 — the tables that hold what we OWN, and the classification that decides what an instrument IS before anything prices it.** Three tables (`security_position_snapshots`, `security_positions`, `security_quotes`) plus four nullable columns on `securities` (`price_basis`, `quantity_unit`, `classification_source`, `quote_symbol`). **Additive and inert:** the whole CR019 investment schema is **0 rows on dev and prod** (verified 2026-09-02), so nothing is migrated, back-filled or re-read; reversal is dropping the three tables and four columns. ⚠️ **The one behavioural change is a REMOVAL: `securities.asset_class` loses `DEFAULT 'stock'`.** That default is the path by which an unclassified CUSIP silently becomes a stock — and a stock is quote-eligible — which prices a 100,000-face bond at an equity's $250 and books $25,000,000. Post-condition asserts the default is *gone*, not that a column exists. The vocabulary also gains `mutual_fund` (the live portfolio holds $147,988 of FCNTX, which the old set could not express) while **keeping CR019's legacy `stock`/`mf`/`misc`**, because that CR is IN-PROGRESS and its Quicken promote maps onto them. **Why a snapshot header and not just position rows:** CR090 reconciles `custodian balance − Σ positions`, and both halves must come from one capture — measured $10.00 wide when paired across two fetches, **$0.50** from one. `status` (`fetched`/`empty`/`absent`/`partial`) then makes "looked and found nothing", "upstream has no such day" (anything before 2026-07-04) and "the fetch broke" three different answers instead of one indistinguishable absence of rows. **Why two dates:** `polled_on` is when the custodian was asked; `valued_on` is when the values were true, and ⚠️ **nothing upstream states it** — the 09-02 snapshot carries 08-31's closes (CR089) — so it ships nullable and mostly null, and no consumer may silently fall back to `polled_on`. **Why quotes get their own table:** `security_prices` is `UNIQUE(security_id, price_date)` with a single `close`, so an intraday quote and a custodian price both dated today would collide and last-writer-wins with no timestamp to tell them apart. **Deliberately NOT `security_lots`** — that table requires `acquired_date` and `cost_per_share`, which a snapshot has neither of, and writing snapshots there would fabricate both and poison CR020's lot model. **Verified** on a from-scratch DB via `Scripts/test-fresh-db.sh` (**1140/1140**, +15 new constraint tests in `cr061Schema.test.js` that assert the constraints rather than the intentions). Applied dev **2026-09-03**, then **prod 2026-09-03** (v3.50.0, at deploy Step 2b). | CR061 |
| 076 | `076_price_basis_per_100_face.sql` | **CR061 P1 correction — 075 assumed ONE bond convention; there are two.** Widens the `securities.price_basis` CHECK to admit `per_100_face`. **Additive:** it changes no row, and re-classification is a separate, inspectable step that touches only `classification_source = 'inferred'` rows so an owner decision is never overwritten. Measured live 2026-09-03: **Fidelity Bond** holds 29 positions priced **77.92–103.07** (a *percent* of par, quantity in $100-face units — `1000 × 98.745 = 98,745`), while **Fidelity Cash Mgt** holds 8 priced **0.9873–1.0002** (a *fraction* of par, quantity in face dollars — `100000 × 0.9989 = 99,890`). 🔴 **`value = quantity × price` holds for both**, which is exactly why the conflation survived ingest, every test, and a reconciliation that ties to the cent: no arithmetic anywhere was wrong. Only the DISPLAY was, and only for one of the two — the page rendered a bond priced at 98.745 as **`9874.500`**, because the renderer applied the fraction convention's ×100 to a price already expressed as a percentage. ⚠️ **Found by LOOKING AT THE RENDERED PAGE, not by a test** — the fifth time this project has recorded that (CR082, CR085, CR087 P0b, CR088 P5). Re-classified 30 inferred securities and propagated the corrected basis to 1,699 stored positions (a defect fix propagates; a genuine *reclassification* would not). **Verified** on a from-scratch DB (**1202/1202**). Applied dev **2026-09-03**, then **prod 2026-09-03** (v3.50.0, at deploy Step 2b — the prod backfill ran after, so its rows were classified correctly from the start and needed no re-classification). | CR061 |
