# Status — Fin

> The one mandatory read at session start. Keep ≤ ~60 lines; link onward, never restate.
> CR statuses live in the [CR index](../cr/README.md); the running version lives in `VERSION`.

**Last updated:** 2026-07-29 · **Live version:** v3.6.8 (see `VERSION` / git tags)

## Current phase
- **Nine PocketSmith sign defects on the Fidelity accounts — all found, fixed, dev + prod (2026-07-29).** A transfer whose legs carry the **same sign** is money appearing with no counterparty; `opening_balance` is a plug that absorbs it, so today's balance stays right while every **earlier** date is wrong — which is how these survived six years and why no balance check could find them. Six were corrected in a second wave hours after [CR058](../cr/cr-058-quicken-valuation-anchors.md) shipped, and **the method is the reusable part**: the Fidelity QIF's 2020–2022 rows sit in `quicken_staging` **cutoff-dropped and unpromoted**, so they are an *independent* record of exactly the era PocketSmith owns and cannot be circular evidence. Comparing `sum(amount)` per `(date, magnitude)` bucket isolates every disagreement in one query; the ACH **PPD trace numbers** then prove two rows describe one event (Fidelity Stocks 2021-04-09 **+25,000 → −25,000**, with Chase's own ledger showing the money arriving). Cash Mgt 2021-07-19 −20,000 and 2021-11-05 −15,000 corroborated the same way. Fidelity Stocks 2020–2022 now has **exactly one** unresolved disagreement — 2022-11-02, ~187,689, a USD→EUR *modelling* difference not a sign error, deliberately untouched pending the statement. Account 27 was re-anchored: only **three** anchors moved (1998-03-20, 2021, 2022-12-28); **Σ, the reversal and every interior anchor including 2020's −9,331.73 are unchanged**, because a uniform lift of the pre-2021 ledger cancels inside the sequential formula. **`--check` caught the drift unprompted** — 25 of 26 dates — one day after shipping the feature built to guard exactly that. 523 backend tests green. *Non-Fidelity clusters (PKO - Deposits modelling, two property purchases) remain untriaged — see the [roadmap](project-roadmap.md#ps-transfer-sign-defects).*
- **"Reset opening" — the `opening_balance` plug is now removable from the UI ([CR033](../cr/cr-033-manual-calibration.md), v3.6.8).** Every ledger row reads `opening_balance + Σ tx`, so an account whose real history starts at its first transaction carries a pre-history balance that inflates **every** date: **CVC Fund VIII** held **28,106.29** dated `2000-01-01`, two years before the fund's first capital call, so its 2022 `Capital Call` of 97,342.11 rendered **125,448.40**. A per-row **Reset opening** button (+ `POST /manual-calibration/reset-opening/:accountId`) zeroes it; the Ledger toolbar gained an **Opening balance** link, since the defect is visible there and fixable only on Manual Calibration. It compensates for **nothing** by design — the existing Reconcile action already books the gap as a *dated, visible* Unrealized-G/L row rather than hiding it back in an account field. *Two traps only building it found:* in **calibrate** mode Reconcile re-anchors `opening_balance = entered − Σ tx` and so restores the **exact** plug, making the obvious *Reset → Reconcile* sequence a no-op (the confirm now says so and points at the mtm switch); and on Quicken-promoted accounts `opening_balance` is a **computed anchor** (CR019 §22 / CR058), not a plug — force-gated, though **dormant**, as all four such accounts are fed. Proven on a dev copy of prod (first row → **97,342.11**, closing → 566,258.71, dev restored) and clicked in a browser. **Owner has not yet run it on prod** — the reset is deliberate, per-account, and drops net worth by the amount removed.
- **[CR058](../cr/cr-058-quicken-valuation-anchors.md) — Quicken-era valuation anchors — built and rolled out to dev (2026-07-28) and **prod (2026-07-29)**, migration 042.** [CR019](../cr/cr-019-quicken-import.md) §22's value-only promote tracks cash but not holdings, so Fidelity Stocks' reconstructed pre-2020 history drifted **negative in every year** — worse than the flat plug it replaced. Now **27 anchor rows** tie each year-end to Quicken's own Net Worth Report, with one reversal at the 2023-01-01 handoff so the PocketSmith era and today's balance are untouched. **Prod reproduced dev to the cent** — same `opening_balance` (874,489.75), same anchors, today unchanged at **1,157,037.74**, all **26 target dates tie**, `quicken-verify` **7 pass / 2 expected warnings / 0 failures**. The account now reads 29,436.00 at end-1998 and 642,513.72 at end-2019, exactly as Quicken reported. Prod was loaded with `copy-quicken-to-prod.js` (CR019 §23 G2) rather than a re-upload, so the 152 name mappings were not redone by hand; `calibration_mode` rode along on the batch row, which is why §4 put it there instead of in a CLI flag. Adds a **`preserve-today`** calibration mode — PS-anchoring drags a feed-owned account back to a stale PocketSmith balance. *Three things only building it found:* `--check` **cleared the anchors before reading them** and so reported drift across all 26 dates of a healthy series; an **unquoted thousands separator** in the target CSV would have parsed as `1`, a silently wrong money value nothing downstream could catch; and the first commit landed on a **misread of a single-suite result while the combined run was red**. *Open:* the §9 step-5 `/investment-returns` before/after — anchors are `is_transfer` rows, so [CR056](../cr/cr-056-investment-returns.md) buckets them as `flow` and the pre-2021 series moves; live on prod, not yet shown to the owner.
- **Two JSX `ReferenceError` crashes, and the lint hole that let both ship — v3.6.6.** The owner reported the **mobile home page** (`/m`) as an error screen while every other `/m` route worked. `ba4ef7f` ("delete 44 dead variables") had rewritten the launcher as `CARDS.map(({ to, label }) => …)` while the body still rendered `<Icon />` — broken since v3.0.96, **two weeks live**. The same commit made the identical edit to `MobileTabBar`; that copy was caught only because it happened to trip the *args* ignore pattern and turned the gate red from v3.4.8 to v3.6.0. **MobileHome produced no finding at all** — `npx eslint` returned clean on the broken file, the build passed, all 211 tests passed. *Cause:* `<Foo />` parses to a JSXIdentifier and core ESLint builds **no scope reference** from it, so both directions were blind — JSX-only use looks **unused**, and a component that doesn't exist isn't **undefined**. The capitalized `varsIgnorePattern` compensated for the first half and, being an ignore, could never buy the second. Now `eslint-plugin-react` is installed for **exactly two rules** (`jsx-uses-vars`, `jsx-no-undef` — *not* `recommended`, which is mostly prop-types), the capitalized escape hatches are **removed** (`^_` only), and reverting either fix errors. **It immediately found a second, worse crash:** `TransActual` renders `<CategorySelector>` in the split-transaction modal but hasn't imported it since **`5e638aa` (2026-03-28)**, which deleted the import and one of its two usages and missed the other — and because the throw is inside TransActual's own render, clicking **Split** on the Actuals page took the **whole page** to the ErrorBoundary, for four months. Removing the hatch also surfaced 4 dead identifiers. **Both fixes verified in a browser, not just by the rule** — `/m` in an iPhone-13 viewport (8 cards, 8 icons, zero console errors) and the split modal on a real PLN row (both selectors populated); deleting either fix reproduces the exact `ReferenceError`. *Takeaway: a guard that cannot see the language construct it guards is not a guard — and the ignore pattern that hid the symptom was what kept it from being noticed.*
- **[CR057](../cr/cr-057-book-income-at-source.md) — Book Income at Source — shipped v3.6.0 (2026-07-28), migration 041.** Investment income sat where the **cash landed**, not on the holding that earned it, so CR056 reported United Beverages at a **confident 0.00%** on ~25M PLN of capital. A per-row **Book at source** action in the Ledger writes a **three-leg booking** (income at the holding + transfer out + the original row re-categorized), balance-neutral on both accounts, with a `dryRun` preview sharing the write path and a snapshot-guarded **undo**. **It overturns CR056's own P2 design:** "including the dividends would break the identity" held only for a *one-legged* change — with the transfer leg, `Δ totalReturn = 0 − (−X) = +X = Δ income`, and `bucketOf` has no fall-through, so both legs land right by construction. **Pass 1 `revise` (5 blocking), pass 2 GO.** Pass 1's headline outlives this CR: the draft's central test — *"the identity must stay 0.0000"* — **could not fail**, because `fxEffect` is a plug; the replacement asserts **`FX effect` unchanged to the cent**, the one number a bad `base_amount` copy moves. The other four findings were the same asserted-not-constructed shape. **Verified on a prod copy:** flows/income moved by the exact amounts, MVs and FX effect unchanged, book values and the balance-sheet/cash-flow reports byte-identical, marks untouched, round-trip clean, undo refuses an edited leg, Transfer Analysis auto-matches 2/2. *Two defects only running it could find:* Cash Flow **excludes** transfers by default (so the income leaves PKO's P&L entirely rather than being relabelled — rev 2 **and** pass 1 both had this wrong), and the double-book guard returned the wrong error because booking rewrites the row's category. Owner walked it on dev and booked 2 of 7. Deferred with reasons: CVC (cross-currency), `Other Investments` (mis-signed, not mis-placed), `Transfer - Historical` (2,741 rows — **not** balance-neutral; each added leg needs a paired mark adjustment).
- **Ledger category filter was blind past its first page (found during the CR057 walkthrough).** Options were derived from the **loaded** rows — `TRANSACTION_BATCH_SIZE = 500` — so on PKO (4,572 rows) the filter could only ever offer categories from the most recent ~11%, and the filter itself ran client-side over that same page. `Financial Income - UB Dividend` sits at position 532, so **four of the five UB dividends were unreachable through the UI**. Both halves now go through the server: new `GET /transactions/categories` (distinct categories on the account, whole period) and the category choice is passed to the API. Pre-existing — the old `<select>` had it too; the new type-to-filter combobox (`SearchableSelect`, tokens-only so dark mode comes free) just made it visible.
- **Book-at-Source conversion label pointed the rate the wrong way — v3.6.4, display only.** It read *"at PLN/USD 0.239682"*, which parses as *PLN per USD*, under which the shown figure is impossible (158,114 × 0.239682 ≈ 37,897, not 659,682.41). `exchange_rates` stores **USD per PLN** and the conversion divides by it; the label now says **"at 4.1722 PLN per USD"** — the direction a reader multiplies. Same rate, same conversion, legs still cancel exactly. *Owner challenged the amounts; the amounts were right and the label was not.*
- **Two PocketSmith transfers were booked as CREDITS on Fidelity Stocks — v3.6.3, wrong for six years.** 2020-08-26 **+617,957.20** (a transfer to Fidelity Cash Mgt credited on *both* accounts, no opposing leg anywhere; Quicken has it leaving as `XOut`) and 2020-11-04 **+100,000.00** (paired against a Chase `+100,000.00 Fid Bkg Svc` row — both legs positive). **They hid because `opening_balance` is a plug:** today's balance stayed right while every date *before* each row was understated, by **1,435,914.40** in total — so any check that reconciles only the current balance could never find them. `fix-ps-transfer-signs.js` re-plugs by the same amount and aborts unless today is unchanged (verified: 1,157,037.74). *The corroboration is the strong part:* the fix collapses [CR058](../cr/cr-058-quicken-valuation-anchors.md)'s 2020 anchor from **−1,445,246.13 to −9,331.73** — Quicken and PocketSmith agree on 2020 to within **2.4%**. Found by a net-to-zero sweep of `is_transfer` rows ≥ 1,000: 15 same-signed clusters out of 903; only these two are confirmed, **the other 13 await triage**.
- **Cross-currency Book at Source — v3.6.2.** CR057 refused a holding in another currency; that was too cautious. The two legs are equal-and-opposite on the **same** account, so they cancel in the holding's currency *and* in USD — the book value is unchanged whatever rate is used, and no mark is disturbed. The rate only decides what the LC-mode figure reads. Derived from the USD base at the transaction date (`fx.rateAsOf`), rounded once and negated verbatim, failing loud on a missing rate. Verified on the real CVC row: **USD 34,942.07 → EUR 30,471.93** @ 1.146697, book unchanged, LC `FX effect` 0.00, and Transfer Analysis **auto-matched** the EUR leg to the USD cash row (it keys on `base_amount`). Unblocks the 6 CVC rows. Plus two owner-found bugs: **stale selections survived a data reload** (the bar read "2 selected" with one row ticked and the single-row action vanished — latent in the hook, made routine by v3.6.0's server-side category filter), and the **modal read snake_case field names** where the pages supply PascalCase, so its summary panel rendered blank. Both found by clicking, not by tests.
- **Two date bugs in the transaction tables — v3.6.1, both owner-found while working the CR057 rows.** (1) `parseEditFormValue` returned a **Date object** for date fields; `JSON.stringify` turned it into a full ISO instant, which the API's `assertDateString` rejects — and since a single-row edit always includes `Date` via consensus, **every** single-row edit on Ledger and Actuals 400'd, whatever field was being changed. Broken since v3.0.54. (2) `new Date("2021-08-19")` parses as UTC midnight, so the tables rendered **a day early** west of UTC — the row read "Aug 18" while the edit picker said 19. New shared `parseDisplayDate()`; the three desktop tables now share it. *The tests only count because they were made able to fail:* the runner is **UTC**, where the buggy parse also gives the 19th, so the spec pins `TZ=America/New_York` and asserts the pin held — reverting both fixes turns 6 of 11 red.
- **The blocking lint gate had been red on `main` since v3.4.8**, and not for a real defect: `eslint-plugin-react` is deliberately not installed, so JSX-only usage is invisible to `no-unused-vars`; capitalized *vars* were exempted for exactly that reason but capitalized *args* were not, so `({ icon: Icon }) => <Icon />` in `MobileTabBar.jsx` was a false-positive **error**. It also made `check-lint-debt.sh` abort with no output (`set -e` + `pipefail`), reading as a debt failure. `argsIgnorePattern` now matches the same convention. **All six guards green.**
- **[CR056](../cr/cr-056-investment-returns.md) — Investment Returns report — shipped v3.5.0 (2026-07-27).** `/investment-returns`: account (parent rolls up) + period + interval → realized income and unrealized G/L per period, absolute (USD ⇄ original) and as **realized / unrealized / total %** on average capital `(open+close)/2`. Ledger-derived: the CR019/CR022 securities tables are **0 rows**, so the numerator is the existing `Unrealized G/L` postings plus auto-derived income categories, MV on the Balance Sheet's additive basis. **Two review passes and then the owner clicking it changed the design more than they polished it.** Pass 1 (technical) returned *revise* with 6 blocking findings, all reproduced against prod first: the reconciliation identity was **asserted, not constructed** — category 206 and NULL-category rows sat in no bucket, leaving Fidelity IRA 2025 silently off by **$8,781.87**; `Transfer - Securities Trades` turned out to carry **real deposits** (Fidelity Stocks 2020 **+$200,460.86** — two `Fid Bkg Svc` ACH transfers, found by reading the rows after confirming the trade legs pair exactly), reversing my own earlier recommendation; a whole-period Dietz fallback produced a **+2,064%** headline; and "has it ever been marked" showed a never-revalued **€422K** property at a confident **0.00%**. Pass 2 (PM) returned *revise on scope* → P1 GO, and caught two wrong assertions in my own test plan. Then the owner opened the page and four more fell: it **rendered in two columns** (`.page-main` is a hard-coded 2-col grid; five pages now carry an undo-class — logged as [roadmap §4.1.7](project-roadmap.md)); Modified Dietz → **average capital** with **three** return rows; the coverage rule was **incoherent** — the table printed United Beverages' +1,151,997 while suppressing the % that same posting implies, so a period is now covered when a valuation falls *inside* it, with misdating disclosed via **†** rather than hidden; and a fourth interval, **`marks`**, lays columns out between valuations (correct by construction — it turned UB's blank calendar grid into a real +138.70% series). Buckets are **exhaustive by construction**; verified on prod data that the identity closes to **0.0000 in every interval** for single accounts, roll-ups and LC mode. **470 backend / 195 frontend / build ✓ / five guards ✓.** No migration. *The known limitation at ship — United Beverages reporting 0.00% realized because its dividends posted to PKO — was **resolved at the source by [CR057](../cr/cr-057-book-income-at-source.md)**, and this report needed no change to pick it up (UB now shows 3.98% / 3.34% realized).* **v3.6.5** adds an **IRR** line below the table (money-weighted, annualized, solved by bisection on the *actual dated* cash flows — not the per-column aggregates), moves `Ending market value` up to close the value block, bolds both balance rows and spaces them off from the return block. **v3.6.7** fixes IRR abstaining on **closed-out** positions: it required a valuation in the period, which blanked `CVC - MIP` (no marks ever, ends at 0.00 after returning ~3.78M PLN on 347K in) for a reason that cannot apply when the ending value is zero — a closed position's ending value is exact, not a cost-basis guess. Now `valued` **or** `closed` qualifies; CVC - MIP reports **63.93%**.
- **[CR054](../cr/cr-054-cash-flow-by-account.md) — Cash Flow "By Account" report — shipped v3.4.0 (2026-07-21).** A third Cash Flow tab (`/cash-flow/by-account`) with the By-Period period-column layout plus the Budget-Worksheet category/account filter chips (`HierarchyFilter`, group derivation extracted to a shared `hierarchyFilterGroups.js`) and a **USD ⇄ original-currency** toggle. Backend is **additive/backward-compatible**: `GET /cash-flow` gained optional `category`/`accounts` name filters and `currency=usd|original` (sums `t.amount` vs `t.base_amount`), returning `meta:{currency,currencies[]}`; absent params ⇒ byte-identical to before, so Summary/By-Period are untouched. Because rows are categories, an *original* total spanning currencies isn't a real number — single-currency selections format with the native symbol, mixed ones show a plain decimal + a warning banner ("filter Accounts to a single currency"). No migration. 12 reports-route tests + 195 frontend tests + 4 CI guards green; USD-vs-original parity checked on dev-DB data. **v3.4.1 (owner-found):** the double-click drill-down ignored the account filter (showed *other* accounts) and displayed USD amounts mislabeled as the report currency — now the drill-down (`GET /cash-flow/transactions` + Summarize panel) carries the account filter and shows native `Amount` in each row's own currency in Original mode. **v3.4.2 (owner-found):** the drill-down also ignored the *category* filter (the filtered report keeps the full P&L tree) — it now intersects the clicked node's leaves with the selected categories.
- **[CR053](../cr/cr-053-forecast-auto-adjust-spend-to-fund.md) — Forecast Auto-Adjust: solve spend-down to fund the plan — shipped v3.3.0 (2026-07-16), live in prod.** When the sweep runs out of assets to sell, pick a set of expense lines and the system solves the **least uniform % cut** so **no** year breaches the low band. It's a numerical solve (the cap-gains-tax feedback rules out a closed form): a **standalone deep-copy scratch** (a throwaway *variant* is impossible — trigger-rejected + force-synced) is threshold-searched over the retained fraction until the engine's own `Cash Shortfall` clears (~10 builds, async job+poll), then **Apply** persists it as a CR050 override on a variant (a base scenario gets a "… — reduced spend" variant; base untouched) and a **verification rebuild** proves it. New service `forecastAutoAdjust.js` + 4 routes + `FCAutoAdjustModal`; `generateForecast(name,{writeAudit})`; 9 new tests (117 forecast/variant green, lint clean, build passes). Senior-review-corrected first (throwaway-variant scratch and client-`fcWarnings` objective both rejected). Proof: "2026 Downside (variant)" ($451K final-year shortfall) → **~2% cut** → funded, base untouched. *Open Phase-0 call:* exclude expense items with dated `Changes` from the picker, or scale those too.
- **[CR051](../cr/cr-051-forecast-expense-currency.md) — foreign-currency expense lines — shipped v3.2.0, live in prod (2026-07-15).** An expense line can now be **USD/EUR/PLN**: the native amount is entered, `base_value_usd` is **derived server-side** at the scenario's base-year FX (client value ignored so it can't rot; 400 if the scenario lacks that FX rate), the engine converts per year, and the table shows the native amount under the USD. Also the **F1 engine guard** — the incexp builder now *fails loud* on a missing/zero FX rate for a currency in use (it would have divided by 1.0 → silent ~4× overstatement, or by 0 → Infinity; latent until a non-USD line existed). Expense-only (foreign income can't express CR047's income-tax override); disabled while Matched. **407 backend / 195 frontend / 7 Playwright** green, lint 0 errors. No migration, no engine behavior change for USD scenarios. *(The owner opted to ship directly; the pre-release dev walkthrough was waived.)* Modelling caveat, documented: holding FX flat while inflating a PLN line at the US rate implicitly assumes real złoty appreciation — trend the FX-PLN assumption weaker to model higher PL inflation.
- **[CR050](../cr/cr-050-forecast-scenario-variants.md) — forecast scenario VARIANTS — the v3.1.0 minor milestone (v3.0.108→v3.1.0, 2026-07-14, migration 039).** Five post-ship fixes (v3.0.110–112) were all in how edits are *captured and reported* as overrides — DATE-as-instant, float noise, missing schedules, and the assumption-editing UI bypassing the override system (silent data loss) — never in the inheritance machinery. v3.1.0 also gives **Forecast Expenditures a draft-add modal** like Modules (no more blank "All" rows).
  A scenario copy is a severed island: you duplicate 30 entities to change one field, nothing
  records *which* field made it a downside, and the copies then rot. A **variant** now inherits
  every item from its base unless overridden — overrides are a **JSONB patch keyed to the base
  row's id** (field-level, because `NULL` is already load-bearing in these columns, so
  "NULL = inherit" was never available), and a **sync step materializes base ⊕ overrides into real
  rows**, so the engine, Review, Compare and the audit CSVs read an ordinary scenario and **do not
  change**. A read-time overlay was rejected: reads are *not* funneled (engine loaders, repo
  finders and `crud.getBaseYearValues` are three paths), which is exactly the CR049 drift. Sync's
  column list comes from `information_schema` — the direct fix for the bug class that dropped
  `cash_sweep_priority` (CR045 §1) and the assumptions (CR048) on the way into a copy.
  **Proof:** a zero-override variant of "2026 Base" builds **1,426 entries byte-identical to its
  base**; override one field and it pins while the base's *other* changes still flow through.
  *The tests earned their keep*: the sweep-priority unique index tripped **transiently** mid-upsert
  (valid final state, invalid intermediate one) — sync now parks the ranked flags first.
  **Owner's next step:** read the `adopt-variant` dry-run for "2026 Downside" (22 differences —
  and **8 modules differ only in `base_value_usd`**, which is not a downside assumption, it is the
  copy having rotted) before adopting. Nothing is adopted yet; prod is untouched.
- **Black Card feed incident (2026-07-14) — 31 duplicate transactions in prod, and the feed had
  been misrouting for a week.** Two sheet rows were both named `Black Card (9915)`; bank-feed's
  converter name-joins tx→account **last-wins**, and its id-based guard (added by `cc23929` for
  exactly this case) read only MX/Chase's `raw.accountId` — **Plaid sends `account_id`**, so the
  guard was dead code and the *stale* row captured every Black Card transaction. Mapping it in fin
  then back-filled its whole staged history (new mappings default `promote_from_date = NULL` ⇒
  "promote everything") on top of a period already covered by a **manual statement upload** —
  31 duplicates, **$8.4K gross**, but **net only +$267** because a payment offset the spending, so
  *a balance check would not have caught it*. Fixed: 30 dups deleted (+1 kept — the feed's matcher
  had collapsed two real $19 credits into one, so the ledger was $19 short), cutoff set, stale sheet
  row deleted by the owner, dead account purged from bank-feed, and the converter fixed in
  **bank-feed `c49b459`** (+2 tests; the new test fails against the old source). Ledger now
  reconciles to the cent (opening −6,206.64 + tx 227.17 = **−5,979.47** = feed). *Takeaway: read the
  upstream source of truth before trusting any downstream view of it — fin's DB and bank-feed's DB
  both showed the stale account as live; only the Sheet's `Last Update` column disambiguated.*
- **Owner acceptance + re-test loop (2026-07-13), and it earned its keep.** The owner walked the
  v3.0.96 release step by step (module save · modals · audit trail · reports · dark mode · fonts —
  **all six passed, no regressions**), then re-tested each fix one at a time. Clicking through found
  **eight bugs no test would have caught**, including one the tests actively concealed: **Modify
  Transfer had never worked** — it fetched the module *list* endpoint, which returns no
  `Invest`/`Dispose`, so it had never once displayed a transfer, while a green unit test on the
  (unreachable) year-matching predicate said otherwise. Shipped v3.0.97 → **v3.0.100**; all fixes
  owner-re-tested and passing. *Takeaway worth keeping: verify a UI fix through the UI or an
  end-to-end fetch — a unit test on a code path that never receives data is worse than no test.*
- **Forecast hardening (CR045 → CR049), 2026-07-12/13.** Owner questions have now opened a run of
  **ten silent-wrong-number engine bugs** — all fixed, each verified against a restored copy of
  prod, all shipped and prod regenerated:
  [CR045](../cr/cr-045-forecast-cash-warnings-liquidation.md) (no sweep module on copied scenarios;
  BaseYear cash flow never reached the sweep; tax-free forced liquidations; a module sold twice) ·
  [CR046](../cr/cr-046-module-income-window-and-hierarchy-graph.md) (income/expense **window**,
  migration 037) · [CR047](../cr/cr-047-module-income-tax-override.md) (**income-only tax rate**,
  migration 038) · [CR048](../cr/cr-048-model-review-fixes.md) (conceptual review: drained sweep
  backups kept paying dividends on money that was gone; basis offset two sales) ·
  [CR049](../cr/cr-049-forecast-base-year-seed-and-final-year-tax.md) (the **final-year** sale never
  funded its own capital-gains tax — 2062 ended at −$60,521 cash beside $4.3M of sellable stock; and
  the engine's **hand-copied base-year query** had drifted from `crud.getBaseYearValues`, zeroing
  every non-liability module expense, so the sweep opened $64,717 rich for the whole horizon).
  "2026 Base" now closes 2062 **exactly on the $200K low band**, with no shortfall in any year.
- **Owner's open items:** the equity-growth experiment on `/forecast-compare` ("2026 Base" vs
  **"2026 Base - Market Returns"**: stocks 2.0× vs 1.0×); FX-stress magnitudes for Downside;
  re-type "New House"/"Sarasota House"; rank a sweep backup in **"2026 Downside"** (see Known
  issue below).
- **[CR042](../cr/cr-042-ui-look-and-feel.md) is ✅ COMPLETE (v3.0.101).** U1–U5 shipped and
  owner-accepted; the last IA items settled 2026-07-13 — and two of the CR's *own* proposals were
  rejected on inspection: the **calibration pages stay** under Transactions (they are recurring
  *work*, not configuration — burying work in a config menu to hit a nav-count target makes the app
  worse; only **Bank Feed Setup** and **Forecast Settings** moved to Settings), and the Forecast
  sidebar is **mirrored to the stepper, not collapsed** (collapsing costs a click on every visit to
  the busiest area of the app; the real defect was two hand-kept lists of the same six pages that
  disagreed — `FCStepNav` now *derives* from `routes.jsx`, so they cannot diverge again). "Upload
  PS": **keep**. Creating a module now opens an unsaved **draft** — no more blank rows left behind
  on Cancel.
- **[CR043](../cr/cr-043-code-structure-program.md) is ✅ COMPLETE (v3.0.104)** — Phases 0–3; only
  long-horizon **Phase 4** (TypeScript, Playwright) remains, blocking nothing. **The lint gate is
  BLOCKING** (errors 0; the last 7 were real hook/render bugs). **N8** unified the response envelope
  behind `Rest.unwrap()` with **no flag day**. **util.js** 651→32 lines behind 12 new contract tests
  — which found `POST /coa/update` had been **silently dropping the account type** for months (the
  CR046/CR047 class again). **Six** blocking CI guards, and they all ratchet *down*: buttons,
  modals, inline-hex, dead-tokens, **lint-debt** (56, may only shrink), **api-envelope** (27 bare
  endpoints left, may only shrink).

## Known issue
- ⚠️ *Owner is redoing "2026 Downside" themselves (2026-07-13) — **do not fix this**; it is recorded
  so the numbers below are not mistaken for an engine fault.*
- **"2026 Downside" has no sweep backup ranked.** `Fidelity Stocks` carries no `cash_sweep_priority`
  there (it does in the other two scenarios), so the engine reports **−$1.25M of shortfall across
  2061–62 while $1.2M of stock sits untouched**. That is CR045 §5 working as designed (unranked =
  "I cannot sell this"), but for a liquid brokerage account it is almost certainly a data slip.
  *CR049 made this larger and more visible — it was −$766K in 2062 alone, on a model that was
  $65K/yr too rich and never funded its final-year tax.* One-row fix (`cash_sweep_priority = 2`),
  left to the owner because it changes Downside's conclusions.

## Live infrastructure
- **Dev and prod are the same host** (`192.168.1.87` / Tailscale `100.94.46.62`). Prod `docker-compose.yml` (project `psproject`, :3005, DB :5433, volume `fin_postgres_data`); dev `docker-compose.dev.yml` (:3105/:5434); v4 `docker-compose.v4.yml` (`finv4`, :3205/:5435, flags ON, isolated volume). Prod frontend: `https://fin.tail413695.ts.net`.
- `bank-feed/` microservice (:3007, separate repo) feeds 28 accounts; ocr-llm LLM gateway at `100.66.213.40:8080` (AI Review).
- Deploy: `./Scripts/deploy-to-production.sh` (DB backup first). Migrations: manual `psql -f`, registry in [migrations.md](migrations.md); runner shipped in CR043 P1.1 (`npm run migrate`).

## Recently shipped
- v3.6.8 — **[CR033](../cr/cr-033-manual-calibration.md): "Reset opening" zeroes the `opening_balance` plug.** New `POST /manual-calibration/reset-opening/:accountId` + a per-row button (only where the plug is non-zero) + an **Opening balance** link in the Ledger toolbar. Sets the field to 0 and nothing else — Reconcile books the resulting gap as a dated entry. Guards: calibrate mode would restore the exact plug (called out in the confirm), Quicken-anchored accounts are force-gated. Verified on a dev copy of prod and in a browser. No migration.
- v3.6.7 — **[CR056](../cr/cr-056-investment-returns.md) fix: IRR now computes for closed-out positions (owner-found).** The guard required a valuation in the period; a fully exited fund has none and needs none — its ending value is **zero, which is exact, not a cost-basis guess**. `CVC - MIP` (no marks ever, 347K PLN in 2017 → ~3.78M back by 2024, ends at 0.00) was blank and now reports **63.93%**. `total.irrBasis` reports `valued` vs `closed`; still-open unvalued accounts correctly abstain. Verified read-only against prod.
- v3.6.6 — **two JSX `ReferenceError` crashes + the lint gate that could not see JSX.** Owner-reported: `/m` was an error screen (`Icon is not defined`, live since v3.0.96). `eslint-plugin-react`'s `jsx-uses-vars` + `jsx-no-undef` now run, the capitalized `no-unused-vars` escape hatches are removed, and the new rule immediately found a **four-month-old** second crash — `TransActual` renders `<CategorySelector>` without importing it, so clicking **Split** on Actuals took the whole page to the ErrorBoundary. Both verified in a browser. 212 frontend tests, lint 0 errors, six guards green. Frontend + tooling only; no migration.
- v3.6.5 — **[CR056](../cr/cr-056-investment-returns.md) refinement: IRR + table layout.** A money-weighted **IRR** (annualized) below the table, solved on the real dated cash flows — opening value as the initial investment, every external flow at its own date on the investor's sign convention, closing value as proceeds; bisection over [−99.99%, +1000%] so it cannot run away. It renders `—` when there is no sign change to solve across, the span is under 30 days, or **nothing valued the period** — an IRR off a cost-basis ending value would report ~0% as though the asset had genuinely not moved (US - Nokomis correctly abstains; Fidelity Stock returns 19.79%). Layout: `Ending market value` moved up under the components that explain the move from `Beginning market value`, both balance rows bold, a spacer between the value block and the return block. Frontend + one new `total.irr` field; no migration.
- v3.5.0 — **[CR056](../cr/cr-056-investment-returns.md): Investment Returns report.** New `/investment-returns` page + `GET /api/v2/reports/investment-returns`. Four interval modes (month/quarter/year/**between valuations**), USD ⇄ original currency, realized / unrealized / total % on average capital. Doubles as a data-quality instrument — the `Unattributed` row surfaced $200K of ACH deposits filed as securities trades and a `Car Purchase/Sale −$29,954` sitting on a brokerage account. No migration; additive endpoint only.
- v3.4.6 — **COA Type filter uses real account types (no "Category"); + migration 040 data fix (owner-found).** The filter offered "Category" and matched on the tree pseudo-type, so filtering *Expense* hid the expense **containers** and left orphaned leaves. Now `typeOptions`/`filteredRows` use the real `account_type` (Asset/Liability/Income/Expense), so a type filter surfaces its containers too — hierarchy restored. Exposed a legacy data bug fixed in **[migration 040](migrations.md)**: five accounts on the wrong side of their `section` (`Income` expense→income; `Liabilities`/`Tax Liabilities`/`Tax Reserve - PL`/`Tax Reserve - US` asset→liability). **Report-neutral** (Balance Sheet is section/tree-driven; the forecast `isLiability` branch gates `ExpensePct`, hardcoded 0; Budget/Tx use type only in GROUP BY). Dev click-verified before deploy. Seed already correct — legacy data only.
- v3.4.5 — **COA add-child dialog inherits the parent's type/currency (owner-found).** Clicking **+** to add an account under a category let you pick any Type, but the backend already **ignores it** and inherits `account_type`/`section` from the parent — so the dialog now locks Type to the parent's real type (read-only, "Inherited from the parent category"). A container shows `Category`/`—` in the tree but its true type/currency come from the traits map, so `buildCoaRows` carries `accountType`/`accountCurrency` and both the **+** and toolbar-picker paths seed them. **P&L** (Income/Expense) currency is display-only (the P&L report sums the *transaction's* currency), shown read-only; **Asset/Liability** keeps an editable currency (mixed-currency containers). **Account #** relabelled "(optional)". Frontend-only, no migration, lint/build green; **not** clicked live before deploy.
- v3.4.4 — **`PeriodSelector` "All" (full-history) preset.** A new "All" chip (Jan 2000 → Dec current year) on the shared period picker, rendered **only** where `enableYearRange` is on (Actuals, Ledger, Budget txns, Balance Trends) — the 9 single-year consumers, which can't express "all time", are untouched. Feeds the existing `year`/`toYear` filter; no backend change, no migration. Frontend-only; not clicked live before deploy.
- v3.4.3 — **[CR055](../cr/cr-055-category-suggest-backoff.md): category-suggest progressive backoff.** The "Suggest categories" merchant key was a fixed 3 tokens, so `AMAZON MKTPL*<orderid>` fragmented into ~177 distinct keys and **Amazon was never suggested despite 177 categorized rows**. Now keys at 3→2→1 leading tokens and takes the most-specific level clearing the unchanged ≥2-sample / >50%-majority bar (Amazon → `amazon mktpl`, 92%); `[SALE]`/`return`/`refund` tags stopped. Verified read-only on the 37k-row prod corpus (Marshalls/Publix now resolve; Target/McDonald's/7-Eleven correctly abstain — split history); 7 new unit tests. No migration. *Not clicked live — exercise on the next feed batch.*
- v3.4.2 — **[CR054](../cr/cr-054-cash-flow-by-account.md) drill-down category-filter fix (owner-found).** With a category filter set, double-clicking a row still listed categories outside the filter (the filtered report keeps the full P&L tree). The drill-down now intersects the clicked node's leaves with the selected categories. Frontend-only.
- v3.4.1 — **[CR054](../cr/cr-054-cash-flow-by-account.md) drill-down fix (owner-found).** The By-Account double-click drill-down ignored the account filter (pulled the category across *all* accounts) and showed USD amounts under the report currency label; now it passes the account filter and, in Original mode, shows each row's native `Amount` in its own currency (list + Summarize).
- v3.4.0 — **[CR054](../cr/cr-054-cash-flow-by-account.md): Cash Flow "By Account" tab.** By-Period layout + category/account filter chips + USD/original-currency toggle; additive `GET /cash-flow` params (`category`/`accounts`/`currency`, `meta.currencies`) that leave the existing tabs byte-identical. Shared `hierarchyFilterGroups.js` (chip derivation lifted out of BudgetWorksheetV2). No migration.
- v3.3.0 — **[CR053](../cr/cr-053-forecast-auto-adjust-spend-to-fund.md): Forecast Auto-Adjust.** Pick a set of expense lines; the system solves the **least uniform % cut** that funds every year (threshold search, full-engine per eval — the cap-gains-tax feedback rules out a closed form; objective read from the engine's own `Cash Shortfall`, not the client util). Runs on a **standalone deep-copy scratch** (a throwaway variant is trigger-rejected + force-synced), async job+poll, scratch torn down. **Apply** persists it as a CR050 override on a variant (base target ⇒ new "… — reduced spend" variant, base untouched) + verification rebuild. New service + 4 routes + `FCAutoAdjustModal`; 9 new tests. No migration. Senior-review-corrected before build.
- v3.2.0 — **[CR051](../cr/cr-051-forecast-expense-currency.md): foreign-currency expense lines.** An expense line can be **USD/EUR/PLN**; the native amount is entered and `base_value_usd` is derived server-side at the base-year FX (client value ignored, 400 if no rate); the engine already converted per year. Plus the **F1 guard** (fail loud on a missing/zero FX rate — was a latent divide-by-1.0/Infinity). Expense-only, disabled while Matched. The real cause every line read USD: the CRUD hook hard-pinned `Currency:"USD"` in three places. 407 backend / 195 frontend / 7 e2e. No migration.
- v3.1.0 — **CR050 forecast scenario VARIANTS in prod, plus the Expenses draft-add modal.** A variant inherits every item from its base unless overridden (JSONB patch, materialized to real rows); five post-ship fixes, all in how edits are captured/reported as overrides.
- v3.0.105 — **CR043 Phase 4: a Playwright suite that clicks the money paths.** Every bug this
  session was the same shape — not a wrong number, a **missing** one — and every unit test passed
  straight through, because a unit test on a code path that never receives data passes happily.
  4 specs, 4.1 s, against a **throwaway** Postgres + API (never dev/prod) serving the **built**
  bundle; runs in CI on every push. The hard part was the **seed**: after migrations the DB is
  empty, so every page renders blank — *indistinguishable from the bug being hunted*.
  [`e2e-seed.sql`](../../server/db/e2e-seed.sql) builds the smallest world where each path yields
  a real number (net worth **108,500.00**) and deliberately contains the **periodic-transfer**
  shape that hid the v3.0.98 bug for two years. **Proven to catch it:** restoring the pre-N8
  consumer makes the suite FAIL — a green suite that cannot fail is worse than none.
- v3.0.102–104 — **CR043 closes.** Lint gate **blocking** (the last 7 errors were real: hooks-after-
  early-return, refs read during render, an accumulator mutated in a render loop). N8: one response
  envelope, migrated via `Rest.unwrap()` (which tolerates both shapes) so no consumer and endpoint
  were ever out of step. `util.js` 651→32 lines — and writing its first-ever tests found **`POST
  /coa/update` silently dropping the account type**: 200 OK, old type echoed back, nothing changed.
  Also fixed: **BudgetRealization re-collapsed every row you had expanded** whenever a filter changed.
- v3.0.97–100 — **eight bugs, all found by the owner clicking through, none of them regressions.**
  The headline: **Modify Transfer had never worked** — it fetched the module *list* endpoint, which
  returns no `Invest`/`Dispose`, so it had never displayed a transfer for any module in any year,
  while a green unit test on the unreachable year-matching predicate said otherwise. Also: periodic
  transfers spanned `Date`→`DateEnd` but were matched on the stored year only; the same modal
  re-found rows by non-unique `Date+Flag` and could edit the wrong one; the synthetic `_cash_sweep`
  module 404'd from the breakdown *and* offered a transfer editor it could never satisfy; the SWEEP
  badge keyed off `cash_sweep_target` and so **hid the ranked backups** that the sweep will actually
  liquidate; the Review toolbar's five equally-loud buttons moved onto the shared `.btn` system; new
  modules open their editor; and **closing any modal scrolled the page away** from the cell just
  edited (Radix focus-restore on a double-click opener — fixed once in `<Modal>`, for every dialog).
  All owner-re-tested. Full detail: [roadmap §1.2](project-roadmap.md).
- v3.0.96 — **first restore drill: PASSED.** A real prod dump restored in 3 s / 0 errors; the server
  booted against it; the balance sheet **and** a regenerated forecast came back **byte-identical to
  prod**. Backups are now verified, not assumed — [runbook](../guides/restore.md) is a transcript, not
  a plan. Plus lint 108 → 64 (44 dead variables deleted; `no-unused-vars` and `no-undef` both at zero).
- v3.0.95 — **overnight run.** CR043 **N10**: the forecast module / inc-exp write API now rejects
  unknown fields instead of silently dropping them — enumerating the contracts surfaced **four dead
  keys** (`AccountNumber`, `Expense`, `Income`, `BaseYear`) posted for months to columns that do not
  exist, the same silent-drop class that cost CR046 its window dates and CR047 its tax override.
  CR042: **U4 complete** (last 2 Forecast modals); **63 dangling design tokens** across 30 files
  fixed (a `var(--text-secondary)` lints clean and silently ignores the theme) + new blocking
  `check-dead-tokens.sh`; **Outfit self-hosted** (variable font, 2 files, SW-precached — no CDN, and
  it finally works offline); the hex guard was widened after it turned out to be missing every
  composite (66 → true count **170**, now frozen). Lint 124 → 108.
- v3.0.90–94 — [CR048](../cr/cr-048-model-review-fixes.md) engine fixes + whole-scenario copy;
  [CR049](../cr/cr-049-forecast-base-year-seed-and-final-year-tax.md) final-year sweep tax + the
  deleted duplicate base-year query; full-width Forecast tables. Prod regenerated.
- v3.0.77–89 — [CR045](../cr/cr-045-forecast-cash-warnings-liquidation.md) (warnings, sweep tax,
  solvency cap) · [CR046](../cr/cr-046-module-income-window-and-hierarchy-graph.md) (migration 037)
  · [CR047](../cr/cr-047-module-income-tax-override.md) (migration 038) + four follow-on fixes.
- v3.0.63–76 — [CR042](../cr/cr-042-ui-look-and-feel.md) UI (U1–U5),
  [CR043](../cr/cr-043-code-structure-program.md) code structure, audit-trail 500 fix.
*(Detail for every release: [project-roadmap.md §1.2](project-roadmap.md).)*

## Next
**With the owner (do not start these unasked):**
- **"2026 Downside"** — the owner will redo this scenario themselves (stated 2026-07-13). Leave it,
  including the unranked-sweep-backup known issue below.
- CR048: the equity-growth and FX-stress decisions.
- CR042 IA: calibration→Settings move; collapsing the six Forecast steps in the sidebar.
- **Open design question:** creating a module now opens its editor — but the module is already
  created by then, so **Cancel leaves a blank, nameless row behind**. Should Cancel delete it?

**Engineering, unblocked:**
- **[CR059](../cr/cr-059-fintable-api-ingestion.md) — Fintable now has a REST API; bank-feed can stop scraping the Google Sheet.** Drafted 2026-07-28 and **P0 measured the same day**: ids **differ** (0/31 accounts, 0/1,387 transactions), so the crosswalk is confirmed — but it is **exact, not heuristic**, because our Sheet transaction ids are `{api_account_id}--{hash}` and already carry the API's account ids (15/15 verified). The API kills the **display-name join** behind the Black Card incident, plus Excel serials, floats and per-upstream currency digging. P0 also found what reading the docs could not: the documented `pending=true|false` filter **422s** (only `0|1`), `type` is the raw bank product name so the keyword inference stays, and **two mapped Revolut wallets no longer exist upstream** (Bank Pekao's connection is unhealthy too). Blocked on two owner decisions — the Chase date basis (`auth_date` vs posted `date`) and those Revolut wallets.
- CR043's lint gate: 64 errors, all hooks/react-refresh restructures. Needs the app in front of
  someone; do it incrementally with tests, never as a batch (a scripted pass in v3.0.96 produced
  *valid JS that passed the build and all 179 tests* but would have thrown on import — only
  `no-undef` caught it).
- CR043 tails: N8 `{data,meta}` envelope unification; `util.js` hygiene split; Phase 4 (TypeScript,
  Playwright).
- Long-running: [CR019](../cr/cr-019-quicken-import.md) investment-side promote,
  [CR023](../cr/cr-023-pocketsmith-removal.md) per-account PS migration (13 left),
  [CR034](../cr/cr-034-security-hardening-ci.md): rotate `BANK_FEED_API_KEY`.
- Full plan: [project-roadmap.md](project-roadmap.md).

## Conventions
Docs layout & rules: [documentation standard](../documentation-standard.md) · working rules
load from `.claude/rules/` (collaboration, git-concurrency, migrations, compose-safety,
env-secrets, data-import) · procedures: `/close`, `/question` · dual-track v3/v4:
[dev-workflow](../guides/dev-workflow.md) · permissions setup:
[claude-code-permissions](../guides/claude-code-permissions.md).

## Drills & reviews
Last restore drill: **2026-07-13 — PASSED** ([runbook + log](../guides/restore.md)): a real prod dump restored in 3 s / 0 errors, the server booted against it, and the balance sheet **and** a regenerated forecast came back **byte-identical to prod**. Backups verified, not assumed.
Secrets inventory: [secrets-inventory.md](secrets-inventory.md) (escrow status open).
