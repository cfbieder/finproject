# Handoff prompt — CR021 / CR022 continuation

**Date prepared:** 2026-05-31
**Reason:** Prior conversation context running low; user requested handoff to fresh thread.

Copy the section under "Prompt for new thread" into the new conversation.

---

## State summary (for the handoff reader)

### What's live

- **bank-feed** microservice at `~/Programs/fin/bank-feed/` (github.com/cfbieder/bank-feed, private). Running locally in Docker. Sees 13 PKO+Fidelity accounts, 311 transactions, hourly cron. Latest commit: `691d5d3` on `main`.
- **fin** read-only spike at `psproject/server/src/v2/routes/bankFeed.js` and `psproject/frontend/src/pages/BankFeedDiagnostic.jsx`. Lives on branch `cr021/phase-7-spike` (PR #2). Not yet merged.
- **CR022** drafted at `docs/cr/cr-022-bank-feed-parallel-import.md`. Just had R1 + R2 requirements added by user on 2026-05-31 — commit `a40cbf1` on `cr021/phase-7-spike`.

### What's open

1. **R1 & R2 in CR022 §2.3** need to flow into the rest of the doc (architecture §3, phased plan §4, test plan §5, dev walkthrough §7). The synthesis agent didn't anticipate them; they were added 2026-05-31 as a new sub-section but the downstream sections still describe a single-direction (bank-feed → transactions) dedup, not cross-source.
2. **PKO drift discussion unfinished.** `(PLN) (5647)` shows +200.00 drift with subsequent_count=0. User explained: PKO sometimes shows "blocked" transactions where balance moves before the transaction posts. I proposed a refinement to `/v1/health/feeds`: only set `drift_significant=true` when subsequent_count > 0; expose `balance_ahead_of_stream` informationally when subsequent_count == 0. User had not decided whether to do that fix.
3. **PR #2 (CR021 Phase 7 spike)** still open on origin. The branch now contains both the spike commit (05576ec) and the CR022 R1+R2 update (a40cbf1). Merging the PR would land both. If user wants CR022 docs separated from the spike merge, cherry-pick a40cbf1 to main first.
4. **CR022 not yet implemented.** Only designed. Implementation (Phases A–G) is the next major work block.

### Repos / branches / latest commits

| repo | branch | tip | what's there |
|---|---|---|---|
| bank-feed | main | 691d5d3 | Phase 0–3 + tests + /v1/health/feeds + per-institution + SnapTrade dispatch |
| psproject | main | 476431d | Quicken work merged via PR #1, includes prior CR021 docs commits |
| psproject | cr021/phase-7-spike | a40cbf1 | bank-feed read-only spike (PR #2 open) + CR022 R1+R2 update |

### Critical context the new thread needs

- **CLAUDE.md is at `/home/cfbieder/psproject/CLAUDE.md`** — read it first. It says: read FC_PROJECT_STRUCTURE.md and FC_NEXT_STEPS.md before any task, no flattery, ask questions one-by-one with options, never `chore:` commits.
- **The two psproject paths are the same directory.** `~/psproject` and `~/Programs/fin` share inodes (verified). Same git repo via either path.
- **bank-feed lives at `~/Programs/fin/bank-feed/`** (sibling to ocr-llm).
- **fintable is the upstream.** $7/mo Standard tier. Multi-aggregator (GoCardless for PKO, SnapTrade for Fidelity). Google Sheet ID stored in bank-feed's `.env` as `FINTABLE_SHEETS_ID`. Sheet has `Accounts` and `Transactions` tabs with `⚡`-prefixed column headers + `⚡ Raw Data` JSON column.
- **Branch discipline:** start non-trivial topics on their own branch. Don't commit cross-topic work to one branch. I learned this the hard way mid-session and the user explicitly asked for it going forward. **At the start of each session, check `git branch --show-current` and confirm with the user before committing.**
- **Never push without explicit user permission.** Local commits are fine.
- **User pace:** terse, decisive, pushes back fast when you over-engineer. Match the tempo — propose ONE recommendation, not 4-way menus, when they've already shown they want execution.

### Memory entries to consult

`~/.claude/projects/-home-cfbieder-psproject/memory/MEMORY.md` index — the relevant ones:
- `project_cr021_bank_feed_service.md` — CR021 architecture + upstream decision
- `feedback_review_docs_first.md` — skim Scripts/ and docs before recommending commands
- `feedback_no_chore_commits.md` — never use `chore:` in commit type
- `feedback_be_critical.md` — push back, no flattery, disagree when warranted

---

## Prompt for new thread

```
We're continuing work on bank-feed (the CR021 microservice that ingests PKO+Fidelity
via fintable.io's Google Sheet) and CR022 (the additive parallel-import path that
brings bank-feed data into fin's transactions table alongside PocketSmith).

State today (2026-05-31):
- bank-feed/main at 691d5d3 is healthy: 2 connections (PKO id=1, Fidelity id=2),
  13 accounts, 311 transactions, hourly cron, /v1/health/feeds endpoint with
  per-account balance reconciliation
- psproject/cr021/phase-7-spike at a40cbf1 (PR #2 open) contains: the read-only
  bank-feed diagnostic spike in fin (page at /bank-feed-diagnostic) + the CR022
  draft (docs/cr/cr-022-bank-feed-parallel-import.md) with two new
  requirements (R1, R2) just added in §2.3
- Tests: 59 passing in bank-feed (node --test); psproject backend Jest unchanged

R1 (per-account opt-in / ignore-list) and R2 (cross-source dedup during PS +
bank-feed parallel run) are in CR022 §2.3 but the rest of the CR (architecture
§3, phased plan §4, test plan §5, dev walkthrough §7) doesn't yet reflect them.
That's the first thing to do: propagate R1 + R2 through the downstream sections.

Open items to address (user will pick order):
1. Propagate R1+R2 through CR022 §3, §4, §5, §7
2. Decide on /v1/health/feeds drift_significant refinement (PKO blocked-tx false
   positive — see HANDOFF_2026-05-31.md "PKO drift discussion unfinished")
3. PR #2 disposition: merge to land the spike + CR022 R1+R2 update, or split
4. Begin CR022 implementation (Phases A-G, ~5-7 days estimated)

Before doing anything else, please:
- Read /home/cfbieder/psproject/CLAUDE.md
- Read /home/cfbieder/psproject/docs/archive/handoff_2026-05-31.md (this
  file you're reading now)
- Read /home/cfbieder/psproject/docs/cr/cr-022-bank-feed-parallel-import.md
  (especially §2.3 for the two new requirements)
- Check git state: `git branch --show-current` and `git status` in psproject

Then ask me which of items 1-4 above you should pick up first. Do not start
work until I answer — context-switching cost matters more than speed here.

Reminder: I'm terse, push back fast, want one clear recommendation per question
not a 4-way menu. Don't open with flattery. Branch hygiene matters — start any
non-trivial topic on its own branch and confirm with me first.
```

---

## Out-of-band notes for the human

- The PKO drift on `(PLN) (5647)` (+200.00) is the user's observation about PKO's "blocked transactions" — balance moves before tx posts. Two interpretations: (a) it's a feed-feature, soften the alarm; (b) it's a real signal that something is in-flight, surface it but don't alarm. Either way the current `drift_significant=true` for `subsequent_count=0` is a false positive class.
- The frontend hasn't been rebuilt since the per-institution + SnapTrade fix. `fin-frontend` container still shows yesterday's bundle. Not needed unless the user wants to see the live BankFeedDiagnostic page reflect the new state.
- Bank-feed runs unattended via hourly cron. If you walk away for a few days, come back and check `sync_jobs` for recent failures + `/v1/health/feeds` for stale/drift signals.
