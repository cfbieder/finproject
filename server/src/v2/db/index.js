/**
 * Database Module Exports
 *
 * Provides access to PostgreSQL database functions.
 * Import as: const db = require('./db');
 */

const postgres = require('./postgres');

module.exports = postgres;
