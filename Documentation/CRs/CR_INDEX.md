# CR Index

Status legend: **COMPLETED** · **IN-PROGRESS** · **OPEN** · **PLANNED** (scoped, not yet started) · **SUPERSEDED** / **OBSOLETE** (replaced or no longer relevant)

Each CR file's first line carries its status and links back to the matching anchor in [NEXT_STEPS.md](../FC_NEXT_STEPS.md). New CRs get the next available number.

| # | Status | Title | One-line description |
|---|--------|-------|----------------------|
| [CR001](CR001_MIGRATION_MONGO_TO_POSTGRES.md) | COMPLETED | MongoDB → PostgreSQL Migration | Moved all storage to PostgreSQL 16; V1 routes and `coa.json` removed. |
| [CR002](CR002_FRONTEND_REFACTOR.md) | COMPLETED | Frontend Architecture Refactor | God components decomposed; `features/` module pattern; ~22 duplicate transaction files deleted. |
| [CR003](CR003_FORECAST_MODULE.md) | COMPLETED | Forecast Module | Engine + UI for BS modules and inc/exp items across all phases (1, 2A, 2B, 3, 4, 5). |
| [CR004](CR004_FC_LINES_MAPPING.md) | COMPLETED | FC Inc/Exp Mapping Layer | User-defined FC Lines decouple budget categories from forecast outputs (Phase 2B, migration 007). |
| [CR005](CR005_CASH_SWEEP.md) | COMPLETED | Cash Sweep & Auto-Balance | Iterative sweep into a designated module with income↔sweep convergence loop (migrations 012–013). |
| [CR006](CR006_AI_REVIEW.md) | COMPLETED | AI Review of FC Plan | Conversational review via local `ocr-llm` gateway; async with polling + browser notifications (migrations 014, 020). |
| [CR007](CR007_PWA_MOBILE_SHELL.md) | COMPLETED | PWA & Mobile Simplified Shell | Installable PWA + dedicated `/m/*` mobile experience with bottom tab bar and 5 mobile pages. |
| [CR008](CR008_HIERARCHY_FILTER.md) | COMPLETED | HierarchyFilter & Transaction Pages Redesign | Two-stage cascading filter; full redesign of `/trans-actual` and `/trans-budget`. |
| [CR009](CR009_TRANSFER_ANALYSIS.md) | COMPLETED | Transfer Analysis + Manual Match Groups | Auto + manual matching of transfer pairs; `transfer_matched` flag (migrations 005–006). |
| [CR010](CR010_COA_MANAGEMENT.md) | COMPLETED | COA Management Redesign + Move | Tree view editor with toolbar, inline actions, quick-add, and Move modal. |
| [CR011](CR011_SOURCE_MAPPINGS.md) | COMPLETED | Source Mappings (Category + Account) | Decouples external system names from internal app names (migrations 018–019). |
| [CR012](CR012_OPENING_BALANCE_CALIBRATION.md) | COMPLETED | Opening Balance Calibration | Accurate balance sheet via `opening_balance + SUM(transactions)` (migration 016). |
| [CR013](CR013_COLLAPSE_CATEGORIES.md) | COMPLETED | Collapse `categories` Table into `accounts` | Single COA source of truth; FK columns repointed; legacy table dropped (migration 021). |
| [CR014](CR014_POCKETSMITH_REPLACEMENT.md) | SUPERSEDED | PocketSmith Replacement | Dual-provider in-app integration plan; superseded by [CR021](CR021_BANK_FEED_SERVICE.md) (microservice approach). |
| [CR015](CR015_PS_REEXPORT.md) | OBSOLETE | Re-export Changes Back to PocketSmith | One-way push to PS; obsoleted by [CR021](CR021_BANK_FEED_SERVICE.md) (PS removed entirely). |
| [CR016](CR016_FRONTEND_TEST_FRAMEWORK.md) | COMPLETED | Frontend Test Framework (Vitest) | Vitest + 96 unit tests across 5 helper modules; closes the largest backend/frontend test gap. |
| [CR017](CR017_CASH_SWEEP_PHASE_C.md) | OPEN | Cash Sweep Phase C — Multi-Module Priority | Withdraw from multiple modules in priority order on shortfall. |
| [CR018](CR018_BALANCE_TRENDS.md) | COMPLETED | Balance Trends Report | `/balance-trends` shows month-end USD balances for selected BS accounts over a chosen period; reuses `HierarchyFilter` + `PeriodSelector`. |
| [CR019](CR019_QUICKEN_IMPORT.md) | IN-PROGRESS | Quicken Historical Import | One-time per-account backfill of pre-2022 Quicken history into the live ledger (cash; investment side descoped to value-only). Phases A–E on dev+prod. **Prod cutover underway via a per-account delete-and-replace loop** (delete `pocketsmith` rows `<2022-12-01` → import QIF → set `pocketsmith.promote_from_date='2022-12-01'` guard per CR023 → calibrate to feed/PS); **PKO done**. The §23 `copy-quicken-to-prod`/`retire-handoff`/Fidelity-historical-container path is **superseded** by this live loop (see §24). Owns the investment schema CR020 depends on. |
| [CR020](CR020_STOCK_INVESTMENT_MODULE.md) | OPEN | Stock Investment Module | Lot-level holdings tracking + portfolio analytics; supplements (doesn't replace) cash ledger. Depends on CR019 (Quicken Import) shipping first with shared schema. Planning skeleton. |
| [CR021](CR021_BANK_FEED_SERVICE.md) | OPEN | Bank Feed Service | Standalone microservice for Plaid/banksync.io + Excel ingestion behind a versioned REST contract; sets up the v3 PocketSmith cutover. Supersedes CR014. |
| [CR022](CR022_BANK_FEED_PARALLEL_IMPORT.md) | IN-PROGRESS | Bank Feed Parallel Import | Additive second import route into fin's `transactions` table with `source='bank-feed'`, running alongside PocketSmith. One migration (`023_bank_feed_import.sql`), new staging table, dev walkthrough gate, automated tests. Phase A (migration 023) + Phase B (converter/staging/dedup + tests) shipped in v2.8.0 (2026-05-31). Includes **R1** per-account opt-in (`account_source_mappings.ignored`, unmapped=pending) and **R2** cross-source dedup (link bank-feed↔PS rows, `BANK_FEED_DEDUP_ENABLED`). PS removal deferred to CR023. |
| [CR023](CR023_POCKETSMITH_REMOVAL.md) | IN-PROGRESS | PocketSmith Removal & PS→Feeds Cutover | **Engine live in prod (v2.15.0–v2.16.1); migration ongoing per-account.** A reusable **cutover engine**: deterministic **symmetric PS-side cutoff** (PS skips rows ≥ a fed account's `promote_from_date`), **source-aware reconcile** (cash→calibrate, brokerage→MTM cat-88 with the 15% phantom-gain guard) + a read-only drift monitor, per-mapping **`feed_sign`** (migration 029) for Plaid/US negative-liability cards, and **sync-before-reconcile** (pull fresh feed data first). **Cut over (13):** PKO ×6, Fidelity ×5, Caixa EUR, **Luxury card 62** (first Plaid/US card). OCME 45 = **manual/CR025** (loan receivable, not a bank account — the "map 45→Pekao feed" plan retired). **Backlog + owner-confirmed dispositions + live exit monitor** (`ps-exit-monitor.js`): [CR023_PS_MIGRATION_TRACKER.md](CR023_PS_MIGRATION_TRACKER.md) — 13 still PS-dependent (8 US accounts → Fintable feeds; Wise/Revolut best-effort; OCME 45 + dormant holdings → manual/CR025). Incremental account-paced **full retirement**; actual PS removal = **deferred tail** (data-preserving runbook + exit criteria). Parked 5564/Individual ignored; forecast-29 out of scope; CR020 separate. **Migration 029** (`feed_sign`). |
| [CR025](CR025_MANUAL_TRANSACTION_ENTRY.md) | OPEN | Manual Transaction Entry | New `/manual-entry` page (card on the `/transactions` landing) to hand-enter a single **actual** transaction (`source='manual'`, `accepted=TRUE`); reuses `AccountPicker`/`CategorySelector`/FX helpers, stays open for rapid entry. Mostly frontend; one small backend gap (make `accepted` insertable in `create()`). |
| [CR026](CR026_UI_REVAMP.md) | IN-PROGRESS | UI Revamp — Sidebar, Design Refresh, Mobile, Help | **P1 (sidebar) + P2 (dark mode) shipped v2.16.0; UI turned ON for prod v3.0.0.** Collapsible VS Code-style sidebar + top strip behind `VITE_NAV_LAYOUT` (default legacy; dev=sidebar); IA regroup; `[data-theme]` dark mode + `--info`/`--on-accent` tokens + ~40 CSS files token-migrated. Preview at `/ui-preview`. Remaining: P3 (⌘K/help), P4 (mobile expansion), §6 WCAG audit, deferred dark-migration (QuickenImport/TransferAnalysis). Auth/multi-tenancy split to a future CR027. |
| [CR028](CR028_SECURITIES_TRADE_NEUTRALIZATION.md) | COMPLETED | Securities-Trade Neutralization & Transfer-Orphan Management | **Shipped v2.16.2–v3.0.1 (2026-06-05).** Makes brokerage neutralize feed-aware: `repo.neutralize` **pairs** an existing offsetting leg (no new row) instead of always inserting a mirror that double-counts feed pairs; a `dryRun` preview + **ConfirmModal warning** before any insert (Ledger selection-bar button). On the **Transfer Analysis** page, unmatched rows get **Remove** (delete an `auto-offset` orphan — the reliable orphan signal; required exposing `t.source` in `findTransfers`) or **Neutralize** (create the offset for a genuine single leg). Per-account **reconcile-mode toggle** (bank/calibrate ⇄ brokerage/mtm → DRIFT vs MTM GAP) via `PATCH /bank-feed/reconcile-mode/:accountId`. Shared `components/ConfirmModal/`. Cleaned the one orphan (+$41,749.13 → Fidelity Options +$42,164 drift back to +$415). |
| [CR029](CR029_FINTABLE_SHEET_PRUNING.md) | PLANNED | Fintable Sheet Pruning (bank-feed admin action) | Guarded "Prune sheet…" admin action in the **bank-feed** service: hard-deletes Transactions-tab rows older than a cutoff (default ≥4 months, beyond the ~90-day bank window) after proving each is mirrored in Postgres `feed_transactions`. Keeps Fintable's append-only sheet under the 10M-cell / sync-slowdown ceiling. Postgres-only archive (DB `raw` JSON is the record). Phase 0 prereqs: grant service-account Editor + read-write scope, and a manual Fintable-tolerates-deletion test. |
| [CR024](CR024_FIDELITY_FEEDS.md) | COMPLETED | Fidelity Feeds: market-value balances + investment-activity cash flow | Stood up the 6 Fidelity (SnapTrade) feeds CR022 left ignored. **Phase 1 (v2.11.0):** `feed_balances` read-override for correct brokerage market-value balances (no lot tracking). **Phase 2 (v2.13.0, 2026-06-03):** imports + categorizes the activity stream by SnapTrade `activity_type` (interest/dividend income; per-account `trade_treatment` for option-vs-equity buy/sell; contribution/withdrawal as matchable transfers; net-zero plumbing suppressed), activated via a per-account cutover cutoff (zero double-count). Both phases on prod. Migrations `025`/`026`/`027`. Resolved Known Issue #4. Remaining: review-queue triage of promoted rows; lot/cost-basis stays CR020. |
