# CR Index

Status legend: **COMPLETED** · **IN-PROGRESS** · **OPEN** · **PLANNED** (scoped, not yet started) · **SUPERSEDED** / **OBSOLETE** (replaced or no longer relevant)

Each CR file's first line carries its status and links back to the matching anchor in [NEXT_STEPS.md](../FC_NEXT_STEPS.md). New CRs get the next available number.

**Version track:** **v4** = the unreleased multi-tenancy line — **CR027** + its sub-CRs **CR027A–E** (their titles carry a `[v4]` tag; built flag-gated/dormant on `main` — see [DEV_WORKFLOW.md](../DEV_WORKFLOW.md)). **Everything else is v3 / current.** The **Track** column marks each row; file names keep the stable `CRNNN` id (version lives in metadata + the title tag, not the path).

## Summary by status

*Manual roll-up — update when a CR's status changes (alongside its row + CR-file header).*

| Status | Count | CRs |
|--------|------:|-----|
| COMPLETED | 24 | CR001–CR013, CR016, CR017, CR018, CR024, CR025, CR026, CR028, CR030, CR031, CR032, CR033 |
| IN-PROGRESS | 3 | CR019, CR022, CR023 |
| OPEN | 2 | CR020, CR021 |
| PLANNED | 2 | CR027 *(v4, umbrella)*, CR029 |
| SUPERSEDED | 1 | CR014 |
| OBSOLETE | 1 | CR015 |
| **Total** | **32** | |

## All CRs

| # | Status | Track | Title | One-line description |
|---|--------|-------|-------|----------------------|
| [CR001](CR001_MIGRATION_MONGO_TO_POSTGRES.md) | COMPLETED | v3 | MongoDB → PostgreSQL Migration | Moved all storage to PostgreSQL 16; V1 routes and `coa.json` removed. |
| [CR002](CR002_FRONTEND_REFACTOR.md) | COMPLETED | v3 | Frontend Architecture Refactor | God components decomposed; `features/` module pattern; ~22 duplicate transaction files deleted. |
| [CR003](CR003_FORECAST_MODULE.md) | COMPLETED | v3 | Forecast Module | Engine + UI for BS modules and inc/exp items across all phases (1, 2A, 2B, 3, 4, 5). |
| [CR004](CR004_FC_LINES_MAPPING.md) | COMPLETED | v3 | FC Inc/Exp Mapping Layer | User-defined FC Lines decouple budget categories from forecast outputs (Phase 2B, migration 007). |
| [CR005](CR005_CASH_SWEEP.md) | COMPLETED | v3 | Cash Sweep & Auto-Balance | Iterative sweep into a designated module with income↔sweep convergence loop (migrations 012–013). |
| [CR006](CR006_AI_REVIEW.md) | COMPLETED | v3 | AI Review of FC Plan | Conversational review via local `ocr-llm` gateway; async with polling + browser notifications (migrations 014, 020). |
| [CR007](CR007_PWA_MOBILE_SHELL.md) | COMPLETED | v3 | PWA & Mobile Simplified Shell | Installable PWA + dedicated `/m/*` mobile experience with bottom tab bar and 5 mobile pages. |
| [CR008](CR008_HIERARCHY_FILTER.md) | COMPLETED | v3 | HierarchyFilter & Transaction Pages Redesign | Two-stage cascading filter; full redesign of `/trans-actual` and `/trans-budget`. |
| [CR009](CR009_TRANSFER_ANALYSIS.md) | COMPLETED | v3 | Transfer Analysis + Manual Match Groups | Auto + manual matching of transfer pairs; `transfer_matched` flag (migrations 005–006). |
| [CR010](CR010_COA_MANAGEMENT.md) | COMPLETED | v3 | COA Management Redesign + Move | Tree view editor with toolbar, inline actions, quick-add, and Move modal. |
| [CR011](CR011_SOURCE_MAPPINGS.md) | COMPLETED | v3 | Source Mappings (Category + Account) | Decouples external system names from internal app names (migrations 018–019). |
| [CR012](CR012_OPENING_BALANCE_CALIBRATION.md) | COMPLETED | v3 | Opening Balance Calibration | Accurate balance sheet via `opening_balance + SUM(transactions)` (migration 016). |
| [CR013](CR013_COLLAPSE_CATEGORIES.md) | COMPLETED | v3 | Collapse `categories` Table into `accounts` | Single COA source of truth; FK columns repointed; legacy table dropped (migration 021). |
| [CR014](CR014_POCKETSMITH_REPLACEMENT.md) | SUPERSEDED | v3 | PocketSmith Replacement | Dual-provider in-app integration plan; superseded by [CR021](CR021_BANK_FEED_SERVICE.md) (microservice approach). |
| [CR015](CR015_PS_REEXPORT.md) | OBSOLETE | v3 | Re-export Changes Back to PocketSmith | One-way push to PS; obsoleted by [CR021](CR021_BANK_FEED_SERVICE.md) (PS removed entirely). |
| [CR016](CR016_FRONTEND_TEST_FRAMEWORK.md) | COMPLETED | v3 | Frontend Test Framework (Vitest) | Vitest + 96 unit tests across 5 helper modules; closes the largest backend/frontend test gap. |
| [CR017](CR017_CASH_SWEEP_PHASE_C.md) | COMPLETED | v3 | Cash Sweep Phase C — Multi-Module Priority | **Released v3.0.25 (2026-06-09).** Withdraw from multiple modules in priority order on shortfall; deposit excess into priority-1. Nullable `cash_sweep_priority` (migration `031`, backfills the legacy single target → priority 1; applied dev+prod), engine `backupModules[]` cascade (single-module path unchanged, 10/10 tests), priority input on the module edit modal, source/dest **Modules** column in the Cash Sweep Summary. Dev end-to-end verified (forced shortfall drains primary → cascades to backup). |
| [CR018](CR018_BALANCE_TRENDS.md) | COMPLETED | v3 | Balance Trends Report | `/balance-trends` shows month-end USD balances for selected BS accounts over a chosen period; reuses `HierarchyFilter` + `PeriodSelector`. |
| [CR019](CR019_QUICKEN_IMPORT.md) | IN-PROGRESS | v3 | Quicken Historical Import | One-time per-account backfill of pre-2022 Quicken history into the live ledger (cash; investment side descoped to value-only). Phases A–E on dev+prod. **Prod cutover underway via a per-account delete-and-replace loop** (delete `pocketsmith` rows `<2022-12-01` → import QIF → set `pocketsmith.promote_from_date='2022-12-01'` guard per CR023 → calibrate to feed/PS); **PKO done**. The §23 `copy-quicken-to-prod`/`retire-handoff`/Fidelity-historical-container path is **superseded** by this live loop (see §24). Owns the investment schema CR020 depends on. |
| [CR020](CR020_STOCK_INVESTMENT_MODULE.md) | OPEN | v3 | Stock Investment Module | Lot-level holdings tracking + portfolio analytics; supplements (doesn't replace) cash ledger. Depends on CR019 (Quicken Import) shipping first with shared schema. Planning skeleton. |
| [CR021](CR021_BANK_FEED_SERVICE.md) | OPEN | v3 | Bank Feed Service | Standalone microservice for Plaid/banksync.io + Excel ingestion behind a versioned REST contract; sets up the v3 PocketSmith cutover. Supersedes CR014. |
| [CR022](CR022_BANK_FEED_PARALLEL_IMPORT.md) | IN-PROGRESS | v3 | Bank Feed Parallel Import | Additive second import route into fin's `transactions` table with `source='bank-feed'`, running alongside PocketSmith. One migration (`023_bank_feed_import.sql`), new staging table, dev walkthrough gate, automated tests. **Phases A–F DONE** (incl. **R1** per-account opt-in `account_source_mappings.ignored`/unmapped=pending and **R2** cross-source dedup, `BANK_FEED_DEDUP_ENABLED`); Phase F executed in prod 2026-06-02 (migrations 023+024, v2.8.2, 6 PKO accounts mapped + first promote seeding the ledger). **Now in Phase G** — ≥1-month parallel-run observation (`ps_only` holds at 0) before leaning off PS. PS removal deferred to CR023. |
| [CR023](CR023_POCKETSMITH_REMOVAL.md) | IN-PROGRESS | v3 | PocketSmith Removal & PS→Feeds Cutover | **Engine live in prod (v2.15.0–v3.0.10); migration ongoing per-account.** A reusable **cutover engine**: deterministic **symmetric PS-side cutoff** (PS skips rows ≥ a fed account's `promote_from_date`), **source-aware reconcile** (cash→calibrate, brokerage→MTM cat-88 with the 15% phantom-gain guard) + a read-only drift monitor, per-mapping **`feed_sign`** (migration 029) for Plaid/US negative-liability cards, and **sync-before-reconcile** (pull fresh feed data first). **Feed side complete (28 fed / 2 manual)** — early cutovers: PKO ×6, Fidelity ×5, Caixa EUR, **Luxury card 62** (first Plaid/US card); the US-card/Wise/Revolut tail since folded in (per CR030). OCME 45 = **manual/CR025** (loan receivable, not a bank account — the "map 45→Pekao feed" plan retired). **Backlog + owner-confirmed dispositions + live exit monitor** (`ps-exit-monitor.js`): [CR023_PS_MIGRATION_TRACKER.md](CR023_PS_MIGRATION_TRACKER.md) — 13 still PS-dependent (8 US accounts → Fintable feeds; Wise/Revolut best-effort; OCME 45 + dormant holdings → manual/CR025). Incremental account-paced **full retirement**; actual PS removal = **deferred tail** (data-preserving runbook + exit criteria). Parked 5564/Individual ignored; forecast-29 out of scope; CR020 separate. **Migration 029** (`feed_sign`). |
| [CR025](CR025_MANUAL_TRANSACTION_ENTRY.md) | COMPLETED | v3 | Manual Transaction Entry | **Shipped v3.0.4 (2026-06-05).** `/manual-entry` page + **Manual Entry** card on the `/transactions` landing — hand-enter a single **actual** transaction (`source='manual'`, `accepted=TRUE`); reuses `AccountPicker`/`CategorySelector`/FX helpers (blank base + Save-block on no FX rate), stays open for rapid entry (sticky account/date/currency). Backend: `repo.create()` now inserts `accepted` (default TRUE for manual, FALSE for importers). No migration. Unblocks the CR023 manual-bucket accounts (OCME 45, holdings). |
| [CR027](CR027_MULTI_TENANCY_FINAL_RELEASE.md) | PLANNED (umbrella) | **v4** | Multi-Tenancy & Final-Release Readiness `[v4]` | **Umbrella/program doc — direction approved, NOT approved as one CR; split into sub-CRs CR027A (tenancy foundation) / B (auth+provisioning) / C (owner cutover) / D (onboarding+demo+importers) / E (release cleanup).** Turn the single-user app into a shared multi-user SaaS + ship a clean release. **Schema-per-tenant** (`tenant_<id>` schemas + `shared` for `exchange_rates` + a `public` control plane) so existing SQL runs unchanged via per-request `search_path` (no `tenant_id` column, no RLS). **Admin-invite email+password** auth (bcrypt, httpOnly cookie, locked CORS; no public signup). **Synthetic fictional demo DB** (`tenant_demo`). **Per-section help + setup instructions** (extends CR026-P3 `HelpPanel`). **Data-source IA**: Ongoing (Fintable/Excel/Manual) vs Migrate-once (Quicken/PocketSmith/Other) — **PocketSmith demoted from live feed to one-time CSV import**. **Legacy retirement** (Mongo scripts, UI-preview mockup, PS-API code per CR023, secrets out of `.env-cmdrc`). **Dual-track workflow:** built flag-gated/dormant on `main` (`FIN_MULTI_TENANT`/`AUTH_ENABLED`), isolated v4 runtime (`docker-compose.v4.yml`, :3205) — see [DEV_WORKFLOW.md](../DEV_WORKFLOW.md). Needs a real **migration runner** (none today). After CR025. |
| [CR026](CR026_UI_REVAMP.md) | COMPLETED | v3 | UI Revamp — Sidebar, Design Refresh, Mobile, Help | **DONE — shipped & ON in prod (v3.0.0; rail flyouts v3.0.7).** Collapsible VS Code-style sidebar (+ collapsed-rail flyouts) + top strip; IA regroup; `[data-theme]` dark mode (100% coverage, D1–D12 fixes, §6 WCAG safe fixes); ⌘K palette + help drawer; mobile read pages (Overview/Balance-Trends/Ledger). **Moved to CR027** (commercialization-gated): P5 onboarding, per-section help, brand-affecting WCAG (§15). **Backlog (optional polish):** inline glossary tooltips, actionable empty states, mobile microinteractions. |
| [CR028](CR028_SECURITIES_TRADE_NEUTRALIZATION.md) | COMPLETED | v3 | Securities-Trade Neutralization & Transfer-Orphan Management | **Shipped v2.16.2–v3.0.1 (2026-06-05).** Makes brokerage neutralize feed-aware: `repo.neutralize` **pairs** an existing offsetting leg (no new row) instead of always inserting a mirror that double-counts feed pairs; a `dryRun` preview + **ConfirmModal warning** before any insert (Ledger selection-bar button). On the **Transfer Analysis** page, unmatched rows get **Remove** (delete an `auto-offset` orphan — the reliable orphan signal; required exposing `t.source` in `findTransfers`) or **Neutralize** (create the offset for a genuine single leg). Per-account **reconcile-mode toggle** (bank/calibrate ⇄ brokerage/mtm → DRIFT vs MTM GAP) via `PATCH /bank-feed/reconcile-mode/:accountId`. Shared `components/ConfirmModal/`. Cleaned the one orphan (+$41,749.13 → Fidelity Options +$42,164 drift back to +$415). |
| [CR029](CR029_FINTABLE_SHEET_PRUNING.md) | PLANNED | v3 | Fintable Sheet Pruning (bank-feed admin action) | Guarded "Prune sheet…" admin action in the **bank-feed** service: hard-deletes Transactions-tab rows older than a cutoff (default ≥4 months, beyond the ~90-day bank window) after proving each is mirrored in Postgres `feed_transactions`. Keeps Fintable's append-only sheet under the 10M-cell / sync-slowdown ceiling. Postgres-only archive (DB `raw` JSON is the record). Phase 0 prereqs: grant service-account Editor + read-write scope, and a manual Fintable-tolerates-deletion test. |
| [CR032](CR032_CORE_CASH_SWEEP_NEUTRALIZATION.md) | COMPLETED | v3 | Fidelity Core-Cash Sweep Auto-Neutralization | **Released v3.0.27 (2026-06-10).** Promote now auto-mirrors Fidelity core-cash sweeps (`REDEMPTION FROM`/`PURCHASE INTO CORE ACCOUNT` — SPAXX/FDIC/Cash Reserves), whose core-position counter-leg the feed never delivers, so each sweep self-nets instead of inflating the reconciled balance (a +$30k/+$34.5k MTM-GAP on Fidelity Options). Categorizer gains description-based detection → `transfer-mirror` action; promote inserts the leg (auto-accepted) + a negated `auto-offset` mirror; `neutralize` guard stops mis-pairing a sweep against a real trade (CR028 follow-up). Backfill script (`backfill-cr032-core-sweeps.js`, report-first) for pre-CR032 rows: prod dry-run = 27 mirrored / 1 lone / 4 needs-review. Backfill intentionally NOT applied (history left as-is per owner). 206/206 backend tests green. No migration. |
| [CR033](CR033_MANUAL_CALIBRATION.md) | COMPLETED | v3 | Manual Calibration (non-fed accounts) | **Released v3.0.29 (2026-06-11).** Non-fed twin of Balance Calibration: per balance-sheet account with no bank feed, compare computed balance vs a user-typed current balance, with the same calibrate/MTM reconcile action. New `manual_balances` table + `accounts.manual_reconcile_mode` (migration 032, dev+prod), `/api/v2/manual-calibration/*`, `/manual-calibration` page. 220/220 backend tests. |
| [CR031](CR031_LEDGER_FILTER_PARITY_YEAR_RANGE.md) | COMPLETED | v3 | Ledger Filter Parity + Period Year-Range | Ledger adopts the Transactions/Budget filter format — single-select `HierarchyFilter` account picker (currency suffix) + always-visible `PeriodSelector` (default This Year). Custom period gains **Year-from / Year-to** (`enableYearRange`) across Ledger/Transactions/Budget via a year-range-aware `buildDateRangeParams` + `toYear` filter. Backend `GET /budget/actual-entries` accepts `fromDate`/`toDate` so Actual totals span the range. New `HierarchyFilter` `singleSelect` mode. **Released v3.0.12 (2026-06-06)**; no migration. |
| [CR024](CR024_FIDELITY_FEEDS.md) | COMPLETED | v3 | Fidelity Feeds: market-value balances + investment-activity cash flow | Stood up the 6 Fidelity (SnapTrade) feeds CR022 left ignored. **Phase 1 (v2.11.0):** `feed_balances` read-override for correct brokerage market-value balances (no lot tracking). **Phase 2 (v2.13.0, 2026-06-03):** imports + categorizes the activity stream by SnapTrade `activity_type` (interest/dividend income; per-account `trade_treatment` for option-vs-equity buy/sell; contribution/withdrawal as matchable transfers; net-zero plumbing suppressed), activated via a per-account cutover cutoff (zero double-count). Both phases on prod. Migrations `025`/`026`/`027`. Resolved Known Issue #4. Remaining: review-queue triage of promoted rows; lot/cost-basis stays CR020. |
| [CR030](CR030_AUTOMATED_PS_RETIREMENT.md) | COMPLETED | v3 | Retire automated PocketSmith (keep one-time CSV upload) | **Released v3.0.11 (2026-06-06).** With every feed-able account on a direct bank feed (per CR023), removed the **automated** PS API integration + legacy PS calibration — `refreshPsApiV2.js`/PS SDK/API converter, `POST /refresh-ps`, `/map-ps-accounts` + `/calibrate` + `/calibration-status`, the Refresh-PS/Accept-PS/legacy-calibration UI (+ mobile `/m/refresh-ps`), and the `/calibrate` test. **Kept** the one-time PS **CSV upload** path (`/upload-ps`, `psCsvIngestorV2.js`, `psdata_staging`) + all historical `source='pocketsmith'` rows (**frozen, never deleted**). Safe: no PS cron existed; shared bank-feed UI edits were surgical; `vite build` passes. No migration. |
