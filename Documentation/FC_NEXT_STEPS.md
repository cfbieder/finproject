# Development Plan (FC_NEXT_STEPS.md)

Living plan for the Fin project — open Change Requests, known issues, ongoing improvement themes, and a chronological history. Companion to [FC_PROJECT_STRUCTURE.md](FC_PROJECT_STRUCTURE.md), which describes the *current* state of the project.

For the full CR list with one-line descriptions, see [CRs/CR_INDEX.md](CRs/CR_INDEX.md).

---

## 1. Change Requests

Each CR is a self-contained markdown file under [CRs/](CRs/). The first line of each CR carries its status. The anchors below are the deep-link targets used from each CR file's "Plan" link.

### 1.1 Open / In-Progress

<a id="cr027"></a>
- **CR027 — [Multi-Tenancy & Final-Release Readiness](CRs/CR027_MULTI_TENANCY_FINAL_RELEASE.md)** — *PLANNED — umbrella/program doc (scoped 2026-06-06; eng review applied 2026-06-06). Direction approved; **NOT approved for implementation as a single CR** — split into **CR027A** (tenancy foundation: template + migration runner + per-request DB scoping), **CR027B** (auth + provisioning + security hardening), **CR027C** (owner-data cutover), **CR027D** (onboarding + synthetic demo + help + Excel importer), **CR027E** (data-source IA + legacy retirement + secrets gate).* Converts the single-user app into a shared multi-user SaaS and bundles the cleanup into a shippable release. **Step 0 — dual-track workflow** (CR §"Step 0"): v4 stays **trunk-based on `main` behind flags** (`FIN_MULTI_TENANT`/`AUTH_ENABLED`, default OFF, shipped dormant like CR026) and **backward-compatible** (no tenant context ⇒ `search_path=public` ⇒ today's behavior) so v3 tweaks carry into v4 with **no merge tax**; a **fully-isolated v4 runtime stack** (`docker-compose.v4.yml` + `Scripts/v4-up.sh` + `Scripts/sync-db-prod-to-v4.sh`: ports 5435/3205/5275, own `postgres_data_v4` volume, flags ON) runs v4 in parallel without touching prod/dev DBs — launched straight from the main tree (no worktree needed since code is on `main`; a git worktree is on-demand only for the short-lived CR027C/E branches); go-live = flip flags + one-time cutover, not a big-bang merge. **Review-driven hard gates added:** (i) a **DB-access audit** — `quickenImport.js` queries `db.getPool()` directly and `db.transaction()`/`getClient()` acquire a fresh client with no `search_path` (≥10 sites: transactions/budget/forecast/transferMatchGroups/refreshBankFeedV2/reconcileToFeed) → cross-tenant leak unless made request-aware; standalone scripts each `new Pool()` and need an explicit `--tenant`; CI grep gate + tests required; (ii) **migration strategy** — today migrations run only via docker `initdb` (empty-volume) and `deploy-to-production.sh` has **no migrate step**; template must be **machine-generated** (`pg_dump --schema-only`, capturing types/sequences/views/functions, not a hand-squash that would wrongly resurrect the `021`-dropped `categories`) with fresh-vs-existing baseline detection + rehearsed rollback; (iii) **auth security spec** — bcrypt/argon2, revocable server-side sessions, `httpOnly`/`Secure`/`SameSite` cookies, CSRF, expiring single-use invite/reset tokens, failed-login lockout, admin/member role boundary, default-deny on all `/api/v2/*` except an allowlist; (iv) **secrets release gate** — `findev123` compose default + tracked PS keys/feed UUIDs must not ship (`.env.example` + CI secret-scan). **Owner decisions:** (1) **schema-per-tenant** — each user gets a `tenant_<id>` Postgres schema, a `shared` schema holds only `exchange_rates`, and a `public` control plane holds `users`/`tenants`/`schema_migrations`; isolation is per-request `SET search_path = tenant_x, shared`, so the ~12 repositories' hand-written SQL runs **unchanged** (chosen over a `tenant_id` column + RLS to avoid query churn and leak risk); (2) **admin-invite email+password** auth (bcrypt, httpOnly cookie session, `helmet`, rate-limited login, **locked-down CORS** — currently `origin:true,credentials:true`); no public signup; (3) **synthetic fictional demo DB** (`tenant_demo`) via a seeded generator — never a copy of real finances; (4) data-source IA split into **Ongoing** (Fintable feed / Excel upload / Manual entry) vs **Migrate-once** (Quicken / PocketSmith / Other) — **PocketSmith demoted from a live feed to a one-time CSV importer** (live PS-API refresh removed from the UI); (5) **per-section help + setup instructions** extending the CR026-P3 `HelpPanel`; (6) **legacy retirement** (Mongo scripts, `/ui-preview` mockup, PS-API code per CR023 sunset, de-personalize seed scripts, secrets out of tracked `.env-cmdrc`). **Largest single piece:** a real **migration runner** (today migrations only auto-run once via `docker-entrypoint-initdb.d`; schema-per-tenant forces per-schema fan-out). **Risks:** connection-pool pressure (dedicated client per request), `search_path` discipline as the security boundary, the one-time owner-data→`tenant_owner` cutover. **Phasing:** P0 schema tooling+runner → P1 per-request DB scoping (AsyncLocalStorage) → P2 auth+provisioning → P3 owner cutover → P4 demo dataset → P5 help → P6 IA cleanup+legacy retirement → P7 (later) per-tenant crons, 2FA. **Sequencing:** after **CR025** (manual entry is the feed-less onboarding path); coordinates with **CR023** (PS removal) and builds on **CR026** (sidebar/help/tenant-switcher slot).

<a id="cr030"></a>
- **CR030 — [Retire automated PocketSmith (keep one-time CSV upload)](CRs/CR030_AUTOMATED_PS_RETIREMENT.md)** — *Released v3.0.11 (2026-06-06)*. With every feed-able account on a direct bank feed (CR023: 28 fed / 2 manual), removed the **automated** PocketSmith integration: PS-API txn fetch (`refreshPsApiV2.js`, `retrieval/pocketsmith.js`, `psdataConverter.js`), `POST /ingest-ps/refresh-ps`, the legacy PS calibration routes (`/map-ps-accounts`, `/calibrate`, `/calibration-status`) + their UI (Refresh PS Data / Accept PS buttons, the "PocketSmith calibration (legacy)" section, `MobileRefreshPS`). **Kept** the one-time **PS CSV upload** (`UploadPS`, `/upload-ps`, `/analyze-ps`, `/clearall`, `/sync-to-transactions`) and all frozen historical `source='pocketsmith'` data. Reconciliation is now feed-driven (`reconcileToFeed`). Safe: no PS cron; surgical edits to the shared Refresh-Feeds & Balance-Calibration pages; `vite build` green. **Follow-ups:** drop unused `PS_API_KEY`/`PS_USER_ID` env; future CR may relocate/remove the CSV upload per CR027-E.

<a id="cr029"></a>
- **CR029 — [Fintable Sheet Pruning (bank-feed admin action)](CRs/CR029_FINTABLE_SHEET_PRUNING.md)** — *PLANNED (scoped 2026-06-06, implement later)*. A guarded **"Prune sheet…"** admin action in the **bank-feed** service (separate gitignored repo) that hard-deletes Transactions-tab rows older than a cutoff to keep Fintable's append-only Google Sheet under the **10M-cell** / sync-slowdown ceiling. Safe because the sheet is a relay buffer — every row is already mirrored, deduplicated, into Postgres `feed_transactions` (`external_id` = GoCardless hash) with the **full Raw Data JSON in `feed_transactions.raw`**; consumers read Postgres, never the sheet. **Owner decision:** **Postgres-only** archive (no archive tab/spreadsheet). Flow: sync first → scan rows with `Date < cutoff` (default ≥4 months, beyond the ~90-day bank window so neither Fintable nor a sync re-introduces them) → **refuse to delete any row not confirmed in `feed_transactions`** → `batchUpdate DeleteDimension` bottom-up, Transactions tab only, never header/Accounts. Surfaced as a button by "Sync now" with a **mandatory dry-run preview** before the real delete; audited as a `sync_jobs` row (`trigger='prune'`). **Phase 0 prereqs (do first):** (1) re-share the sheet with the service account as **Editor** + flip `googleSheets.js` `SCOPES` `spreadsheets.readonly`→`spreadsheets`; (2) **manual test** — hand-delete a batch of old rows, sync, confirm Fintable neither re-adds nor duplicates (abort criterion if it does). **Non-goals:** archiving outside Postgres, pruning the Accounts tab or the `feed_transactions` table, scheduled/automatic pruning.

<a id="cr026-ui-revamp"></a>
- **CR026 — [UI Revamp: Sidebar, Design Refresh, Mobile, Help](CRs/CR026_UI_REVAMP.md)** — *✅ COMPLETED — shipped & ON in prod: sidebar/dark/⌘K/help v3.0.0, mobile read pages v3.0.5–6, rail flyouts v3.0.7*. **Shipped:** a collapsible VS Code-style **sidebar** (`components/Sidebar/`) + top utility strip (`components/TopStrip.jsx`) replacing the top nav, behind a `VITE_NAV_LAYOUT` flag — **default `legacy` (top bar) in docker/prod, default `sidebar` on dev**, per-browser override via `localStorage.navLayout`. IA regrouped (Overview · Accounts & Transactions · Budget · Forecast · Reports · — · Data Sources · Settings) via additive `SIDEBAR_GROUPS`/`getSidebarNav()` in `routes.jsx` (legacy nav + landing pages untouched). **Dark mode** (P2): a `[data-theme="dark"]` token-override layer in `index.css` + a `useTheme` store (default light, persisted, no-FOUC `index.html` script) + Theme toggles in the sidebar footer & top strip; added `--info` (purple) and `--on-accent` (filled-button text) semantic tokens; ~40 page/component CSS files migrated off hardcoded hex onto tokens (fixing several latent dead undefined-token fallbacks along the way). Non-functional preview at `/ui-preview` (+ standalone `Documentation/CRs/CR026_UI_PREVIEW.html`) for look-and-feel review. **Prod-safe:** the whole feature is flag-gated OFF in prod (legacy top bar, dark mode unreachable) — this release ships it dormant. **Dark-mode defect pass (2026-06-05, browser-verified, local/uncommitted):** real-Chromium verification of all 33 routes found 12 dark-mode defect classes (D1–D12) — hardcoded white `rgba()` gradients + inline JS styles + solid light fills the hex-only migration grep never caught; all repointed to tokens (`--surface-elevated`/`--surface-muted`/`--bg-tertiary`/semantic badge tokens), light mode unchanged, re-audit clean (see CR026 §14). **§6 WCAG AA contrast audit (2026-06-05, local/uncommitted):** token-level audit, both themes — dark theme passes AA; light-theme (original brand palette) carries the debt. **Safe fixes applied** to `index.css` (darkened `--muted`, badge `*-strong` tokens, `--chart-amber`; de-duplicated `--chart-indigo` vs `--chart-purple` for colorblind separation). **Deferred brand-affecting failures (logged, need a design call):** white-on-filled-button text (3.7/2.8:1), `--warning` text (2.65:1), colored financial body numbers (<4.5 at body size), input borders (<3:1), residual chart CVD overlap — see CR026 §15. **Done (CR026 COMPLETE):** P3 (⌘K palette + help drawer) shipped; P4 mobile shipped (data **Overview** + **Balance Trends** + **Ledger** read pages + tab-bar revisit); collapsed-rail **flyouts** (v3.0.7); `QuickenImport.css`+`TransferAnalysis.css` migrated → **100% dark coverage**; §6 WCAG safe fixes applied. Carried forward — **→ CR027** (commercialisation-gated): P5 onboarding, per-section help, brand-affecting WCAG (§15). **→ backlog** (optional polish): inline glossary tooltips, actionable empty states, mobile microinteractions (§7.4). Commercialisation (auth/multi-tenancy/de-personalisation) is split to a future **CR027**; this CR ships only hooks (brand-token extraction, reserved tenant-switcher slot, flag-gated onboarding).

<a id="cr025"></a>
- **CR025 — [Manual Transaction Entry](CRs/CR025_MANUAL_TRANSACTION_ENTRY.md)** — *SHIPPED to prod (v3.0.4, 2026-06-05)*. A dedicated `/manual-entry` page — reached via a new **Manual Entry** card on the `/transactions` landing — to hand-enter a single **actual** transaction into `transactions` (`source='manual'`, `accepted=TRUE`). Almost entirely frontend: `POST /api/v2/transactions` → `repo.create()` already exists and reusable form pieces (`AccountPicker`, `CategorySelector`, `TransactionDateSelector`, `computeTransactionBaseAmount`) are in place; the card is one route entry in `routes.jsx`. **Two backend gaps to close:** (1) `create()`'s INSERT omits `accepted`, so a manual row defaults `accepted=FALSE` and is sweepable by a feed refresh — make `accepted` insertable, defaulting `TRUE` for `source='manual'`; (2) the POST route passes `req.body` straight through (no v1→v2 transform), so the form must send snake_case (no route change). After a successful save the form **stays open** (account/date/currency sticky, amount/description/category cleared, focus back to amount) for rapid sequential entry. **Out of scope:** budget transactions (separate flow), bulk/CSV entry, editing existing rows (bulk-edit modal already covers it). No migration.

<a id="cr023"></a>
- **CR023 — [PocketSmith Removal & PS→Feeds Cutover](CRs/CR023_POCKETSMITH_REMOVAL.md)** — *Engine live in prod (v2.15.0–v2.16.1, 2026-06-04/05); migration ongoing per-account*. **Live on prod:** the cutover engine (`reconcile_mode`, source-aware `reconcileToFeed` with the phantom-gain guard, PS-side cutoff), the read-only bank-recon monitor, and the §G PS-rec scoped to non-cut-over accounts. **MTM activated** on the 4 Fidelity brokerage accounts (Bond basis-anchored to cost basis; net +$61.6k May Unrealized G/L; overrides removed). **Cash Mgt 30 calibrated** (cash-hub $253k accumulated drift, not a bug). **Caixa cut over** (2026-06-02) → every mapped account now reconciles / is cut over; §G panel empty. **Done since (2026-06-05):** **Luxury card 62 cut over** (first Plaid/US card; per-mapping **`feed_sign`** migration 029 fixed the negative-liability sign bug); **OCME 45 → manual/CR025** (it's a loan receivable, not a bank account — the "map 45→Pekao feed" plan retired); account→app routing live (`?app=fin`); **sync-before-reconcile** (v2.16.1, fail-open); `ps-exit-monitor.js` exit gate; and the [PS→Feeds migration tracker](CRs/CR023_PS_MIGRATION_TRACKER.md) with **owner-confirmed dispositions**. **Open:** the **13 still-PS-dependent accounts** (8 US → add to Fintable feeds, `feed_sign=+1` on the 5 cards; Wise×2/Revolut best-effort feed; OCME 45 + dormant holdings → manual/CR025, gated on **CR025** shipping); the deferred PS-removal tail. As-built detail in the CR doc §10.1–10.5. The endgame for the PS→bank-feed migration, recast as a reusable **cutover engine** rather than a one-off. **State:** PS is OFF for PKO+Fidelity (all 11 mapped accounts cutoff-gated + reconciling to the bank), but PS still feeds ~15 other accounts as the live catch-all. bank-feed now exposes 14 accounts / 3 institutions (PKO, Fidelity, Bank Pekao) and is a **shared read-only microservice** (fin + the OCME app + future consumers, independent per-consumer mapping policy). **Resolved scope (Q&A decisions in CR §0):** (1+8) **source-aware MANUAL calibrate** (fed→`feed_balances`, PS-only→PS, auto-follows migration) + a read-only **source-partitioned** drift monitor (PS-rec for PS-fed accts, bank-rec for fed accts; depopulates over time); (2) the central new code is a **deterministic symmetric PS-side cutoff** — PS promote skips rows ≥ a fed account's `promote_from_date`, giving a clean date handoff by construction and closing the CR022 §G recurring-charge dedup weakness; plus an 8-step per-account runbook; (3) **migrate OCME account 45 onto its Bank Pekao feed** (first migration); genuinely-unfed residual = US cards/Chase/Capital One/Wise/Caixa/Revolut/SP-Panorama → feeds-when-available or manual/Excel; (4) parked PKO 5564 + Fidelity Individual stay **ignored** (out of fin scope); (5) **actual PS removal is a deferred tail** — exit criteria + a **data-preserving** removal runbook (retire PS code/sync only; keep all `pocketsmith` rows + `psdata_staging` frozen) defined, executed in a future CR when the last account is off PS; (6) forecast-on-29 **out of scope** (leaf balances correct → roll-up forecasts correctly); (7) cutover settings stay **script-only** (only the monitor + a per-account "Calibrate to feed" button get UI, on the existing Bank Feed Setup page); (9) **CR020** (lot/cost-basis) kept **separate**. Build footprint: one PS-promote change (cutoff, reuses existing `promote_from_date`), one calibrate change, one new recon endpoint + page section, the OCME migration, tests, docs — **likely no new migration**. End-state = incremental account-paced **full retirement**.

<a id="cr024"></a>
- **CR024 — [Fidelity Feeds: market-value balances + investment-activity cash flow](CRs/CR024_FIDELITY_FEEDS.md)** — *IN-PROGRESS — Phase 1 built + green on dev (2026-06-02); Phase 2 pending bank-feed `activity_type` handoff*. **Phase 1 as-built (dev):** migration `025_fidelity_feeds.sql` (`bankfeed_balances` cache + `account_source_mappings.balance_from_feed`/`trade_treatment`); `ingestBalances()` folds `/v1/balances` into the cache on the cron `/ingest` path; `reports.js` leaf-level read-override (feed value for as-of ≥ 2026-05-30, additive fallback earlier, parents aggregate overridden leaves); idempotent `seed-cr024-fidelity-mappings.js` maps the 5 accounts `balance_from_feed=TRUE` keeping `ignored=TRUE` (tx suppressed till Phase 2); ps-anchor already skips all 5. Verified live on dev: Bond corrected $1.01M→$1.20M, parents roll up, PKO unaffected; 178/178 backend tests. **Prod DEPLOYED + verified (v2.11.0, 2026-06-02):** migration 025 applied before the code deploy (pg_dump bracket), seed run, 13/13 balances cached; balance sheet shows feed values, parents roll up, 0 tx leaked, idempotent; daily 06:00 cron refreshes the cache. **Phase 2 built + dev-verified (2026-06-03):** bank-feed shipped `activity_type` on `/v1/transactions`; fin migration `026_fidelity_activity.sql` (`bankfeed_staging.activity_type`/`.suppressed`), `categorizeFidelityActivity` routes income/transfer/suppress/review, promote assigns `category_id` (still `accepted=FALSE`). Dev: 233 inserted, 130 suppressed, categorization exact; 188/188 tests. **Phase 2 ACTIVATED on prod (v2.13.0, 2026-06-03)** via a per-account cutover cutoff (migration `027_promote_from_date.sql`, `seed-cr024-fidelity-cutoffs.js` = PS-last-tx+1) instead of blind dedup — the dry-run showed dedup would double-count 26 ambiguous rows (reports count `accepted=FALSE` rows). Activation: promoted **60 Fidelity rows** (Dividend 15, Interest 4, Option Trade 20, Transfer-Securities-Trades 19, Transfer-Bank 2), 33 suppressed, **0 PS rows at/after cutoff → zero double-count by construction**; Phase 1 balances intact; `accepted=FALSE` for review. CR024 is now DONE on prod (both phases); CR020 still owns lot/cost-basis. Stands up the 6 Fidelity (SnapTrade-via-fintable) feeds CR022 deliberately left `ignored` (investment-side not built). Live investigation: every Fidelity account has `Σ(captured tx)=0` / no inception baseline, so `opening_balance + Σ(amount)` cannot derive any balance — the only authoritative number is bank-feed `feed_balances` (forward-only from 2026-05-30); historical market value is unrecoverable from this feed. Rich activity (`type`/`units`/`price`/`option_type`) sits in `feed_transactions.raw.parsed` but is **not** on the `/v1/transactions` contract. **Two layers, one CR (user chose single-CR):** **Phase 1** — `feed_balances` read-override on the balance sheet for the mapped Fidelity accounts (26 IRA / 27 Stocks / 28 Options / 30 Cash Mgt / Fixed-Income→29-or-31 *open*; Individual stays ignored), as-of ≥2026-05-30 else existing additive value unchanged; ingest `/v1/balances`→new `bankfeed_balances` table (no bank-feed change needed); plus ps-anchor cleanup on account 26. **Phase 2** — import + categorize the activity stream by SnapTrade `activity_type`: INTEREST→Interest Income, DIVIDEND→Dividend Income (must, all accounts); BUY/SELL→per-account `trade_treatment` (`offset` default → Transfer-Securities-Trades; `income` → cash-basis option P&L for the Options account); REI→offset; CONTRIBUTION/WITHDRAWAL→matchable Transfer (NOT income/expense — large $190K/$162K inter-account moves); LOAN/JOURNALED/OPTIONEXPIRATION→suppressed (net-zero plumbing); unknown type→review (fail-safe). All rows land `accepted=FALSE` (review queue, pre-filled suggestions). Mapping grounded in the live 338-row activity inventory. **Gated on a `Finance → bank-feed` handoff:** add an additive optional `activity_type` to `/v1/transactions` (populated from `raw.parsed.type`). Migration `025_fidelity_feeds.sql` (`bankfeed_balances`, `account_source_mappings.trade_treatment` + `.balance_from_feed`). **Non-goals:** lot/cost-basis/holdings/FIFO (stays CR020), reconstructing pre-feed historical market value, touching PS. Resolves Known Issue #4. **Mapping resolved:** Fixed Income → leaf 31 Fidelity Bond (29 "Fidelity Fixed Income" is the parent *category* rolling up 30 Cash Mgt + 31 Bond — read-override sits on the leaves, 29 aggregates; not mapped); option P&L → 76 "Option Trade" (near-dup 219 deleted from prod 2026-06-02).

<a id="cr022"></a>
- **CR022 — [Bank Feed Parallel Import](CRs/CR022_BANK_FEED_PARALLEL_IMPORT.md)** — *PHASE G (observing, ≥1 month) — Phases A–F DONE; prod parallel run live since 2026-06-02.* Additive second import route in fin that consumes the CR021 bank-feed `/v1/*` contract and writes into the existing `transactions` table with `source='bank-feed'`, alongside PocketSmith. Shipped: migrations `023_bank_feed_import.sql` (`bank_feed_external_id` partial-unique col, `bankfeed_staging`, `sync_metadata` seed, `account_source_mappings.ignored`) + `024_bank_feed_ignore_unmapped.sql` (drop NOT NULL on `account_source_mappings.account_id` for ignore-without-mapping); converter/repository/orchestrator/route files under `server/src/v2/`; R1 mapping UI (typeahead `AccountPicker` + ignore toggle on the diagnostic page); 167/167 backend tests; `smoke-bank-feed.js` (7/7 live). Reuses the source-agnostic review queue and source-discriminated `account_source_mappings`. **R1** per-account opt-in — unmapped account stages but never promotes (fail-closed); ignore is a standalone skip (no mapping needed). **R2** cross-source dedup — at promote a bank-feed row links onto a matching `source='pocketsmith'` row (`(account_id, ABS(amount), currency)` ±1 day) instead of inserting, id stable; the reverse direction drops the later PS row via **one guarded `NOT EXISTS` in `ingestPs.js` `syncStagingToTransactions`** (the only PS-code edit, behind `BANK_FEED_DEDUP_ENABLED` + a column-existence self-disable — NOT `refreshPsApiV2.js`); `merged_with_ps_count` is the parallel-run health signal. **Dev walkthrough done** (§7.0): 312 live PKO tx staged, R1 fail-closed + ignore-without-mapping verified, 102 rows promoted after UI mapping, idempotent; R2 link path test-covered (PS went stale 2026-03-31 → no live twins). **Phase F DONE (2026-06-02):** prod was confirmed at 022, migrations 023+024 applied inside a pg_dump bracket, v2.8.2 deployed (healthy), smoke 7/7, 6 PKO accounts mapped / 7 ignored (5 Fidelity + business-PLN(5564)), first promote seeded the ledger (134 PS rows linked, 32 bank-feed rows in the review queue), stage-only daily cron added (06:00). Day-one reconciliation `total_ps_only=0`, matched=143 — but only after the fintable sheet history was deepened to cover the 30-day window and bank-feed re-synced (the shallow first read showed a benign old-edge backfill gap). **Now in Phase G observation:** re-read `GET /reconciliation?sinceDays=30` weekly; `ps_only` must hold at 0 for ≥1 month before opening CR023 (PS removal). 32 bank-feed review-queue rows await triage on `/refresh-ps`. **Non-goals:** removing PocketSmith, generalizing `pending_transactions`, modifying the existing `/refresh-ps` *UI*, investment-side schema. PS removal is a separate future CR after ≥1-month parallel run. **Coordinating dependency:** bank-feed's `fintableSync.js` hardcodes the PKO institution onto every account — needs a CR021-side fix before Fidelity arrives. (Bank-feed now exposes 13 accounts: PKO PLN/EUR/USD + Fidelity-side USD; cash-side CR022 maps the PKO accounts.)

<a id="cr021"></a>
- **CR021 — [Bank Feed Service](CRs/CR021_BANK_FEED_SERVICE.md)** — *IN-PROGRESS*. Standalone microservice that replaces PocketSmith, behind a versioned `/v1/*` REST contract for v3 of the main fin app. **Upstream chosen 2026-05-30: fintable.io via Google Sheets** (after Phase 0 rejected banksync.io for no-PKO catalog, moneysheets.io for no-developer-API, GoCardless direct for closed signups; Plaid Production stayed In Review). Repo: [`~/Programs/fin/bank-feed/`](../../bank-feed) (github.com/cfbieder/bank-feed, private). **Phases 0–3 shipped 2026-05-30:** skeleton + Google Sheets adapter + hourly scheduler + real-Sheet smoke (7 PKO accounts, 127 transactions, 7 balance snapshots live in `/v1/transactions`, `/v1/accounts`, `/v1/balances`). Remaining: Phase 4 (Excel/CSV upload for Fidelity), Phase 5 (gap detection + balance reconciliation + stale-feed alerts), Phase 6 (admin UI), Phase 7 (fin main-app integration spike → CR022 v3 cutover). **Update 2026-06-03 (v0.2.0):** `POST /v1/sync` hardened for client-triggered refresh — `?max_age=<minutes>` freshness cap, `?force=true` bypass, and in-flight coalescing (the hourly cron now routes through the same guard). Enables an on-demand "Refresh from bank" button for any consumer; plain call unchanged. Also added a **second consumer**: the OCME app now imports from the same fintable Sheet via the `/v1/*` contract over Tailscale (reachability confirmed from `ocmedev`, shares the existing `BANK_FEED_API_KEY`) — build guide at [Documentation/OCME_BANK_FEED_IMPORT_GUIDE.md](OCME_BANK_FEED_IMPORT_GUIDE.md). A 3rd live institution (Bank Pekao) now flows through the per-institution dispatch.

<a id="cr014"></a>
- **CR014 — [PocketSmith Replacement](CRs/CR014_POCKETSMITH_REPLACEMENT.md)** — *SUPERSEDED 2026-05-28 by [CR021](#cr021)*. Original plan integrated dual providers (GoCardless + Plaid) directly into the main `fin` app. Replaced by the microservice-with-contract approach.

<a id="cr015"></a>
- **CR015 — [Re-export Changes Back to PocketSmith](CRs/CR015_PS_REEXPORT.md)** — *OBSOLETE 2026-05-28*. PocketSmith is being removed entirely via [CR021](#cr021), so re-exporting to it no longer makes sense.


<a id="cr017"></a>
- **CR017 — [Cash Sweep Phase C — Multi-Module Priority Sweep](CRs/CR017_CASH_SWEEP_PHASE_C.md)** — *✅ COMPLETED — Released v3.0.25 (2026-06-09); migration 031 on dev+prod, dev end-to-end verified*. Extends CR005's single sweep target to a **priority-ordered set**: on shortfall, drain the highest-priority module (swept balance → own balance) before cascading into lower-priority backups; excess deposits into priority-1 only. Schema = nullable `cash_sweep_priority` (migration `031`, backfills today's `cash_sweep_target=TRUE` → priority 1, legacy boolean kept in sync as "priority==1"). Engine [`cash-sweep.js`](../server/src/services/forecast/cash-sweep.js) gains an optional ordered `backupModules[]` cascade — the single-module path is byte-identical (8 prior engine tests unchanged; +2 new = 10/10). [`index.js`](../server/src/services/forecast/index.js) selects the sweep set by priority and passes backups to both call sites; PUT route maps `CashSweepPriority`; module edit modal gets a **Sweep Priority** input; the Cash Sweep Summary CSV/modal gain a source/dest **Modules** column. `vite build` green. **Remaining:** apply `031` to dev → end-to-end verify a two-module scenario → apply to prod (pre-deploy) → deploy. Not committed. Known limitation: yield convergence still recomputes only the primary module (backups are emergency-only, second-order).

<a id="cr019"></a>
- **CR019 — [Quicken Historical Import](CRs/CR019_QUICKEN_IMPORT.md)** — *IN-PROGRESS*. One-time backfill of pre-2022 Quicken history. Cash side lands in `transactions` with per-account soft cutoff and transfer pairing; investment side builds the full lot-level schema (`securities`, `security_lots`, `security_transactions`, `security_lot_disposals`, `security_prices`, `security_source_mappings`) that CR020 depends on. Four staging tables + admin UI with three mapping surfaces + Promote with calibration. Blocks CR020. **Phases A–E shipped to dev AND prod 2026-05-22.** Parser + FX seeder at [`server/src/v2/scripts/quicken-import.js`](../server/src/v2/scripts/quicken-import.js), promote/rollback at [`quicken-promote.js`](../server/src/v2/scripts/quicken-promote.js), admin API at [`routes/quickenImport.js`](../server/src/v2/routes/quickenImport.js), admin UI at [`QuickenImport.jsx`](../frontend/src/pages/QuickenImport.jsx) (live at `/quicken-import`). **129 passing tests** including end-to-end promote+rollback on real PKO data (3,098 transactions in 3 seconds, balance preservation verified). `runPromote` guards against investment-side batches (refuses fail-loud); `findTransfers` filters by `skip_transfer_analysis`. Next: investment-side promote (lot walker, §6.4 steps 1/3/5/6/7) — schema and parser are ready, only the promote-time logic remains. **Update 2026-05-29:** cash promote pivoted to a 1→1 model (single tx per row, post-hoc Transfer Analysis matching) — dev-only, supersedes the fanout (the 3,098-tx / 129-test figures above are pre-pivot). **Update 2026-05-30:** dev walkthrough validated the 1→1 model end-to-end and **removed auto-match from promote** (it was dead code that also irreversibly mutated PS-era `transfer_matched` flags; matching is now manual via `/transfer-analysis`). Suite at 59 passing. Also cleared three §19 follow-ups same day: bulk-create is now role-aware + atomic (no orphan leaves); the quicken jest suite uses a `_qpr `-namespaced fixture (no shared-dev mapping clobber); and role-corrupting stored mappings now block promote (`findRoleInvalidMappings` + pre-flight `roleInvalid`/`canPromote`). These 05-30 changes are committed on branch `fix/cr019-remove-promote-automatch` (PR #1), dev-only — not yet on prod. **Update 2026-06-01:** investment side descoped to value-only promote (§22, no lot walker — CR020 stays blocked); calibration redesigned to PS-anchored (§22.1 — `opening_balance = PS closing_balance − Σtx`, fixes collapsed history). **Update 2026-06-02:** (a) `quicken-verify.js` balance-invariant rewritten to the PS-anchored contract (was stale, all-red false positives); (b) orphan batch `bank_pko_save` deleted, `prop_nokomis` deferred (unmapped, history missing); (c) **issue #3 resolved for non-backfill accounts** — new `ps-anchor.js` (§22.2) reconciles active BS accounts to PS `closing_balance`; 12 CLEAN anchored on dev (PKO Savings +569,970 PLN, Fidelity IRA +148,590, …), 13 DIVERGENT reported (4 Fidelity mark-to-market + likely credit-card ledger inconsistencies — held for per-account decision). **Cutover model decided: re-run-pipeline-on-prod** — all data fixes are idempotent scripts run on prod, no dump/restore, no manual SQL. **Update (later 2026-06-02):** DIVERGENT card gaps resolved (PocketSmith intra-day ordering artifact, data complete, `computed` already correct — not a bug); Fidelity brokerage routed to the bank-feed/CR022 thread (feed `feed_balances` into the balance sheet — Known Issue #4); **`retire-handoff.js` shipped (§22.3)** — scripts the formerly-manual Fidelity 635 handoff (validated on dev, idempotent), the last manual-SQL artifact for cutover. **ps-anchor skip-guard shipped** — ps-anchor now skips feed-owned brokerage containers (Fidelity), so it won't fight the feed integration. **Update (later 2026-06-02):** prop_nokomis resolved (redundant with PS, kept; verify now WARNs promoted-0-row batches → gate green); **Rollback→Delete** shipped (rolled_back batches now deletable, with a tx-count guard); **prod cutover runbook drafted (CR019 §23)** — re-run-on-prod sequence with two tooling gaps to close first (G1 a COA seed script, G2 a dev→prod staging+mapping copy or UI re-map). CR019 cutover scripts (verify/ps-anchor/retire-handoff) all complete + idempotent. **G1 + G2 closed:** `seed-cr019-coa.js` (idempotent COA seeder) and `copy-quicken-to-prod.js` (dev→prod staging+mapping copy with name-translation, validated against a throwaway target) shipped. The §23 cutover runbook is now fully scripted (seed → copy → promote → retire-handoff → ps-anchor → verify). Open: STEP 1 deploy (prod v2.8.2 lacks the PS-anchored/value-only promote code — needs a fresh release from main); execute cutover (coupled to bank-feed release); PKO/Chase real-export backfill (needs user QIFs).

### 1.2 Completed (chronological, latest first)

- **Recon panel — rewrite the sign description (two-axis explainer)** — *Released v3.0.24 (2026-06-08)*. Follow-up to v3.0.23: the recon panel's intro now spells out that a feed is normalized by **two independent** sign settings — **balance sign** (automatic: a liability the bank reports `+owed` is stored `−`) and **transaction sign** (the *flip tx* toggle: ON only when a feed delivers purchases as `+`) — with the worked **Chase vs PKO** example (both show "(owed)" yet differ on the checkbox because Chase reports purchases `+` while PKO already reports them `−`). Replaces the old one-liner that conflated the two axes and kept reading as an inconsistency. Copy-only (a `<p>`/`<ul>` restructure of `.bfd-subtitle`); browser-verified rendering; `vite build` green; no DB/logic change.
- **Recon "flip tx" — surface the transaction-sign axis** — *Released v3.0.23 (2026-06-08)*. The "flip tx" (`feed_negate_tx`) checkbox looked inconsistent: two liability cards both shown by the bank as `+owed` (PKO VISA, Amazon/Marriot Visa) had *different* box states. Investigation (prod data) confirmed it's **correct, not a bug** — `flip tx` is the **transaction-sign** axis (does the feed deliver each tx with fin's sign?), fully independent of the **balance-sign** "(owed)" axis (`feed_sign`): Chase/Plaid cards report a purchase `+`/payment `−` (reverse of fin → flip ON, normalized to purchases-negative; proven by an `AUTOMATIC PAYMENT +583.34` row), while GoCardless/PKO reports purchase `−` already (flip OFF). Both reach the same internal convention. The confusion was that the recon screen showed only the balance axis. **Fix:** added a muted caption under the checkbox — **"feed reverses tx signs"** (ON) / **"feed tx signs match fin"** (OFF) — plus a rewritten tooltip stating the two axes are independent. Browser-verified (caption tracks `feed_negate_tx` per row). Frontend-only, no data change (the flags were already right); `vite build` green.
- **Bank-reconciliation status filter** — *Released v3.0.22 (2026-06-08)*. Added a **Status** dropdown beside the existing Feed filter on the CR023 [`BalanceReconciliation`](../frontend/src/components/BalanceReconciliation/BalanceReconciliation.jsx) table — filters rows to **Reconciled / Drift / MTM gap / No feed** (or All), each option showing a live count that reflects the active Feed selection. A `rowStatus()` helper derives the bucket from `reconciled` + `reconcile_mode` (mirrors the table's own status-pill logic); the two filters compose (feed → status). Browser-verified driving the real component with a mocked feed (counts `Reconciled 2 / Drift 1 / MTM 1 / No feed 1` of 5; each selection filters to exactly the right rows). `vite build` green; frontend-only, no DB migration.
- **Transfer picker = balance-sheet only + gutter vertical-align fix** — *Released v3.0.21 (2026-06-08)*. Two review-queue fixes. **(1) Transfer destination:** the "Transfer to account" picker listed **both** `[BS]` and `[P&L]` accounts; a net-worth-neutral transfer can only offset to a real asset/liability, so the [`AccountPicker`](../frontend/src/pages/RefreshPS.jsx) options now filter `o.section === "balance_sheet"` (drops P&L income/expense and the profit_loss-section `[Transfer]` clearing accounts) — to *categorize* against a P&L account you use Edit category, not Transfer. **(2) Gutter alignment:** the left checkbox+⋮ gutter sat **top-aligned** in tall (wrapped-description) rows and its box didn't match the other columns' height — because the `<td>` itself was `display:flex`, which drops a table cell out of row-height stretching. Moved the flex onto an inner `.trans-budget-table__rowctrl` wrapper and kept the `<td>` a normal `vertical-align:middle` cell, so the gutter now stretches to the full row height and vertically centers (browser-verified: `centerOffset` 0 in a 175px/7-line row, same as short rows). `vite build` green; no DB migration.
- **Move kebab to the left checkbox gutter (drop sticky-right column)** — *Released v3.0.20 (2026-06-08)*. The v3.0.19 kebab lived in a **sticky-right** action column that read as a detached floating box. Moved the ⋮ to sit **beside the selection checkbox** in the first column, and flipped the freeze from right to **left** (`.trans-budget-table` first `th`/`td` now `position:sticky; left:0` with opaque per-row fills + the selected-row left accent preserved) so the checkbox+⋮ form a clean row-control gutter flush to the left edge — always reachable, no detached column. The trailing actions column (`th`/`td`) is gone; `RowActionMenu` now opens **down-right** (left-aligned to the trigger, shifts left only on right-edge overflow). **CSS cleanup:** removed the now-dead sticky-right `.trans-budget-table__actions-header`/`__actions-cell` rules, all `.trans-budget-table__action-btn*` styles (unused since v3.0.19), and a duplicate `.trans-budget-table__checkbox-cell` block. Checkbox kept (bulk Accept/Category). Browser-verified (gutter stays pinned at x=77 while columns scroll to −573; menu portals/opens/fires/closes; 0 right-action cells); `vite build` green; no DB migration.
- **Review-queue row actions → kebab (⋮) menu** — *Released v3.0.19 (2026-06-08)*. Replaced the five inline per-row buttons (Category/Split/Neutralize/Transfer/Accept) — redundant on every line and space-hungry even after the v3.0.17 sticky-column fix — with a single **kebab (⋮)** per row that opens a popup menu. New [`RowActionMenu`](../frontend/src/features/Transaction/RowActionMenu.jsx) renders the menu through a **portal to `<body>`** with `position:fixed` coords from the trigger's `getBoundingClientRect()`, so the table's horizontal `overflow:auto` scroller never clips it; the menu flips above the trigger near the viewport bottom, and closes on outside-click / scroll / resize / Escape. The v3.0.18 busy state carries over: while a row's neutralize/accept is in flight the kebab shows a spinner and every menu item disables (the active one reads "Neutralizing…"/"Accepting…"). The action column stays sticky-right (now just the narrow kebab). **Kept** the row-selector checkbox (still powers Accept Selected (N) + bulk Category (N)). Only `RefreshPS` renders `<TransactionTable>`, so scope is the Refresh-Feeds review page. Browser-verified end-to-end (esbuild-bundled component driven in chromium: open/position/portal-not-clipped/item-fires/close/busy all green); `vite build` green; no DB migration. *(Right-click-to-open was considered and rejected — undiscoverable and dead on touch; the kebab is the discoverable, touch-safe equivalent.)*
- **Per-row action busy state (Neutralize / Accept feedback)** — *Released v3.0.18 (2026-06-08)*. Closes the dead-state loose end logged in v3.0.16/v3.0.17. [`RefreshPS.jsx`](../frontend/src/pages/RefreshPS.jsx)'s `isNeutralizing` boolean was set but never read, so the per-row **Neutralize** button gave no in-flight feedback and could be double-fired; the per-row **Accept** had the same gap (`acceptingId` tracked the row id but was never passed to the table). Replaced the boolean with **`neutralizingId`** (the row being neutralized), threaded `neutralizingId` + the existing `acceptingId` into [`TransactionTable`](../frontend/src/features/Transaction/TransactionTable.jsx), and per row: the in-flight button relabels (**"Neutralizing…"** / **"Accepting…"**) while **all five actions on that row lock** (`disabled`) to prevent same-row races; added a `.trans-budget-table__action-btn:disabled` style (opacity 0.5, default cursor). `handleNeutralizeClick` also early-returns if a neutralize is already running. Frontend-only, no DB migration; `vite build` green; browser-verified (disabled row dims, label flips, sticky column unaffected). **Still open (deliberately deferred):** TransactionTable.jsx trips 5 `react-refresh/only-export-components` lint errors because it co-exports 8 shared hooks/constants/helpers used by 6 files — a dev-HMR-only lint; clearing it is a separate module-extraction refactor, not bundled here.
- **Pin review-queue action column + ignore local `backups/`/`logs/`** — *Released v3.0.17 (2026-06-08)*. Follow-ups from the v3.0.16 closeout. **(1) Sticky action column:** the shared [`TransactionTable`](../frontend/src/features/Transaction/TransactionTable.jsx)'s per-row action buttons (Category / Split / Neutralize / Transfer / Accept) sat in a far-right column that needed horizontal scrolling to reach on the wide Refresh-Feeds review table. Pinned the column to the right edge in [`PageLayout.css`](../frontend/src/pages/PageLayout.css) — `position: sticky; right: 0` on `.trans-budget-table__actions-header` (z-index 4) + `.trans-budget-table__actions-cell` (z-index 1) with a left-edge separator shadow and **opaque per-row fills** (base/even/hover/selected) so scrolling cells don't bleed through the frozen column. Mirrors the existing sticky-left frozen-column pattern (balance reports / `fc-review-table`); CSS-only, applies to every table using `TransactionTable`. **(2) `.gitignore`:** added lowercase `backups/` and `logs/` (only `Backups/` was ignored) so local runtime artifacts can't be swept into a commit. No DB migration; `vite build` green. *(Did **not** fix the pre-existing dead `isNeutralizing` state — still logged.)*
- **Refresh Feeds — drop redundant "Accept Bank Feed" button** — *Released v3.0.16 (2026-06-08)*. The `/refresh-ps` (Refresh Feeds) review header carried both **Accept Bank Feed** (accept rows where `Source === "bank-feed"`) and **Accept All** (accept every queued row). Post-CR023/CR030 the bank feed is the **sole** queue source, so the two buttons hit the same set — removed the per-source button from [`RefreshPS.jsx`](../frontend/src/pages/RefreshPS.jsx). `handleAcceptBySource` is **kept** (with an `eslint-disable-next-line no-unused-vars` + comment) so a per-source button is one line away if the queue ever regains a second source. Confirmed in passing that the user's other two asks already ship as **per-row** action buttons (not header buttons): **Neutralize** (`POST /transactions/:id/neutralize`, offsetting entry for brokerage trades) and **Transfer** (`POST /transactions/:id/transfer`, auto-books the other leg into a picked account). Frontend-only, v3 tweak, no DB migration. **Known follow-ups (logged §4.3):** those per-row buttons sit in a far-right column that needs horizontal scroll to reach; and a **pre-existing** dead `isNeutralizing` state means the per-row Neutralize button shows no in-flight/disabled state (not fixed here).
- **Mobile Refresh Feeds page (CR026 mobile)** — *Released v3.0.15 (2026-06-07)*. New phone page [`MobileRefreshFeeds.jsx`](../frontend/src/mobile/pages/MobileRefreshFeeds.jsx) at `/m/refresh-feeds` (home-launcher card, not a tab). **Refresh + status scope only:** a days-window select (7/14/30/60/90) + "Refresh feeds" button → `POST /api/v2/ingest-bank-feed/refresh {sinceDays}`, then a result summary (New / Linked to PS / Staged / Ignored accts), last refresh/ingest times (`GET /api/v2/util/appdata`), and the review-queue count (`POST /api/v2/ingest-ps/review-new-transactions` → `data.length`). Categorizing/accepting reviewed rows stays on the desktop `/refresh-ps` page (modal-heavy review/split/transfer/neutralize — out of mobile scope). Wired into `App.jsx` (lazy route + `DESKTOP_TO_MOBILE["/refresh-ps"]`), `MobileHome` card, `MobileLayout` title. Reuses existing `m-*` CSS; additive, dormant-safe (touches no existing route). Replaces the PS-API `MobileRefreshPS` that CR030 removed.

- **Mobile "Switch to desktop view" trap fix (CR026 follow-up)** — *Released v3.0.14 (2026-06-07)*. The real cause behind "the mobile left menu bar doesn't react": [`MobileHome.jsx`](../frontend/src/mobile/MobileHome.jsx)'s **"Switch to desktop view"** button calls `setForceDesktop(true)` → `localStorage.forceDesktop="true"`, which made `useIsMobile` return false **permanently, in every orientation** — so a phone got the desktop sidebar (whose rail is hover-only, dead to touch), with **no return path** anywhere on the desktop layout. Self-healing fix in [`useIsMobile.js`](../frontend/src/mobile/useIsMobile.js): `forceDesktop` is now honored **only on a fine-pointer (mouse) device** — on a coarse-pointer (touch) phone the flag is ignored, so already-trapped phones recover automatically on next load with no manual storage-clearing. The "Switch to desktop view" button is hidden on touch devices (new exported `isCoarsePointer()`) so it can't re-trap. Frontend-only, dormant-safe. (v3.0.13's touch dead-band change still stands; it just addressed a different, flag-free case.)

- **Mobile nav touch dead-band fix (CR026 follow-up)** — *Released v3.0.13 (2026-06-07)*. On a phone in landscape (or any small touch tablet) at **641–900px**, `useIsMobile` returned false (width > 640) so the device got the **desktop sidebar**, which CSS renders as an icon-only **rail** whose sub-navigation only opens on `:hover`/`:focus-within` — unreachable by a finger, so the "left menu bar didn't react." Root cause: two disagreeing breakpoints — the JS mobile-shell hand-off at **640px** vs the CSS auto-rail at **900px**, leaving a 259px band where a touch device got a mouse-only rail. Fixed in [`mobile/useIsMobile.js`](../frontend/src/mobile/useIsMobile.js): `detect()` now also returns true when the device has a **coarse (touch) pointer** AND viewport ≤ new `TOUCH_BREAKPOINT` (900, synced to `Sidebar.css`), so touch devices in that band get the working `/m/*` bottom-tab shell; narrow **mouse** windows (fine pointer) keep the desktop rail, and the `localStorage.forceDesktop` escape hatch still overrides. Frontend-only, dormant-safe (no flags/DB/API). See CR026 §17 closeout note. *(Release also carries uncommitted CR023/CR027 doc status updates from other streams, folded in by owner request.)*

- **[CR031 — Ledger Filter Parity + Period Year-Range](CRs/CR031_LEDGER_FILTER_PARITY_YEAR_RANGE.md)** — *Released v3.0.12 (2026-06-06)*. Ledger's bespoke Type→Group→Sub-Group→Account cascade replaced with a **single-select `HierarchyFilter`** (BS COA group pills + radio checklist, currency-code suffix; running balance still needs one account) and an **always-visible `PeriodSelector`** (default This Year, the old "Period Filter" checkbox dropped). Custom period gains **Year-from / Year-to** (`enableYearRange`) on **Ledger + Transactions + Budget** — `buildDateRangeParams` is now year-range-aware (`fromDate=(year,fromMonth)`, `toDate=(toYear,toMonth+1)` exclusive) and a new `filters.toYear` carries it; Actual list + totals route through it (client-side filter re-applies the UTC range). Backend `GET /api/v2/budget/actual-entries` now accepts `fromDate`/`toDate` (exclusive upper bound) so Actual **totals span the range**, falling back to single `actualYear` when absent. New `HierarchyFilter` `singleSelect`/`getItemSuffix`/`selectedLeaf` props (opt-in, others unaffected) + shared `buildPeriodChipLabel`. `vite build` green; the prod deploy rebuilds `fin-server` so the route ships live (dev `fin-server-dev` still needs a local rebuild). No DB migration.

- **[CR030 — Retire automated PocketSmith](CRs/CR030_AUTOMATED_PS_RETIREMENT.md) + COA feed badge** — *Released v3.0.11 (2026-06-06)*. With every feed-able account on a direct bank feed (CR023: 28 fed / 2 manual), removed the **automated** PocketSmith integration — PS-API txn fetch (`refreshPsApiV2.js`, `retrieval/pocketsmith.js`, `psdataConverter.js`), `POST /ingest-ps/refresh-ps`, the legacy PS calibration routes (`/map-ps-accounts`, `/calibrate`, `/calibration-status`) and their UI (Refresh PS Data / Accept PS buttons, the "PocketSmith calibration (legacy)" section, `MobileRefreshPS`) — while **keeping** the one-time PS **CSV upload** (`UploadPS`, `/upload-ps`, `/analyze-ps`, `/clearall`, `/sync-to-transactions`) and all frozen historical `source='pocketsmith'` data. Reconciliation is now feed-driven (`reconcileToFeed`). Also: **COA feed badge** — leaf accounts on a live feed show an Rss icon via new `GET /api/v2/bank-feed/fed-accounts`. Net 15 files, 61+/2280−. Safe: no PS cron; surgical edits to the shared Refresh-Feeds & Balance-Calibration pages; `vite build` green; kept routes verified 200, removed routes gone. No DB migration. Follow-up: drop now-unused `PS_API_KEY`/`PS_USER_ID` env.

- **UI — Per-feed (institution) filter on Balance Reconciliation** — *Released v3.0.10 (2026-06-06)*. The bank reconciliation table (`<BalanceReconciliation/>` on the **Balance Calibration** page) gained a **Feed** dropdown to filter rows to a single institution (Chase, PKO, Fidelity, Amex, Wise, …); the unreconciled count reflects the active filter. Backend enriches each `GET /api/v2/bank-feed/balance-recon` row with its `institution`, derived from the bank-feed service (`account.external_id → connection_id → connection.institution_name`, via a new `buildExternalIdToInstitution()` joining `/v1/accounts` + `/v1/connections`) — **best-effort, fail-open**: a bank-feed outage leaves `institution=null` and the rows fall under "Unknown" rather than failing the recon load. Files: `routes/bankFeed.js`, `repositories/bankFeedReconciliation.js`, `components/BalanceReconciliation/BalanceReconciliation.jsx`. No DB migration.

- **UI fix — "CASH FLOW" total-row bleed-through on horizontal scroll** — *Released v3.0.9 (2026-06-06)*. Follow-up to v3.0.8: the **Cash Flow** report's footer total row (`.balance-report-table__net-cash-flow`) had the same frozen-column bleed — its `td` background was the semi-transparent `rgba(107,142,107,0.04)` tint, which also painted the sticky first cell, so scrolling period values showed through (the INCOME/EXPENSE level-0 rows were fine because they use opaque `var(--primary-subtle)`). Forced the frozen first cell (`td:first-child` / `__name`) to opaque `var(--surface)`, mirroring the Net Worth fix. One edit in `features/CashFlow/CashFlowReport.css` covers **Cash Flow Periods**, the regular **Cash Flow** report, and **Budget Variances** (all reuse that CSS + the `__net-cash-flow` footer). CSS-only, no DB migration.

- **UI fix — Frozen "Account" column bleed-through on horizontal scroll** — *Released v3.0.8 (2026-06-06)*. The sticky first column on the **Balance Sheet** (`.balance-report-table`) and **Balance Trends** (`.balance-trends-table`) reports showed the scrolling year/period values *through* it. Cause: the frozen column's opaque `background: var(--surface)` was overridden in several row states by **semi-transparent** fills that win on specificity — the even-row stripe (`rgba(248,250,254,0.35)`), the Net Worth tint (`rgba(107,142,107,0.04)`), the highlighted-row green (`rgba(87,188,103,0.15) !important`), and (Balance Trends) a `var(--surface-hover, rgba(107,142,107,0.06))` hover whose token is **undefined**, so it always resolved to the transparent rgba fallback. Fix re-asserts an **opaque** fill on the frozen column (`td:first-child` / `__name` / `__td-account`) for every striped/hover/highlight/total state (opaque highlight tint added per theme). Files: `features/Balances/BalanceReport.css`, `pages/PageLayout.css`, `pages/BalanceTrends.css`. CSS-only, no DB migration.

- **Net Worth Chart — Period selector (Month / Quarter / Year)** — *Released v3.0.3 (2026-06-06)*. `/balance-chart` gained a Period dropdown alongside the From/To pickers. Bars are now period-end snapshots driven by the shared `periodHelpers` engine (`buildPeriodSeries` wrapping `buildEndDateSeries` + `planColumns`): Month = month-ends, Quarter = quarter-ends, Year = Dec-31. In-progress period snapshotted as-of today; future periods dropped. **Year mode hides the month pickers** (year-only From/To, range widened to whole years). The two-line x-axis reflects the period type (`01`/`26` · `Q2`/`26` · `2026`); tooltip/range-summary carry the full `(MTD)`/`(QTD)`/`(YTD)` label. Files: `pages/BalanceChart.jsx`, `features/Charts/BalanceChartDateSelectorMonthYear.jsx`, `features/Charts/BalanceChartPanel.jsx`. No backend change (reuses `GET /reports/balance` per period end).

- **UI fix — Period report layout** — *Released v3.0.2 (2026-06-05)*. (1) `.balance-grid--single` now uses `grid-template-columns: minmax(0, 1fr)` (was `1fr`); the `0` minimum stops the grid track growing to fit a wide `max-content` table, so the inner `overflow:auto` container shows a horizontal scrollbar instead of widening the whole page. Fixes Cash Flow Periods + Balance Sheet Periods (and other `balance-grid--single` pages). (2) Cash Flow Periods toolbar now renders year-only From/To selects when Period = Year (mirrors Balance Sheet Periods) — no redundant month dropdown; selecting a year clamps From→Jan 1 / To→Dec 31.

<a id="cr016"></a>
- **CR016 — [Frontend Test Framework (Vitest)](CRs/CR016_FRONTEND_TEST_FRAMEWORK.md)** — *COMPLETED 2026-05-20*. Vitest + `jsdom` scaffolded; 96 tests across 5 helper modules (`dateHelpers`, `formatters`, `treeTraversal`, `forecastHelpers`, `cashFlowHelpers`); `npm test` exits non-zero on failure. Component/hook tests + Playwright E2E deferred to future CRs.

<a id="cr013"></a>
- **CR013 — [Collapse `categories` Table into `accounts`](CRs/CR013_COLLAPSE_CATEGORIES.md)** — *COMPLETED 2026-04-28*. Migration 021. Single COA source of truth; FK columns repointed; legacy table dropped.

<a id="cr006"></a>
- **CR006 — [AI Review of FC Plan](CRs/CR006_AI_REVIEW.md)** — *COMPLETED 2026-04-28*. Conversational review via local `ocr-llm` gateway, async with polling, browser notifications. Migrations 014, 020.

<a id="cr012"></a>
- **CR012 — [Opening Balance Calibration](CRs/CR012_OPENING_BALANCE_CALIBRATION.md)** — *COMPLETED 2026-04*. Migration 016. Accurate balance sheet via `opening_balance + SUM(transactions)`.

<a id="cr007"></a>
- **CR007 — [PWA & Mobile Simplified Shell](CRs/CR007_PWA_MOBILE_SHELL.md)** — *COMPLETED*. Installable PWA + dedicated `/m/*` mobile experience.

<a id="cr005"></a>
- **CR005 — [Cash Sweep & Auto-Balance](CRs/CR005_CASH_SWEEP.md)** — *COMPLETED*. Iterative sweep with income↔sweep convergence loop. Migrations 012–013.

<a id="cr011"></a>
- **CR011 — [Source Mappings (Category + Account)](CRs/CR011_SOURCE_MAPPINGS.md)** — *COMPLETED*. Decouples external system names from internal app names. Migrations 018–019.

<a id="cr009"></a>
- **CR009 — [Transfer Analysis + Manual Match Groups](CRs/CR009_TRANSFER_ANALYSIS.md)** — *COMPLETED*. Auto + manual matching of transfer pairs; `transfer_matched` flag. Migrations 005–006.

<a id="cr008"></a>
- **CR008 — [HierarchyFilter & Transaction Pages Redesign](CRs/CR008_HIERARCHY_FILTER.md)** — *COMPLETED*. Two-stage cascading filter; full redesign of transaction-explorer pages.

<a id="cr010"></a>
- **CR010 — [COA Management Redesign + Move](CRs/CR010_COA_MANAGEMENT.md)** — *COMPLETED*. Tree view editor with toolbar, inline actions, quick-add, Move modal.

<a id="cr004"></a>
- **CR004 — [FC Inc/Exp Mapping Layer](CRs/CR004_FC_LINES_MAPPING.md)** — *COMPLETED*. User-defined FC Lines decouple budget categories from forecast outputs. Migration 007.

<a id="cr003"></a>
- **CR003 — [Forecast Module](CRs/CR003_FORECAST_MODULE.md)** — *COMPLETED*. Engine + UI for BS modules and inc/exp items across all phases (1, 2A, 2B, 3, 4, 5).

<a id="cr002"></a>
- **CR002 — [Frontend Architecture Refactor](CRs/CR002_FRONTEND_REFACTOR.md)** — *COMPLETED*. God components decomposed; `features/` module pattern; ~22 duplicate files deleted.

<a id="cr001"></a>
- **CR001 — [MongoDB → PostgreSQL Migration](CRs/CR001_MIGRATION_MONGO_TO_POSTGRES.md)** — *COMPLETED*. All storage moved to PostgreSQL 16; V1 routes and `coa.json` removed.

---

## 2. Open Backlog (non-CR items)

Small fixes, refactors, and one-off cleanups that don't warrant their own CR file. New work that grows beyond a line item gets promoted to a CR.

- [ ] **Forecast: `baseYears` workaround cleanup** — `baseYears` is now properly populated but a `value == null` detection pattern still lives in `FCReviewTable`. Low-priority cosmetic.
- [ ] **Frontend DRY items** still pending (full list in §3 below):
  - `collectCollapsiblePaths()` duplicated in `Balance.jsx` and `BalanceChart.jsx` — move to shared `treeHelpers.js`.
  - Month options array recreated in multiple components — move to shared constants.
  - FX rate lookup duplicated in BudgetInput and various transaction modals — move to shared `currency.js`.
- [ ] **Shared components missing** (per §3.3): `<Modal>`, `<DataTable>`, `<FormField>`, `<LoadingSpinner>`, `<ErrorMessage>`, `<ConfirmDialog>`, `<CurrencyInput>`. Tracked here as a theme; promote to a CR if a focused refactor is scheduled.
- [ ] **Backend service layer simplification** (per §4): break up `fcbuilder-module.js` (835 lines), `cashFlowFetcher.js` (619 lines). Centralize error handling with an `AppError` class. Promote to a CR when scheduled.
- [ ] **TypeScript migration** (per §3.7) — gradual: utilities → hooks/components → pages.
- [ ] **API design pass**: consistent `{ success, data, meta }` response envelope, pagination on list endpoints, structured logging via Pino.

---

## 3. Known Issues

1. **Test coverage:** 73 backend Jest tests + 17 HTTP smoke checks + 96 frontend Vitest tests across 5 helper modules ([CR016](#cr016) complete; component / hook tests + Playwright E2E deferred to future CRs). Run backend: `cd server && npm test`. Frontend: `cd frontend && npm test`. Smoke: `node server/src/scripts/smoke-after-021.js`. Full test layout: [Testing/TEST_OVERVIEW.md](Testing/TEST_OVERVIEW.md).
2. **Cloud-init ISO** still attached to the VM as a CD-ROM. Harmless but can be ejected:
   ```bash
   virsh --connect qemu:///system change-media fin sda --eject
   ```
3. **Timezone-sensitive date handling:** The `pg` library serializes JavaScript `Date` objects using the server's local timezone. Fixed globally in `postgres.js` with `types.setTypeParser(1082, val => val)` so DATE columns return plain `YYYY-MM-DD` strings. Frontend code must format local-time `Date` objects using `getFullYear()`/`getMonth()`/`getDate()` — never `.toISOString().split("T")[0]` which shifts by one day in UTC+ timezones.
4. **⤇ Cross-thread handoff (CR019 → bank-feed/CR022 thread, 2026-06-02): Fidelity brokerage balances are wrong; the fix is to feed them from `feed_balances`.** The 5 fin Fidelity balance-sheet accounts compute balance as `opening_balance + Σtx` from PocketSmith data, which is a *half-finished trade-neutralization* (only 7 of thousands of 2020–2026 trades neutralized via the `neutralize()`/`auto-offset` button; missing pre-2020 opening balances; partial "Unrealized" market rows). Result: wrong balances — e.g. Fidelity Bond reads **$1.01M** vs the real **$1.20M**. **Authoritative values already exist** in the bank-feed `feed_balances` (fresh daily, `source=fintable`, bank-feed DB `localhost:5435`). **Root cause:** the 5 fin accounts are mapped only to `source='pocketsmith'`; their 6 Fidelity feed accounts already sit in `account_source_mappings` as `account_id=NULL, ignored=true` (CR022 deliberately deferred them as out-of-cash-scope). **This is NOT CR022 cash scope and NOT full CR020 lot-tracking — it's a distinct, small piece: read `feed_balances` for brokerage/fed accounts on the v3 balance sheet (the bank-feed migration's stated intent: "the v3 balance sheet reads from feed_balances when available"). Needs triage as a new small CR or CR020 sub-item.** Precise mapping (feed `external_id` → fin account): `5216d738-82a9-4956-9b23-aff70d07c827` Rollover IRA→26; `4edb12ab-749d-4e1f-bbe4-5d31aaee30d8` Stocks→27; `3bd9f941-8d06-4302-8950-35b532cebbaa` Options→28; `e5a23070-13bb-49af-8f2d-e552e159b570` Cash Management→30; `e420ad75-9a54-4c3b-b98a-5adbd8b6061e` Fixed Income→31; `190d7bf1-c77d-43bc-9061-b7646669b176` Individual→**leave ignored** (user instruction). **Cleanup:** CR019's `ps-anchor.js` wrongly anchored fin **26 (Fidelity IRA)** to PS's stale April closing_balance ($266,853) as "CLEAN" — once 26 is fed from `feed_balances`, run `DELETE FROM transactions WHERE account_id=26 AND source='ps-anchor'`. Brokerage accounts should be excluded from `ps-anchor` entirely; the feed owns them. Full analysis in memory `cr019_quicken_import_progress.md` and CR019 §22.2.

---

## 4. Frontend Improvement Themes (ongoing)

### 4.1 DRY Violations (remaining)

1. ~~Transaction filter logic duplicated across TransActual, TransBudget, useTransactions~~ — *Resolved* (CR002).
2. `collectCollapsiblePaths()` duplicated in `Balance.jsx` and `BalanceChart.jsx` — move to shared `treeHelpers.js`.
3. ~~Date initialization logic in BudgetInput~~ — *Resolved* via `PeriodSelector`.
4. Month options array recreated in multiple components — move to shared constants.
5. FX rate lookup duplicated in BudgetInput and transaction modals — move to shared `currency.js`.

### 4.2 Missing Shared Components

| Component | Used in | Current state |
|-----------|---------|---------------|
| `<Modal>` | 5+ places | Each modal custom-built |
| `<DataTable>` | 6+ places | Tables custom each time |
| ~~`<FilterPanel>`~~ | 4+ places | Partially via `HierarchyFilter` (CR008) and `PeriodSelector` |
| `<FormField>` | 10+ places | Inputs custom per form |
| `<LoadingSpinner>` | All pages | "Loading..." text varies |
| `<ErrorMessage>` | All pages | Error display inconsistent |
| `<ConfirmDialog>` | 5+ places | Delete confirms duplicated |
| ~~`<DateRangePicker>`~~ | 4+ places | Partially via `PeriodSelector` |
| `<CurrencyInput>` | 3+ places | Amount inputs inconsistent |

### 4.3 UI/UX Improvements

| Issue | State | Proposed |
|-------|-------|----------|
| Loading states | Mix of "Loading...", spinners | Unified `<LoadingSkeleton>` |
| Error display | Different per page | Unified `<ErrorBanner>` with retry |
| ~~Empty states~~ | Inconsistent | *Resolved:* `EmptyState` with 8 unDraw variants, wired into 14 pages |
| Button styles | `.generate-report-button` everywhere | Variants: primary, secondary, danger |
| Form validation | Scattered | Centralized validation with error messages |
| Date selection | Different controls per page | Partially via `PeriodSelector`; other pages pending |
| ~~Per-row action buttons hard to find~~ | *Resolved v3.0.17:* the `TransactionTable` action column (Category / Split / Neutralize / Transfer / Accept) is now **pinned to the right edge** (`position: sticky; right: 0` on `.trans-budget-table__actions-header` + `__actions-cell`, opaque per-row fills) so it stays reachable without horizontal scrolling | — |

### 4.4 Performance & TypeScript

- Component memoization (`React.memo`, `useMemo`, `useCallback`) for expensive components.
- Virtual scrolling (`@tanstack/react-virtual`) for tables with 1000+ rows.
- Debounced filters; SWR/stale-while-revalidate for API caching.
- TypeScript migration: utilities → hooks/components → pages.

### 4.5 Decisions Made

| Decision | Choice |
|----------|--------|
| Component library | Radix UI + custom styling |
| Charting | Recharts |
| Mobile support | Responsive (1080px / 768px / 640px breakpoints) + dedicated `/m/*` shell |
| State | Enhanced React Context (upgrade to Zustand if complexity grows) |

---

## 5. Backend Improvement Themes (ongoing)

### 5.1 Service Layer Complexity

| Service | Lines | Notes |
|---------|-------|-------|
| `fcbuilder-module.js` | 835 | Monolithic; mixes data access with business logic |
| `fcbuilder-incexp.js` | 436 | Duplicated patterns from module builder |
| `cashFlowFetcher.js` | 619 | Complex aggregation, hard to maintain |
| `balanceSheetFetcher.js` | 324 | Could be simplified with SQL views |

### 5.2 Missing Abstractions

- **Repository pattern** — partially adopted under `server/src/v2/repositories/`. Continue extracting where services still embed SQL.
- **Base repository** — common operations in a shared base class.
- **Error handling** — centralize with an `AppError` class and error-handling middleware.

### 5.3 API Design

- Consistent response envelope `{ success, data, meta }` or `{ success, error }`.
- Pagination on list endpoints (`page`, `pageSize`, `total`, `totalPages`).
- Standardize `sortBy`, `sortOrder`, `fromDate`, `toDate` across endpoints.
- Structured logging via Pino.

---

## 6. Testing Strategy

See [Testing/TEST_OVERVIEW.md](Testing/TEST_OVERVIEW.md) for the test inventory, naming conventions, and run commands.

Phased plan:

- **Phase 1 — Backend unit tests (Jest, in place):** Forecast engine, route handlers with mocked repos. 73 tests today.
- **Phase 2 — HTTP smoke tests (in place):** `smoke-after-021.js` covers all SQL JOINs rewritten by migration 021. Add new smoke scripts for major schema changes.
- **Phase 3 — Frontend unit tests (Vitest, [CR016](#cr016)):** Helpers covered. *Complete 2026-05-20: 96 tests across 5 helper modules (`dateHelpers`, `formatters`, `treeTraversal`, `forecastHelpers`, `cashFlowHelpers`). Hook tests deferred to a future CR (need React Testing Library + Context mocking).*
- **Phase 4 — E2E (Playwright, future):** Critical flows — PocketSmith sync + accept, budget entry, forecast module creation.

---

## 7. Migration History

Chronological log of substantive infrastructure / behavioural changes. Smaller user-facing features are documented in the relevant CR file or FC_PROJECT_STRUCTURE.md.

| Date | Event |
|------|-------|
| 2026-06-09 | **Released v3.0.25 — CR017 multi-module priority cash sweep.** Migration `031_cash_sweep_priority.sql` applied to **dev + prod** (backfilled the legacy single `cash_sweep_target` → priority 1: 1 row dev / 2 rows prod across scenarios 47+52): `forecast_modules.cash_sweep_priority INT NULL` (NULL=out, 1=primary, 2,3,…=backups), backfills the legacy `cash_sweep_target=TRUE` single target → priority 1, partial-unique on `(scenario_id, cash_sweep_priority)`; the `cash_sweep_target` boolean is kept and maintained as "priority==1" for back-compat. Engine [`cash-sweep.js`](server/src/services/forecast/cash-sweep.js) `computeCashSweepIterative` gains an optional ordered `backupModules[]` cascade (single-module path byte-identical; shortfall drains primary then backups in priority order; excess deposits into priority-1 only) + per-backup `_sweep_bal` carry-forward + an audit `Modules` column. [`index.js`](server/src/services/forecast/index.js) selects the sweep set by priority and feeds both call sites; PUT route maps `CashSweepPriority` (+`CashSweepTarget` back-compat) and keeps ranks unique; module edit modal → **Sweep Priority** input; Cash Sweep Summary modal/CSV → source/dest **Modules** column. Engine tests 10/10, `vite build` green. **Dev end-to-end verified:** a forced ~10M shortfall on scenario "2026 Base" drained the primary (Fidelity Cash Mgt −2.07M) then cascaded into the backup (Fidelity Stocks −1.41M) before the residual Cash Shortfall; `Modules` audit column = "Fidelity Cash Mgt \| Fidelity Stocks"; clear path verified; dev state restored. Prod migration applied (DB backed up first), code deployed via `deploy-to-production.sh`. |
| 2026-06-06 | **Released v3.0.7** — CR023/CR028 **per-mapping transaction-sign flip + Chase cutover**. New `account_source_mappings.feed_negate_tx` (migration 030): the bank-feed promote **negates amount+base** for an account whose feed reports transactions with the **opposite sign** to fin (e.g. Chase cards report purchase positive / payment negative / balance +owed — the reverse of fin and of the Plaid Luxury/Amex cards). Independent of `feed_sign` (balance-only); set via a per-row **"flip tx"** checkbox → `PATCH /api/v2/bank-feed/feed-negate-tx/:accountId` (governs FUTURE promotes — set before importing). **Recon display fix:** the bank column now shows the **sign-adjusted "expected"** value (with the raw `+owed` noted when signs differ) so a liability reading `−x` computed vs `+x` bank reads as clearly RECONCILED rather than looking mismatched. **Chase cut over:** Checking (6) + Savings (7) — clean assets (Savings reconciled to the cent); the two Chase **credit cards** (Amazon Visa 60, Marriot Visa 59) use `feed_negate_tx=TRUE`. No data migration beyond `030`. Tests: `bankFeedImport.test.js` negation case. |
| 2026-06-05 | **Released v3.0.6** — patch; **CR026 P4 — read-only mobile Ledger** (`/m/ledger`). Pick a balance-sheet (asset/liability) leaf account from a full-width picker; view its transactions with a chronological **running balance** (cumulative from 0, newest-first, account currency) via `fetchTransactionsV2({accountId})`. Reuses the `.m-tx` row grid + a new `.m-select`. Read-only (no edit/delete — editing stays desktop). Wired into routing + desktop↔mobile redirect map + home launcher + title map. Device-emulation verified (balances accumulate correctly). Completes the CR026 P4 read-page slices (Overview, Balance Trends, Ledger); mobile microinteractions (§7.4) remain. **Frontend-only, mobile shell only.** |
| 2026-06-05 | **Released v3.0.5** — patch; **CR026 P4 (mobile expansion, first slices).** The mobile home (`/m`) is now a live-data **Overview** (net-worth hero + Δ vs last month, This-Month cash-flow grid) instead of a bare launcher (the Overview itself already rode v3.0.4 since its commit was in that build; this release adds the rest). **Bottom tab bar revisited** — added an **Overview** tab, dropped the periodic **Refresh** tab (still reachable from the home launcher) so the 5 tabs are the highest-frequency destinations (CR026 §7.3). New read-only **Balance Trends** mobile page (`/m/balance-trends`): net worth over the last **6/12/24 months** (parallel `fetchBalanceReportV2` per month-end) as a recharts line chart + net-worth hero. Wired into routing + the desktop↔mobile redirect map + home launcher. **Frontend-only, no DB migration; mobile shell only** (desktop unaffected). Deferred next P4 slice: read-only mobile **Ledger** (account picker + running balance). |
| 2026-06-05 | **Released v3.0.4** — [**CR025 — Manual Transaction Entry**](CRs/CR025_MANUAL_TRANSACTION_ENTRY.md) shipped. New `/manual-entry` page + **Manual Entry** card on the `/transactions` landing: hand-enter a single **actual** transaction (`source='manual'`, `accepted=TRUE`), staying open for rapid entry (account/date/currency sticky; amount/description/category clear; focus back to Amount). Reuses `AccountPicker`/`CategorySelector`/`useTransactionExchangeRates`+`computeTransactionBaseAmount` (non-USD auto-computes USD base; **blank + Save blocked when no FX rate** — never silently sends `amount`). **Backend:** `repo.create()` now inserts `accepted` — default **TRUE** for `source='manual'`, FALSE for importers (preserves prior behavior); explicit value honoured. No DB migration. Test `createTransaction.test.js`. Unblocks the CR023 manual-bucket accounts (OCME 45 loan receivable, dormant holdings) leaving PocketSmith. |
| 2026-06-05 | <a id="cr028"></a>**Released v2.16.2 → v3.0.1** — [**CR028 — Securities-Trade Neutralization & Transfer-Orphan Management**](CRs/CR028_SECURITIES_TRADE_NEUTRALIZATION.md). Makes brokerage "Neutralize" feed-aware: `repo.neutralize` now **pairs** an existing offsetting leg (opposite amount ±3d → both set to `Transfer - Securities Trades`, **no new row**) instead of always inserting a mirror that **double-counts** feed pairs (Fidelity delivers both the trade leg + the SPAXX sweep). A `dryRun` preview + **ConfirmModal warning** fires before any mirror insert (the only path that can orphan). **Ledger** gets a guarded Neutralize selection-bar button (`fix` for string-id rejection). On the **Transfer Analysis** page, unmatched rows now resolve in place: **Remove** for an `auto-offset` orphan (the reliable orphan signal — required exposing `t.source` in `findTransfers`), **Neutralize** for a genuine single leg. Per-account **reconcile-mode toggle** (bank/calibrate ⇄ brokerage/mtm → DRIFT vs MTM GAP, e.g. CDs in Cash Mgt) via `PATCH /api/v2/bank-feed/reconcile-mode/:accountId`. Shared `components/ConfirmModal/`; bank reconciliation moved into `components/BalanceReconciliation/` on the **Balance Calibration** page (above the legacy PS calibration), off Bank Feed Setup. **Data fix:** removed the one spurious orphan (+$41,749.13) → Fidelity Options drift +$42,164 → **+$415** (real MTD move); 0 orphans remain across 2026. No DB migration. Tests: `neutralize.test.js` (pair/mirror/dryRun). |
| 2026-06-05 | **Released v3.0.0** — major; **CR026 UI revamp turned ON for prod.** Flips the desktop default from the legacy top bar to the **collapsible sidebar + dark-mode toggle + ⌘K command palette + help drawer**, by wiring `VITE_NAV_LAYOUT=sidebar` into the prod build (new `docker-compose.yml` build arg + `frontend/Dockerfile` `ARG VITE_NAV_LAYOUT`; the plain `vite build` doesn't read `env-cmdrc`, so the build arg is the real switch). `Layout.jsx` still honours a per-browser `localStorage.navLayout` override. Bundles this session's CR026 hardening (all already on `main`): dark-mode defect fixes **D1–D12** (rgba white gradients + inline JS styles + solid light fills → tokens; `f2132d9`), **§6 WCAG AA safe contrast fixes** (`99f7e98`), and the **deferred dark-migration tail** QuickenImport + TransferAnalysis (`5864ca0`) → **100% dark coverage** of reachable surfaces. Light mode stays the default; dark is opt-in via the toggle. **Frontend-only, no DB migration; reversible** by flipping the build arg back to `legacy`. Known deferred (logged, non-blocking): brand-affecting light-mode contrast (CR026 §15), collapsed-rail flyouts, P4 mobile expansion. |
| 2026-06-05 | **Released v2.16.1** — patch (CR023). **Sync-before-reconcile (6b):** `bankFeedClient.sync()` (`POST /v1/sync?max_age&force`) + a **fail-open** `syncUpstream()` (never throws — a bank-feed outage falls back to cached data), called at the start of `ingest()` (covers the 6am stage-only cron + manual refresh) and in the reconcile route (15-min freshness + balance re-ingest) before `reconcileToFeed`, so reconciliation never runs on morning-stale balances (the `feed_balances`-freeze lesson). Window via `BANK_FEED_SYNC_MAX_AGE_MIN` (default 60); reconcile response carries `_synced: fresh\|synced\|cached`. Unit test added; verified on prod (`_synced:"fresh"`). **No new DB migration.** Server+frontend rebuild. **Correction to the v2.16.0 note below:** the per-mapping `feed_sign` for Plaid/US negative-liability cards **did add a migration — `029_feed_balance_sign.sql`** — and shipped earlier in **v2.15.4** (2026-06-04) together with the **Luxury card 62 cutover** (first Plaid/US card; `feed_sign=+1`), not in 2.16.0. |
| 2026-06-05 | **Released v2.16.0** — minor; bundles two threads off shared `main`. **CR026 — UI revamp P1 + P2 (frontend-only, flag-gated OFF in prod):** a collapsible VS Code-style **sidebar** (`components/Sidebar/`) + top utility strip (`components/TopStrip.jsx`) behind `VITE_NAV_LAYOUT` (docker/prod default `legacy` top bar; dev default `sidebar`); IA regroup via additive `SIDEBAR_GROUPS`/`getSidebarNav()` in `routes.jsx`; **dark mode** — `[data-theme="dark"]` token-override layer in `index.css`, a `useTheme` store (default light, persisted, no-FOUC `index.html` script), Theme toggles in sidebar/top-strip, new `--info`/`--on-accent` tokens, and ~40 page/component CSS files migrated off hardcoded hex onto tokens (also repaired several dead undefined-token fallbacks); `/ui-preview` mockup route. **Prod impact: none visible** — prod stays on the legacy top bar and dark mode is unreachable there; this ships the feature dormant. **CR023 (backend, prod-affecting):** symmetric PS **lower** cutoff in `ingestPs.js` (stops re-promoting Quicken-backfilled pre-handoff rows; reuses col `027`, no migration) + audited set-once `seed-ps-lower-cutoffs.js`; `ps-exit-monitor.js` read-only PS-removal exit gate; per-mapping `feed_sign` for Plaid/US negative-liability balances; fin requests `?app=fin` from the bank-feed for account routing. Plus a CR025 planning doc + the CR023 PS→Feeds migration tracker. **No new DB migration.** Backend container rebuild + frontend rebuild; flag keeps the new UI off for prod users. |
| 2026-06-04 | **CR023 — symmetric PS *lower* cutoff (deleted-row resurrection fix).** A PS sync re-imported the 2,170 pre-handoff PKO rows the owner had deleted: PS dedups on `ps_id` vs the *live* `transactions` table, so a deleted pre-handoff row looks new and re-promotes (staging persists; PS fetch is `updated_since`-based, not date-bounded). The existing `promote_from_date` cutoff guarded only the **upper / bank-feed** boundary. Added the symmetric **lower** bound in [`ingestPs.js`](server/src/v2/routes/ingestPs.js) `syncStagingToTransactions`: a PS staging row dated **< its own `pocketsmith` mapping's `promote_from_date`** is excluded (the backfilling source — `quicken-import` — owns that era). One-line clause, dormant by default (NULL on every PS mapping), shares the `cutoffOn` gate; **no migration** (reuses col `027`). 2 DB tests added (suite 34/34). **Prod-applied (server-only container rebuild; frontend untouched so in-flight CR026 stayed out):** PKO acct 18 `pocketsmith.promote_from_date='2022-12-01'`, deleted the 2,170 `accepted=FALSE` re-imports (CSV backup kept), verified a scoped re-promote of a deleted `ps_id` re-inserts 0. End state acct 18: quicken ≤2022-11-26 → PS 2022-12-01→2026-05-28 → bank-feed 2026-05-29+. Audited set-once seeder `seed-ps-lower-cutoffs.js` (mirror of `seed-bankfeed-cutoffs.js`) carries the explicit per-account handoff dates (lower bound is operator-chosen, not auto-derived). Backend commit local-only (not pushed — CR026 commits sit unpushed ahead on `main`). Generalises to Chase + other Quicken-backfilled accounts (add a row to the seeder). |
| 2026-06-03 | **Released v2.14.1** — patch; frontend-only, no schema/API/route change. **Balance Trends — local-currency display** (`BalanceTrends.jsx`/`.css`): added a `USD` (default) / `Local` / `Both` currency selector beside the Interval pills. Pure render toggle — `flattenBalanceLeaves` now also captures the native balance (`node.total`, USD fallback) alongside `totalUSD`, so flipping currency needs no regenerate. `Local` formats each account in its own currency via `Intl` `currencyDisplay:"code"` (`PLN 153,300`); `Both` stacks the native figure over a muted USD figure per cell. The per-period **Total column stays USD in all three modes** (mixed-currency sums aren't meaningful). Excel export is mode-aware (`Both` emits native + `(USD)` columns per account; filename gains a `-{currencyKey}` suffix). Answers the "see values in local currency and/or USD" ask. |
| 2026-06-03 | **Released v2.14.0** — minor; accumulated additive work since v2.13.2, no schema/route change in the deployed app. **Balance Trends report transposed** (`BalanceTrends.jsx`/`.css`): the table now renders **period-ends down the rows × accounts across the columns** (was accounts × periods), with a sticky `Period` first column, each account column header carrying its native-currency sub-label, and the per-period **Total (selected, USD)** moved from a footer row to a trailing column (divider border, muted fill). Partial-period `(MTD/QTD/YTD)` marker + primary-color tint now sit on the period **row** label; Excel export re-oriented to match (Period rows, account columns, currency sub-row, Total column). Frontend-only. **CR024 tooling (feat):** `seed-bankfeed-cutoffs.js` generalizes the per-account cutover-cutoff seeder (PS-last-tx+1) beyond Fidelity to any mapped bank-feed account; VISA credit-card duplicate cleanup logged. **CR019 (fix):** `seed-cr019-coa.js` dry-run now resolves pending parents correctly. Also bundles docs-only CR021 (bank-feed v0.2.0 guarded `/v1/sync` + OCME consumer guide) and the CR023 planning stub. |
| 2026-06-03 | **bank-feed v0.2.0 — guarded `POST /v1/sync` for client-triggered refresh** (CR021-side; separate `~/Programs/fin/bank-feed/` repo, commit `5572d9c` — no fin release). Lets consumers (OCME, fin) offer an on-demand "Refresh from bank" button without a client read forcing a Google-Sheet pull per request and without overlapping syncs racing on the shared service. Additive within the `/v1` contract: `?max_age=<minutes>` skips the Sheet read if the last successful sync is younger (`{skipped:true, reason:"fresh", last_synced_at, age_minutes}`); `?force=true` bypasses; concurrent calls **coalesce** onto one running sync (`coalesced:true`). Plain `POST /v1/sync` unchanged (always syncs). New pure `freshnessDecision()` + module-level in-flight handle in `fintableSync.js`; scheduler routes through `requestSync` so the hourly cron coalesces with manual syncs. 6 new tests (`tests/syncGuard.test.js`), suite 72/72; rebuilt + verified live on :3007 (freshness skip / force / 3 concurrent → 1 `sync_jobs` row). Contract README + HANDOFFS updated. **Triggered by:** the new OCME bank-feed import — a second consumer of the same fintable Sheet over Tailscale. Hand-off guide added at [Documentation/OCME_BANK_FEED_IMPORT_GUIDE.md](OCME_BANK_FEED_IMPORT_GUIDE.md) (psproject commit `ae42ae7`): OCME consumes `/v1/*` (no Google code), stages+promotes with `source='bank-feed'`/`accepted=FALSE`, keeps R1 opt-in, drops R2 (single-source). Also surfaced/handled live: a new fintable account (`OC MEDYCYNY ESTETYCZNEJ`, **Bank Pekao** — a 3rd institution after PKO + Fidelity) the hourly poll hadn't yet picked up; a manual sync ingested it correctly (per-institution dispatch created connection 3, `PEKAO_PKOPPLPW`). |
| 2026-06-03 | **Released v2.13.2** — patch; frontend-only, no schema/API/route change. **Forecast Review "Change in Net Assets" bridge** (`FCReviewTable.jsx`, `FCReview.jsx`): replaced the old "Change in Net Worth" equity bridge (Operating excl Tax / Tax / Capital & Unrealized / Total — and a latent bug where the total used *gross Assets*, ignoring liabilities) with a reconciling decomposition **ΔNet Assets = Operating Cash Flow + Σ Unrealized G/L**. Operating CF = Income + Expense incl. tax (transfers excluded; via `resolveCashValue` so it ties to the rows above). Unrealized total is derived as `ΔNet Assets − Operating` (always reconciles); expandable into one row **per non-bank BS line** computed as `±Δbalance + line transfers` (signed +asset/−liability, FX revaluation captured, Bank Accounts excluded, per-module transfers rolled up to level-2 via `balanceAccountMap`); an "Other (unattributed)" row surfaces any gap. Total = `netAssetsByYear` diff. Added the section's own year header row (mirrors the Cash Flow Summary). **Design note:** the engine anchors the budget year (BaseYear) at each module's *Market Value* with no growth, so a non-zero budget-year per-line unrealized = `module Market Value − actual prior balance` — i.e. it flags a module whose Market Value is not calibrated to its actual (e.g. United Beverages MV 14M PLN ≈ ½ its 2025 actual). Per owner decision this re-basing is **intentionally treated as unrealized G/L** (a calibration signal), not suppressed. New props threaded `FCReview → FCReviewTable → EquityBridgeRows`: `balanceAccountMap`, `bankAccountLabels`. |
| 2026-06-03 | **Released v2.13.1** — patch; frontend-only, no schema/API/route change. **Forecast Review report UI fixes** (`FCReviewTable.jsx`, `index.css`): (1) the series-select checkboxes had inflated to 44×44px on coarse-pointer devices — the PWA WCAG tap-target rule (`@media (pointer: coarse)`, added in commit `7186ced`) applied to *every* checkbox; scoped a `.fc-review-table input[type="checkbox"]` opt-out (`min-height/width:0`) since the `<td>` already gives a large click area; (2) the Cash Flow Summary section (rendered far below the main `<thead>`) now repeats the year header row — years + `(Budget)`/`(Actual)` sub-labels + pre-forecast column shading — directly above its Income/Expense/Transfers rows so columns stay readable without scrolling back up. |
| 2026-06-03 | **Released v2.13.0** (and v2.12.0) — minor; additive. **CR024 Phase 2 — Fidelity investment-activity cash flow.** bank-feed shipped additive `activity_type` on `/v1/transactions` (compute-on-read from `raw.parsed.type`). fin: migrations `026_fidelity_activity.sql` (`bankfeed_staging.activity_type`/`.suppressed`) + `027_promote_from_date.sql` (`account_source_mappings.promote_from_date` cutover gate). `categorizeFidelityActivity` routes by SnapTrade type + per-account `trade_treatment` (INTEREST/DIVIDEND→income, Options BUY/SELL→Option Trade, other trades+REI→Transfer-Securities-Trades, CONTRIBUTION/WITHDRAWAL→Transfer-Bank, LOAN/JOURNALED/OPTIONEXPIRATION→suppress, PAYMENT/unknown/null→review). Promote assigns `category_id` (fail-loud name→id), suppresses net-zero plumbing, and honours the cutoff. **v2.12.0** shipped the categorizer dormant; **v2.13.0** added the cutover gate and activated it: a dry-run found blind dedup would double-count 26 ambiguous Fidelity rows (reports count `accepted=FALSE`), so activation used a per-account cutoff = PS-last-tx+1 (26/27→05-23, 28→05-27, 30/31→05-16). Prod activation promoted 60 Fidelity rows, 33 suppressed, 0 PS-overlap → zero double-count; Phase 1 balances intact; rows `accepted=FALSE`. Backend 188/188. |
| 2026-06-02 | **Released v2.11.0** — minor; additive, no breaking changes. **CR024 Phase 1 — Fidelity market-value balances:** migration `025_fidelity_feeds.sql` (`bankfeed_balances` cache + `account_source_mappings.balance_from_feed`/`trade_treatment`); `refreshBankFeedV2.ingestBalances()` folds `/v1/balances` into the cache on the cron `/ingest` path; `reports.js` balance-sheet **read-override** — `balance_from_feed` leaf accounts return the latest `feed_balances ≤ asOfDate` instead of `opening_balance+Σtx` (pre-coverage dates fall back to additive; parent categories aggregate overridden leaves); idempotent `seed-cr024-fidelity-mappings.js` maps the 5 Fidelity accounts `balance_from_feed=TRUE` keeping `ignored=TRUE` (transactions stay suppressed until Phase 2). Backend-only. Migration 025 applied to prod **before** the code deploy (pg_dump bracket). Verified on prod: Fidelity Bond corrected ~$1.01M→$1.20M, parents roll up, PKO unaffected, 0 transactions leaked, idempotent re-ingest; 178/178 backend tests. Phase 2 (activity-stream cash-flow categorization) still gated on the `Finance → bank-feed` `activity_type` handoff. |
| 2026-06-02 | **Released v2.10.0** — minor; one additive feature on top of v2.9.0, no breaking changes. **CR022 — category suggestions from history ("learn from my selections", Approach A):** new `categorySuggest` service + `POST /api/v2/transactions/category-suggestions` derive a merchant key per description (dedupe bank-feed doubling, strip refs/IBANs/locations, leading tokens) and suggest the category most often assigned to that merchant across accepted history (≥2 samples, >50% majority; null otherwise). A **Suggest categories** button on the Refresh Feeds review queue fills uncategorized rows as *pending* (never auto-accepts); self-improving as rows are accepted. Verified on prod: 18/28 uncategorized bank-feed rows got confident, sensible suggestions (e.g. Uber→Travel-Taxi 98%/57, Green Coffee→Food and Drink 100%/14). Frontend-only UI + one new read-only endpoint; no schema/migration change. **Note:** ships off shared `main`. |
| 2026-06-02 | **Released v2.9.0** — minor; accumulated additive work since v2.8.2, no breaking changes. **CR019 (Quicken import):** the prod cutover is now fully scripted (CR019 §23) and all idempotent with no manual SQL — new `ps-anchor.js` (reconcile non-backfill PS accounts to `closing_balance`, issue #3; skips feed-owned brokerage), `retire-handoff.js` (scripts the formerly-manual Fidelity 635 handoff), `seed-cr019-coa.js` (idempotent COA seeder, G1), `copy-quicken-to-prod.js` (dev→prod staging+mapping copy with name-translation, G2); `quicken-verify.js` PS-anchored balance-invariant + benign-WARN for promoted-0-row batches; PS-anchored calibration + value-only investment promote; **Rollback → Delete** (rolled_back batches now deletable, tx-count guarded). **CR022 (bank-feed parallel import):** Phase F prod parallel run live + Phase G observation; feeds-UI rework (Refresh Feeds page, Bank Feed Setup, per-source accept), Source column in the review queue, 'Transfer to account' cross-account review action, USD `base_amount` via FX for promoted bank-feed rows, reconciliation gate refinements. **Note:** ships both threads' work off shared `main`; no new migration since `024` (already on prod). The CR019 backfill remains dev-only (its prod cutover is a separate scripted run via §23); CR022 stays additive/parallel to PocketSmith. |
| 2026-06-01 | **Cash Flow Periods (renamed) + Balance Sheet Periods (new report).** `/cash-flow-monthly` renamed to `/cash-flow-periods` (page `CashFlowMonthly.jsx` → `CashFlowPeriods.jsx`, "Monthly"/"Month-by-month" wording dropped) and gained a **Period** selector (Month / Quarter / Year); the month-span generator was generalized to emit per-period spans clamped to the selected range. New **Balance Sheet Periods** report at `/balance-sheet-periods` renders the hierarchical balance sheet as of the last day of each Month/Quarter/Year in range, one column per period end — reusing `BalanceReport` (its hard 3-column cap lifted via a new `maxPeriods` prop) and the existing `GET /reports/balance` endpoint, with Balance Trends' future-period/partial-snapshot handling. The period-end series helpers (`buildEndDateSeries`, `planColumns`, `formatColumnHeader`, etc.) were extracted from `BalanceTrends.jsx` into shared `utils/periodHelpers.js` and `BalanceTrends` refactored to import them. **Frontend-only** — no backend, schema, or API changes (both reports consume existing endpoints). Build + lint + 96 Vitest helper tests pass. Note: old `/cash-flow-monthly` bookmarks now 404; nav regenerates from `routes.jsx`. |
| 2026-05-31 | **Version scripts no longer clobber `.env`** — `bump-version.sh` and `deploy-to-production.sh` previously did `cat > .env` (whole-file overwrite with just `VITE_APP_VERSION`), which silently wiped manually-added vars; the v2.8.0 bump destroyed `BANK_FEED_URL` / `BANK_FEED_API_KEY` this way. Both scripts now `sed` the version line **in place**, preserving other vars (see [FC_PROJECT_STRUCTURE.md §13](FC_PROJECT_STRUCTURE.md#13-environment-variables)). Also noted: `.env` is in `.gitignore` but was force-added (still tracked) — `git rm --cached .env` recommended so the secret can never be staged. The actual key value was never committed (verified across all history). |
| 2026-05-31 | **Released v2.8.0** — cuts a prod release of the work accumulated since v2.7.18 (minor: new feature surfaces, all additive). Ships: **CR019** Quicken import pivot to the 1→1 cash-promote model with post-hoc Transfer Analysis matching, auto-match removed from promote, role-aware atomic bulk-create, and mapping-UX polish; **CR021** Phase 7 read-only bank-feed integration spike (proxy + diagnostic page); **CR022** Phase A (`023_bank_feed_import.sql` — `bank_feed_external_id` partial-unique column, `bankfeed_staging` table, R1 `account_source_mappings.ignored`, R2 link target) + Phase B (bank-feed converter, staging repository, cross-source dedup helper + Jest tests). Bank-feed import paths are dormant/additive (no active scheduler or route wired in this release); migration 023 runs on the prod DB. **Note:** released from the `cr022/implementation` branch ahead of the CR022 dev-walkthrough hard gate — a deliberate, owner-approved early prod push of dormant infrastructure. |
| 2026-05-31 | **CR022 R1 + R2 propagated through the full CR** ([CR022](#cr022)). The two requirements added to §2.3 on 2026-05-31 (R1 per-account opt-in via `account_source_mappings.ignored`, unmapped=pending; R2 cross-source dedup linking bank-feed rows onto matching `source='pocketsmith'` rows behind `BANK_FEED_DEDUP_ENABLED`) were flowed into §3 architecture, §4 phased plan, §5 test plan, §6 migration (`ignored` column added to `023`), §7 dev walkthrough (R1 ignore + R2 synthetic-duplicate steps + link-aware rollback), §8 risks (the old "cross-source dedup out of scope" row replaced; false-merge + R1-lockout rows added), and §9 decision log. Doc-only — no code yet. Notable correction: "PS code paths untouched" softened to "one guarded reverse-dedup lookup in `refreshPsApiV2.js`" (R2.2). |
| 2026-05-30 | **CR022 opened — Bank Feed Parallel Import** ([CR022](#cr022)). Additive second import route from CR021's bank-feed service into fin's existing `transactions` table with `source='bank-feed'`, running alongside PocketSmith. One schema migration (`023_bank_feed_import.sql` — adds `bank_feed_external_id VARCHAR(100)` partial-unique on `transactions`, parallel `bankfeed_staging` table, `sync_metadata` seed). Reuses source-agnostic review queue and source-discriminated `account_source_mappings`. PS code paths untouched. Dev walkthrough on `fin-server-dev` with `pg_dump` rollback bracket is a hard gate before prod push. PS removal deferred to a future CR after ≥1-month parallel-run observation. |
| 2026-05-30 | **CR021 Phases 0–3 shipped — bank-feed live with real PKO data.** Upstream landed on **fintable.io via Google Sheets** after a multi-round Phase 0 (banksync.io rejected for no-PKO catalog; moneysheets.io rejected for no-developer-API + single-account/single-currency template limit; GoCardless direct closed new signups; Plaid Production stayed in risk-review). Fintable's Sheet preserves the full GoCardless JSON payload per transaction. New repo at `~/Programs/fin/bank-feed/` (github.com/cfbieder/bank-feed, private): Express + Postgres + `googleapis` service-account auth + hourly scheduler. End-to-end smoke against the live Sheet ingested 7 PKO accounts, 127 transactions, 7 balance snapshots; idempotent on `(account_id, external_id)`. v3 fin cutover remains a future CR022. |
| 2026-05-28 | **CR021 opened — Bank Feed Service** ([CR021](#cr021)). Decision to replace PocketSmith via a standalone microservice exposing a versioned REST contract (`/v1/*`), rather than CR014's in-app dual-provider integration. Upstream investigation revealed banksync.io is a regulated-entity wrap over Plaid, so the practical upstream candidates collapse to Plaid (direct, free Development tier — feasibility TBD for PKO/PSD2 individual access), GoCardless (free EU-friendly tier), or banksync.io (paid, sidesteps regulatory access). Phase 0 (1 day, no code) resolves the upstream choice. CR014 marked SUPERSEDED; CR015 marked OBSOLETE. v3 main-app cutover deferred to a future CR022. |
| 2026-05-20 | **CR016 closed — Frontend test framework (Vitest) complete.** Final state: 96 tests across 5 helper modules in `frontend/src/utils/__tests__/` — `dateHelpers` (21), `formatters` (25), `treeTraversal` (17), `forecastHelpers` (20), `cashFlowHelpers` (13). Infrastructure: `vitest@^2.1.9` + `jsdom@^25` in devDeps; `npm test` / `npm run test:watch` scripts; standalone `vitest.config.js` (jsdom env, mirrors Vite path aliases); exits non-zero on failure. Deterministic via `vi.useFakeTimers()` where time-sensitive; no network, no real DB. Component/hook tests and Playwright E2E deferred to future CRs. |
| 2026-05-19 | **Balance Trends — auto-regenerate on interval change** ([CR018](#cr018)). Switching the Month/Quarter/Year pill now re-runs `handleGenerate` automatically so the column count + headers stay in sync with the selected interval (previously the table kept the prior interval's columns until the user clicked Generate). Year/month dropdowns still require an explicit Generate. |
| 2026-05-19 | **Balance Trends — year range + Interval (Month/Quarter/Year) + future-period filter** ([CR018](#cr018)). `PeriodSelector` extended with an opt-in `enableYearRange` prop that adds a Year (to) dropdown alongside Year (from) in Custom mode (no behavior change for other pages). Interval pill bar (Month default / Quarter / Year) controls column granularity. Columns whose period start is in the future are dropped; the current period is included with snapshot as-of today and its header gains an `(MTD)`/`(QTD)`/`(YTD)` suffix in primary color. Page state renamed (`monthEnds` → `columns: [{label, asOf, isPartial}]`) to model the partial-period case cleanly; `interval` state renamed to `intervalKey` to avoid shadowing the global `setInterval`. |
| 2026-05-19 | **Released v2.7.15** — Balance Trends report at `/balance-trends` (Reports & Graphs > Reports, [CR018](#cr018)). Month-end USD balances across the selected period for one or more BS accounts. Reuses `HierarchyFilter` (BS COA groups, right-click solo-select) + single-period `PeriodSelector` (no Transfers/Unrealized, budget year hidden). Table renders accounts × month-ends with a Total (selected, USD) footer row; Excel export. Reuses `Rest.fetchBalanceReport(asOfDate)` once per month-end (parallel) and flattens each tree to a leaf map. |
| 2026-05-18 | **Released v2.7.14** — HierarchyFilter checklist gains a type-to-narrow search input above the leaf list (only visible when a specific group is active; resets on group change; case-insensitive substring; empty-state message). Affects Budget Worksheet and Transaction pages that consume `HierarchyFilter`. |
| 2026-05-04 | **Forecast scenario copy — faithful field carry-over** — `copyScenario()` was inserting only a subset of source columns: `forecast_modules` missed `setup_status` / `cash_sweep_target` / `tax_rate_override`; `forecast_income_expense` missed `setup_status` / `fc_line_id` / `budget_source_year`; the new-scenario INSERT into `forecast_scenarios` missed `cash_sweep_low` / `cash_sweep_high`. Default `setup_status='new'` was silently filtered out by the generator (`COALESCE(setup_status,'new') NOT IN ('new','exclude')`), producing "Modules: 0, Entries: 0" on every copy. Sweep band loss caused ~109 fewer entries on copied scenarios (no cash sweep / no downstream rebalances). Existing-target branch now also mirrors sweep band from source. |
| 2026-05-04 | **Forecast scenario copy — "Update PY Actual values" path corrected** — when the Copy modal's PY-refresh checkbox was used, the year-end balance SQL summed `t.base_amount` (always USD) into `balance_lc`, then wrote that USD figure into `forecast_modules.base_value` (local currency) and 0 into `base_value_usd` for non-USD accounts. Engine then double-converted, halving the asset base for PLN/EUR modules. Fixed by summing `t.amount WHERE t.currency = a.currency` into `balance_lc`. Also stopped overwriting `market_value` / `market_value_usd` from ledger — those are broker-reported and can't be derived from the transaction stream. |
| 2026-04-28 | **Documentation reorganization** — split into PROJECT_STRUCTURE.md (current state) + NEXT_STEPS.md (plan) + CRs/ folder + Testing/ + Archive/. CR_INDEX.md added. CLAUDE.md cross-references updated. |
| 2026-04-28 | **Categories table collapsed into `accounts`** ([CR013](#cr013), migration 021). FK columns repointed; `categories` and `category_source_mappings` dropped. HTTP smoke test (`smoke-after-021.js`) added — 17 endpoint checks against live server. |
| 2026-04-28 | **Transfer Analysis: FX category fuzzy matching now triggers in production:** The `isFxCategory` predicate in `GET /api/v2/transactions/transfer-analysis` checked `name === 'FX'`, but the production category is named `Transfer - FX` (id 208), so the FX branch (1% amount tolerance, 1-day window) never fired and every FX pair fell into the standard exact-match path. Result: a full screen of unmatched WISE EUR↔USD transfers. Fixed by accepting both names. |
| 2026-04-28 | **AI Review context restructured to prevent flow-vs-balance misreads** — `buildForecastContext()` split into three explicit sections: (1) Annual P&L Flows by Year (with sign convention; `Bank Accounts` clarified as flow not balance), (2) Balance Sheet — Year-End Market Value, (3) Cash Account & Cash Sweep Activity. Sweep entries excluded from P&L Flows. |
| 2026-04-28 | **nginx 301 trailing-slash bug broke + New Review:** `location /api/v2/ai-review/` triggered nginx's directory-canonicalization redirect; browsers convert 301-redirected POSTs to GETs. Fixed via regex location `~ ^/api/v2/ai-review(/|$)`. |
| 2026-04-28 | **AI Review made async with polling + browser notifications** ([CR006](#cr006), migration 020). 202 returned immediately; background worker; 8s polling; browser notification when tab hidden; pulsing red dot for unread. nginx `proxy_read_timeout` raised to 360s. |
| 2026-04-27 | **AI Review migrated from Anthropic API to local `ocr-llm` gateway** ([CR006](#cr006)). Local-only fallback chain `ollama_heavy → ollama_mid`. `LLM_GATEWAY_URL` env var. Anthropic SDK removed. Cross-repo coordinated via `ocr-llm/HANDOFFS.md`. |
| 2026-04-13 | **Ledger date filter fix + duplicate detection** — backend now accepts `fromDate`/`toDate` (was silently ignoring); duplicate-finder client-side analysis (same amount+currency within 3 days, or identical descriptions). |
| 2026-04-04 | **Graph Quick Adjustments (FC Exp + FC Module)** — double-clicking a forecast graph point opens an inline adjustment modal. FC Exp modal edits periodic changes (Fixed/Percent/One-Off); FC Module modal edits Invest/Dispose transfers. Save & Regenerate refreshes graph. |
| 2026-04-04 | **Income-Sweep Convergence** ([CR005](#cr005)) — Step 7b iterative loop in `index.js` converges yield income with sweep-adjusted MV. Mirrors Excel Goal Seek for the income↔sweep circular dependency. |
| 2026-04-03 | **Global DATE timezone fix** — root-cause fix in `postgres.js` (`types.setTypeParser(1082, val => val)`) plus frontend uses local-time `getFullYear()`/`getMonth()`/`getDate()` instead of `.toISOString().split("T")[0]`. Affected pages: Budget Realization drilldown, Budget Worksheet popups, Balance pages, date helpers. |
| 2026-04-02 | **AI Review API key security fix** — moved Anthropic key from `appdata.json` to `ANTHROPIC_API_KEY` env var (later replaced entirely by `LLM_GATEWAY_URL`). |
| 2026-04-02 | **AI Review UX fixes** — context moved into first user message, per-review delete buttons, drawer z-index above nav. |
| 2026-04-02 | **FC Review enhancements** — account double-click graph; graph base year fix via shared `resolveCashValue()` / `resolveBalanceValue()` helpers. |
| 2026-03-29 | **Ledger category filter + amount total** — category dropdown populated client-side; total Amount in table footer for filtered rows. |
| 2026-03-28 | **Budget Worksheet filters → HierarchyFilter** ([CR008](#cr008)) — pill-style filters with leaf counts; CategorySelector retained only for right-click quick-pick. |
| 2026-03-28 | **FC Module Phase 2B-5 Engine Update** ([CR003](#cr003)) — engine resolves FC Lines; `expense_growth_method` (`inflation` vs `pct_of_value`); 6 new tests. |
| 2026-03-28 | **FC Module Phase 2B-4b Add from Actuals** — replaces "Seed from Actuals"; tree view with leaf pre-selection and parent aggregation. |
| 2026-03-28 | **FC Module Phase 2B-4 Forecast Expenses Integration** — "Add from FC Lines" replaces "Seed Budget" + "Coverage". |
| 2026-03-28 | **Unmatched modules endpoint fix** — exclude children of matched parent accounts. |
| 2026-03-27 | **Manual Match Groups** ([CR009](#cr009), migration 005) — persistent many-to-one transfer matching. |
| 2026-03-27 | **`transfer_matched` flag + Actuals Transfer Status filter** ([CR009](#cr009), migration 006). |
| 2026-03-27 | **HierarchyFilter component** ([CR008](#cr008)) — two-stage cascading filter for transaction pages. |
| 2026-03-27 | **Day-level date editing in transaction edit modals** — `TransactionDateSelector` now includes Day with leap-year-aware clamping. |
| 2026-03-26 | **Balance Sheet page redesign** — KPI cards, inline period controls, expand/collapse icon buttons, Net Worth footer. |
| 2026-03-26 | **Budget Worksheet page redesign** — two-panel side-by-side layout, KPI cards, right-click category quick-pick. |
| 2026-03-26 | **COA Management page redesign + Move feature** ([CR010](#cr010)). |
| 2026-03-26 | **Transaction pages redesign** ([CR008](#cr008)) — `/trans-actual` and `/trans-budget` with KPI cards, slide-in drawers, contextual selection bar. |
| 2026-03-16 | **Trans-Actual filter & account selector fixes** + leaf-only account filtering (`leafOnly` param on `GET /api/v2/accounts`). |
| 2026-03-15 | **Per-row action buttons in TransactionTable** — Split/Neutralize/Change Category moved from top-bar to per-row. |
| 2026-03-15 | **Category Trend graph** at `/category-trend` (Reports & Graphs > Graphs) — actual vs budget grouped bar chart. |
| 2026-03-14 | **Neutralize Transaction for brokerage accounts** — one-click offset entry tagged "Transfer - Securities Trades". |
| 2026-03-13 | **Forecast FX moved to Forecasting category & income/expense FX fix** in `fcbuilder-incexp.js`. |
| 2026-03-13 | **Monthly Budget FX Rates** at `/budget-fx` (migration 004). |
| 2026-03-13 | **Docker cleanup in backup cron** — prunes build cache + dangling images > 48h. |
| 2026-03-11 | **KPI Summary Cards** — `KpiCards.jsx` with Recharts mini-charts on Budget Realization and Forecast Review. |
| 2026-03-11 | **Export to Excel** — SheetJS-based exports on Balance Sheet, Cash Flow, Budget Realization, Actual + Budget Transactions. |
| 2026-03-11 | **Liability expense percent fix** in `fcbuilder-module.js`. |
| 2026-03-03 | **Quick Add for missing categories** in COA Management. |
| 2026-02-28 | **Split Transaction feature** on `/refresh-ps` and `/trans-actual`. |
| 2026-02-21 | **Full responsive/mobile-friendly UI** — `@media` breakpoints across 14 CSS files; `backdrop-filter` fix for slide-out drawer. |
| 2026-02-17 | **Clear Filters button** on `/trans-actual` and `/trans-budget`. |
| 2026-02-17 | **Transaction edit modal improvements** — restricted to Description + Category on `/trans-actual`; hierarchical CategorySelector. |
| 2026-02-17 | **Transaction acceptance** (migration 003) — `accepted` flag protects manual edits from PS refresh overwrite. |
| 2026-02-16 | **Balance Sheet UI improvements** — Net Worth footer, P1/P2/P3 pill badges, single-row filter bar. |
| 2026-02-16 | **Transaction table column optimization** — `noWrap` on date/amount columns; description ellipsis. |
| 2026-02-16 | **Route reorganization** — `/refresh-ps` moved from Database to Transactions category. |
| 2026-02-16 | **Transaction pages filter bar redesign** — shared `PeriodSelector`/`CategorySelector`/`AccountSelector`. |
| 2026-02-16 | **RefreshPS page enhancements** — Review & Edit New tab; integrated CategorySelector in TransactionEditModal. |
| 2026-02-16 | **Budget Worksheet UI overhaul** — three reusable shared selectors created. |
| 2026-02-15 | **Budget Variances page** at `/budget-variances`. |
| 2026-02-15 | **Frontend refactoring** ([CR002](#cr002)) — unified Transaction module, hook extraction, ~2,200 net LOC reduction. |
| 2026-02-14 | **V1 compat removal** + endpoint fixes + V1 retirement + COA migration to SQL + timezone fix + data fixes ([CR001](#cr001)). |
| 2026-02-13 | Version bumped to v2.0.6. Documentation updates. |
| 2026-02-08 | Decommissioned dev machine; VM is sole environment. Database restored to VM via `pg_dump`/`pg_restore`. Server Dockerfile fixed (`postgresql-client-16`). VM recreated after loss; provisioning scripts added. |
| 2026-02-07 | Migrated from dev machine to KVM VM at `192.168.1.82`. |
| Earlier | Migrated from MongoDB to PostgreSQL 16 ([CR001](#cr001)). |
| Earlier | UI overhaul: Lucide icons, shared layout, category landing pages. |

---

*Last updated: 2026-06-01*
